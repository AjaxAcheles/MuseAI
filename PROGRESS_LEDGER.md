# Progress Ledger

The living record of what is real, what is a stub, and what a session left half-done. The agent updates this in-repo copy at the end of every session, and it is read at the start of every session.

## Current position
- **Last completed:** `04.05`
- **Next up:** `04.06`
- **Current session block:** S4

## Status key
`done` - built, done-check passes | `stub` - placeholder with docstring, no real logic | `half-done` - started, not passing (see notes)

## Ledger
| ID | Module | Produced | Status | Notes / open items | Done-check |
|----|--------|----------|--------|--------------------|------------|
| 01.01 | Scaffold | Full directory tree, `.gitignore`, `.gitkeep` placeholders | done | Existing repo metadata was preserved. | Every directory in the merged tree exists; Git is initialized; `.gitignore` lists `_design/`. |
| 01.02 | Scaffold | `pyproject.toml`, `.env.example`, `uv.lock`, package markers, importable source stubs, no-op `app.py` entry point | done | Native Windows cannot install `falkordblite`; WSL is the intended runtime. User confirmed `app.py` boots in WSL. | Stubs import without error; `uv run app.py` starts and stops cleanly in WSL. |
| 01.03 | Scaffold | Root `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` | done | In-repo ledger starts future-session tracking. | `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` exist at repo root; Next up reads `02.01`. |
| 02.01 | Config/Startup | Typed Pydantic v2 config models (`AppConfig`/endpoints/thresholds/runtime/logging) + `load_config()` with env secret overrides | done | Permissive parse; `extra='forbid'` fail-fast deferred to 02.02. `api_key` sourced from `{ENDPOINT}_API_KEY` env (defaults `""`). `base_url` `${...}` placeholders left literal (secrets-only override per scope). Design's `supports_inference_antislop` not in config.yaml, so not modeled. | `load_config('config.yaml')` returns `AppConfig`; nested endpoint/threshold/runtime fields are typed values; clean under `-W error::UserWarning`. |
| 02.02 | Config/Startup | `extra="forbid"` on all six config models — fail-fast on unknown/mistyped keys at parse time | done | Pydantic `ValidationError` reports the offending key's `loc` (e.g. `('bogus_top_level_key',)`, `('endpoints','planner','base_url')`). `RuntimeConfig` keeps `protected_namespaces=()` alongside forbid. | Temp config with a bogus top-level key and a nested `base_url`→`base_uri` typo each raise `ValidationError` locating the key; real `config.yaml` still loads. |
| 02.03 | Config/Startup | Shared node-event logger: `get_logger(node_name)` writes JSON lines to a rotating `logs/fsm.log`; `log_node_event()` emits node, pointer, duration, outcome, and optional error fields | done | Handler attaches once per logger name, `propagate=False`, and level resolves from strict `config.yaml` logging config. Rotation envelope remains the section 3.1 file-size housekeeping default. | Strict config load verified; three events append as three valid JSON lines; a tiny-cap done-check rotates to `fsm.log.1`/`fsm.log.2` while every retained line parses as one JSON object. |
| 02.04 | Config/Startup | Dedicated inference-boundary logger: `get_llm_io_logger()` writes JSON lines to rotating `logs/llm_io.log`; `log_llm_call()` records full request payload, final response text, and duration | done | Separate from the FSM node-event logger and not wired into `call_llm()` yet. Records only the final assembled response, not streaming chunks. | Simulated call wrote exactly one valid JSON line with request, response, and duration; temp check confirmed no `fsm.log` was created. |
| 02.05 | Config/Startup | Resource lifecycle: `init_resources(config)` creates runtime directories and placeholder store artifacts; `reset_resources(config)` wipes documented file-based artifacts and reinitializes them | done | Store initializers and `scan_startup_crash_sentinel()` are labelled no-op stubs pending their owning store/crash modules. Reset is not wired to an HTTP route. | Temp-root done-check: init created data/log directories and store artifacts; reset removed SQLite, Graphiti, Chroma, event-log, style JSON, and snapshot ZIP artifacts; init and reset each ran twice cleanly. |
| 02.T1 | Config/Startup (test) | Synthetic config-loader tests for valid typed load, unknown key, type mismatch, and missing endpoint secrets; loader now rejects missing or empty endpoint API secrets | done | Endpoint `api_key` is required and populated from `{ENDPOINT}_API_KEY`; absent or empty secrets fail config validation instead of silently becoming `""`. | `python -m pytest tests/test_config.py -q --basetemp .pytest_tmp` passed: 4 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_config.py -q` because uv cannot access/remove the WSL-style `.venv\lib64`; rerun in WSL should use the fixed loader. |
| 02.T2 | Config/Startup (test) | Synthetic observability/lifecycle tests for node logging, inference-I/O logging, resource init/reset, and crash-sentinel no-op behavior | done | Module A (Config/Startup/Observability) is complete: strict config, both durable loggers, resource lifecycle/reset, and focused synthetic tests are present. Store init signatures and crash-sentinel scan remain honest no-op stubs for later modules. | `python -m pytest tests/test_observability.py -q --basetemp .pytest_tmp_obs` passed: 3 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 7 passed. Local Codex Windows sandbox could not complete exact `uv run pytest ...` because uv cannot rebuild the locked WSL-style `.venv`; rerun in WSL should exercise the same tests. |
| 03.01 | Coordinator/State Machine | Strict Pydantic v2 `FSM_Pointer` schema with `arc_id`, `chapter_id`, `scene_id`, `beat_index`; strict Pydantic v2 `FailureObject` schema with `error_code`, `offending_text`, `suggested_fix`, `critic_source`; `failure_object_json_schema()` helper | done | `OrchestratorState` remains an honest stub for 03.02. `FailureObject` keeps open string fields because the design gives examples, not exhaustive enums. | `python -c "from fsm.state import FSM_Pointer, FailureObject, failure_object_json_schema; print(FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(FailureObject(error_code='PACING_ISSUE', offending_text='too fast', suggested_fix='slow down', critic_source='pacing')); print(bool(failure_object_json_schema()))"` passed and printed both model instances plus `True`. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv cannot remove the WSL-style `.venv\lib64`; rerun in WSL should exercise the same schema. |
| 03.02 | Coordinator/State Machine | `OrchestratorState` TypedDict with documented fields and annotated list reducers; `accumulate_or_reset()`; `make_initial_state()` with fresh mutable defaults and validated overrides | done | No graph wiring, resource initialization, node execution, routing, or commit-reset behavior is implemented here. | `python -c "from fsm.state import FSM_Pointer, make_initial_state, accumulate_or_reset; s = make_initial_state('proj_1', FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(sorted(s.keys())); print(accumulate_or_reset([1], [2])); print(accumulate_or_reset([1], []))"` passed and reset printed `[]`; additional checks confirmed fresh mutable defaults and clear unknown-override rejection. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv rebuilt `.venv` but failed to spawn `python`; rerun in WSL should exercise the same code. |
| 03.T1 | Coordinator/State Machine (test) | Isolated synthetic tests for `FSM_Pointer`, `FailureObject`, `OrchestratorState`, `accumulate_or_reset()`, and `make_initial_state()` defaults/overrides/reducer contract | done | M01 pass-1 state-types batch is tested. Tests use in-memory synthetic fixtures only; no graph, node, router, LLM, or memory-store integration is exercised. | `python -m pytest tests/test_state.py -q` passed: 8 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 18 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_state.py -q` or `uv run pytest -q` because uv could not remove the active `.venv\lib64`; rerun in WSL should exercise the same tests. |
| 04.01 | Persistent Memory | `connect_db(db_path)` in `memory/sqlite_db.py`: raw `sqlite3` connection lifecycle accepting `str \| Path`, creating the parent directory, setting `row_factory = sqlite3.Row`, and enabling `PRAGMA foreign_keys = ON` per connection; private `_resolve_db_path()` helper | done | Connection setup only — no tables, reads, writes, CommitIntent scanning, or runtime wiring yet (next paste creates the schema). The `init_db()` no-op stub is retained so `core/runtime.py:16` keeps importing cleanly. Docstring opens with the M02 (`& Interfaces`) tag. | Micro-check printed `1` (foreign keys ON). Exact `uv run python -c ...` could not complete in the Windows sandbox — `uv` cannot remove the WSL-style `.venv\lib64` (same blocker as 03.x); ran the identical snippet under system `python` instead, which printed `1`. Follow-up confirmed `sqlite3.Row` rows, nested parent-dir creation, and a clean `core.runtime` import. Rerun under WSL should exercise the same code. |
| 04.02 | Persistent Memory | `init_db(db_path)` in `memory/sqlite_db.py`: opens its own `connect_db` connection, creates the outline/planning schema (`Arcs`, `Chapters`, `Scenes`, `Beats`, `Threads`) with `CREATE TABLE IF NOT EXISTS`, commits, and closes; schema held in module-level `_SCHEMA_STATEMENTS` | done | Signature changed from the 02.05 stub's `init_db(config, db_path)` to `init_db(db_path)` per the prompt. **⚠ Latent break to flag:** `core/runtime.py:34` still calls `init_db(config, SQLITE_DB_PATH)` (config-first, 2 positional args) — that call will now raise `TypeError` until the runtime-wiring increment updates it. Import line `from memory.sqlite_db import init_db` is unaffected. `Beats` is minimal (id/scene_id/beat_index/status) because §2.1 gives no Beats CREATE TABLE — reason commented in code; prose/PAD columns deferred. CHECK vocabularies and FK refs match §2.1; `Scenes.ordering` present for `ordering ASC` reads. Characters/CharacterEmotions/CommitIntent/RaptorNodes deferred to next paste. | Micro-check listed all five tables `['Arcs','Beats','Chapters','Scenes','Threads']`. Exact `uv run python -c ...` again blocked by the Windows `.venv\lib64` removal error (same as 03.x/04.01); ran the identical snippet under system `python`. Follow-up confirmed idempotent double-init, `Scenes.ordering`, status `CHECK` firing on bad value, and FK enforcement on a missing parent (proves `PRAGMA foreign_keys = ON` carries through). Rerun under WSL should exercise the same code. |
| 04.03 | Persistent Memory | Extended `init_db(db_path)` with the remaining relational tables (`Characters`, `CharacterEmotions`, `CommitIntent`, `RaptorNodes`) and a `_INDEX_STATEMENTS` set of deterministic-read indexes; all created via `IF NOT EXISTS` | done | PAD bounds enforced at the store level: `pleasure`/`arousal`/`dominance` each `CHECK(... BETWEEN -1.00 AND 1.00)`. `CommitIntent`/`RaptorNodes` verbatim from §2.1; `CommitIntent.status CHECK(IN ('pending','committed'))` + `idx_commitintent_status` make pending rows discoverable without scanning unrelated tables. `Characters`/`CharacterEmotions` under-specified in §2.1 (no CREATE TABLE) → kept minimal (`CharacterEmotions` is one current-state PAD row per character; per-beat history lives in the event ledger) with reasons commented. Indexes: chapters→arc FK, scenes(chapter_id,ordering), beats(scene_id,beat_index), threads.status, commitintent.status, raptornodes.parent_id, raptornodes.level. No writes/reads/runtime-wiring; M10 owns CommitIntent orchestration. **Note:** the paste's "Finally" block had stale numbering (referred to this paste as `04.01`); recorded honestly as `04.03` since `04.01`/`04.02` are already done. | Done-check listed all 9 tables and exited 0 after a double `init_db`. Exact `uv run python -c ...` again blocked by the Windows `.venv\lib64` removal error (same as 03.x/04.01/04.02); ran the identical snippet under system `python`. Follow-up confirmed data survives re-init (idempotent, no drop), PAD `CHECK` firing on all three axes, `CommitIntent.status` `CHECK` firing, and all seven indexes present. Rerun under WSL should exercise the same code. |
| 04.04 | Persistent Memory | `upsert_beat_commit(db_path, *, beat_id, scene_id, beat_index, prose, word_count=None, committed_at=None, status='completed')` in `memory/sqlite_db.py`: first relational write primitive — idempotent committed-beat upsert keyed by `beat_id`. Extended `Beats` with the now-documented committed-state columns `prose` (nullable), `word_count` (default 0), `committed_at`. | done | Implements the SQLite half of node_commit_transaction step 2 for committed prose only (per §2.4/node 12). Explicit keyword signature (no untyped blob); committed/validated text only. `ON CONFLICT(id) DO UPDATE` so replay updates in place — never duplicates. `word_count` defaults to `len(prose.split())` (backs `_commit_router` SUM(word_count)); `committed_at` defaults to UTC ISO-8601. Calls idempotent `init_db` first; single write wrapped in explicit `BEGIN`/commit with `except: rollback; raise` so a beat is never partially applied. PAD/thread sub-writes, `.jsonl`, Graphiti, CommitIntent orchestration (M10), and FSM state deferred. The `Beats` committed-state columns fulfil the `04.02` deferral comment. **Carryover still open:** `core/runtime.py:34` `init_db(config, …)` vs single-arg `init_db` mismatch (runtime-wiring increment). | Micro-check: seeded Arc→Chapter→Scene (FK on `Beats.scene_id` is enforced), upserted the same `beat_id` twice → `COUNT(*) WHERE id='b1'` is `1`, prose updated in place, `word_count`=5, status `completed`, `committed_at` set, exit 0. Exact `uv run python -c ...` again blocked by Windows `.venv\lib64` removal (same as 03.x/04.0x); ran under system `python`. Follow-up confirmed FK-violation path rolls back, re-raises, and leaves no partial row. Rerun under WSL should exercise the same code. |
| 04.05 | Persistent Memory | Extended `upsert_beat_commit` with `pad_states` and `thread_updates`, bundled into the beat commit's single atomic transaction; private `_upsert_pad_states` / `_apply_thread_updates` helpers. Re-keyed `CharacterEmotions` to PRIMARY KEY `(beat_id, character_id)` (added `beat_id` FK to Beats) + index `idx_characteremotions_character`. | done | PAD bundled inside the beat commit (not a standalone event), upserted on `(beat_id, character_id)` so replay updates in place; PAD `[-1.00,1.00]` CHECKs preserved. Thread updates applied idempotently by id via `UPDATE Threads SET status` under the existing status CHECK (thread creation stays the planner's job). **Supersedes the 04.03 minimal guess** (PAD was PK=`character_id`, current-state-only) — node 12 step 2 specifies PAD written "upsert keyed by beat_id", so the per-beat composite key is correct; added the character FK index to keep the 04.03 "FK lookups indexed" invariant. No numeric thresholds added. Read helpers, `.jsonl`, Graphiti, crash replay, M10 orchestration still deferred. Numbering: prompt's "Finally" said `04.02→done / Next up 04.03` (stale, already done) — recorded honestly as `04.05`. Carryover still open: `core/runtime.py:34` `init_db(config,…)` signature mismatch. | Done-check: synthetic beat commit (2 characters + 1 thread update) written twice → 1 beat row, exactly 1 PAD row per `(beat_id, character_id)` (2 total), thread status applied; exit 0. Exact `uv run python -c ...` again blocked by Windows `.venv\lib64` removal; ran under system `python`. Follow-up confirmed whole-commit atomicity: an out-of-bounds PAD value fires the CHECK, rolls back, and leaves no beat or PAD row. Rerun under WSL should exercise the same code. |

*(Append a row per increment. Keep "Current position" accurate - it is read first each session.)*

## Stub inventory (deliberate no-ops, wired later)
- Store init signatures called by `init_resources()` - no-op/placeholders until the memory-store modules land (04).
- `scan_startup_crash_sentinel()` - no-op until CommitIntent records and crash recovery land (13).
- `core.antislop.detect_slop()` and `core.antislop.resolve_slop()` - no-op passthroughs until anti-slop implementation.
- `memory.graphiti_client._apply_event()` - intentional early no-op for graph writes during crash-recovery scaffolding.
