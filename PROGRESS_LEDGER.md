# Progress Ledger

The living record of what is real, what is a stub, and what a session left half-done. The agent updates this in-repo copy at the end of every session, and it is read at the start of every session.

## Current position
- **Last completed:** `INT-A·B`
- **Next up:** `05.01` (next module — LLM Inference Boundary, M04. Remaining M02 stores — Graphiti/RAPTOR/Chroma/Style — stay deferred stubs per the vertical-slice plan.)
- **Current session block:** S4

## Status key
`done` - built, done-check passes | `stub` - placeholder with docstring, no real logic | `half-done` - started, not passing (see notes)

## Ledger
| ID | Module | Produced | Status | Notes / open items | Done-check |
|----|--------|----------|--------|--------------------|------------|
| 01.01 | Scaffold | Full directory tree, `.gitignore`, `.gitkeep` placeholders | done | Existing repo metadata was preserved. | Every directory in the merged tree exists; Git is initialized; `.gitignore` lists `_design/`. |
| 01.02 | Scaffold | `pyproject.toml`, `.env.example`, `uv.lock`, package markers, importable source stubs, no-op `app.py` entry point | done | Native Windows cannot install `falkordblite`; WSL is the intended runtime. User confirmed `app.py` boots in WSL. | Stubs import without error; `uv run app.py` starts and stops cleanly in WSL. |
| 01.03 | Scaffold | Root `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` | done | In-repo ledger starts future-session tracking. | `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` exist at repo root; Next up reads `02.01`. |
| 02.01 | Config/Startup | Typed Pydantic v2 config models (`AppConfig`/endpoints/thresholds/runtime/logging) + `load_config()` with env secret overrides | done | Permissive parse; `extra='forbid'` fail-fast deferred to 02.02. `api_key` sourced from `{ENDPOINT}_API_KEY` env (defaults `""`). `base_url` `${...}` placeholders left literal (secrets-only override per scope). Design's `supports_inference_antislop` not in config.yaml, so not modeled. | `load_config('config.yaml')` returns `AppConfig`; nested endpoint/threshold/runtime fields are typed values; clean under `-W error::UserWarning`. |
| 02.02 | Config/Startup | `extra="forbid"` on all six config models — fail-fast on unknown/mistyped keys at parse time | done | Pydantic `ValidationError` reports the offending key's `loc` (e.g. `('bogus_top_level_key',)`, `('endpoints','planner','base_url')`). `RuntimeConfig` keeps `protected_namespaces=()` alongside forbid. | Temp config with a bogus top-level key and a nested `base_url`→`base_uri` typo each raise `ValidationError` locating the key; real `config.yaml` still loads. |
| 02.03 | Config/Startup | Shared node-event logger: `get_logger(node_name)` writes JSON lines to a rotating `logs/fsm.log`; `log_node_event()` emits node, pointer, duration, outcome, and optional error fields | done | Handler attaches once per logger name, `propagate=False`, and level resolves from strict `config.yaml` logging config. Rotation envelope remains the section 3.1 file-size housekeeping default. | Strict config load verified; three events append as three valid JSON lines; a tiny-cap done-check rotates to `fsm.log.1`/`fsm.log.2` while every retained line parses as one JSON object. |
| 02.04 | Config/Startup | Dedicated inference-boundary logger: `get_llm_io_logger()` writes JSON lines to rotating `logs/llm_io.log`; `log_llm_call()` records full request payload, final response text, and duration | done | Separate from the FSM node-event logger and not wired into `call_llm()` yet. Records only the final assembled response, not streaming chunks. | Simulated call wrote exactly one valid JSON line with request, response, and duration; temp check confirmed no `fsm.log` was created. |
| 02.05 | Config/Startup | Resource lifecycle: `init_resources(config)` creates runtime directories and placeholder store artifacts; `reset_resources(config)` wipes documented file-based artifacts and reinitializes them | done | Store initializers and `scan_startup_crash_sentinel()` are labelled no-op stubs pending their owning store/crash modules. Reset is not wired to an HTTP route. | Temp-root done-check: init created data/log directories and store artifacts; reset removed SQLite, Graphiti, Chroma, event-log, style JSON, and snapshot ZIP artifacts; init and reset each ran twice cleanly. |
| 02.T1 | Config/Startup (test) | Synthetic config-loader tests for valid typed load, unknown key, type mismatch, and missing endpoint secrets; loader now rejects missing or empty endpoint API secrets | done | Endpoint `api_key` is required and populated from `{ENDPOINT}_API_KEY`; absent or empty secrets fail config validation instead of silently becoming `""`. | `python -m pytest tests/test_config.py -q --basetemp .pytest_tmp` passed: 4 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_config.py -q` because uv cannot access/remove the WSL-style `.venv\lib64`; rerun in WSL should use the fixed loader. |
| 02.T2 | Config/Startup (test) | Synthetic observability/lifecycle tests for node logging, inference-I/O logging, resource init/reset, and crash-sentinel no-op behavior | done | Module A (Config/Startup/Observability) is complete: strict config, both durable loggers, resource lifecycle/reset, and focused synthetic tests are present. Store init signatures and crash-sentinel scan remain honest no-op stubs for later modules. | `python -m pytest tests/test_observability.py -q --basetemp .pytest_tmp_obs` passed: 3 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 7 passed. Local Codex Windows sandbox could not complete exact `uv run pytest ...` because uv cannot rebuild the locked WSL-style `.venv`; rerun in WSL should exercise the same tests. |
| 03.01 | Coordinator/State Machine | Strict Pydantic v2 `FSM_Pointer` schema with `arc_id`, `chapter_id`, `scene_id`, `beat_index`; strict Pydantic v2 `FailureObject` schema with `error_code`, `offending_text`, `suggested_fix`, `critic_source`; `failure_object_json_schema()` helper | done | `OrchestratorState` remains an honest stub for 03.02. `FailureObject` keeps open string fields because the design gives examples, not exhaustive enums. | `python -c "from fsm.state import FSM_Pointer, FailureObject, failure_object_json_schema; print(FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(FailureObject(error_code='PACING_ISSUE', offending_text='too fast', suggested_fix='slow down', critic_source='pacing')); print(bool(failure_object_json_schema()))"` passed and printed both model instances plus `True`. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv cannot remove the WSL-style `.venv\lib64`; rerun in WSL should exercise the same schema. |
| 03.02 | Coordinator/State Machine | `OrchestratorState` TypedDict with documented fields and annotated list reducers; `accumulate_or_reset()`; `make_initial_state()` with fresh mutable defaults and validated overrides | done | No graph wiring, resource initialization, node execution, routing, or commit-reset behavior is implemented here. | `python -c "from fsm.state import FSM_Pointer, make_initial_state, accumulate_or_reset; s = make_initial_state('proj_1', FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(sorted(s.keys())); print(accumulate_or_reset([1], [2])); print(accumulate_or_reset([1], []))"` passed and reset printed `[]`; additional checks confirmed fresh mutable defaults and clear unknown-override rejection. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv rebuilt `.venv` but failed to spawn `python`; rerun in WSL should exercise the same code. |
| 03.T1 | Coordinator/State Machine (test) | Isolated synthetic tests for `FSM_Pointer`, `FailureObject`, `OrchestratorState`, `accumulate_or_reset()`, and `make_initial_state()` defaults/overrides/reducer contract | done | M01 pass-1 state-types batch is tested. Tests use in-memory synthetic fixtures only; no graph, node, router, LLM, or memory-store integration is exercised. | `python -m pytest tests/test_state.py -q` passed: 8 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 18 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_state.py -q` or `uv run pytest -q` because uv could not remove the active `.venv\lib64`; rerun in WSL should exercise the same tests. |
| 04.01 | Persistent Memory | SQLite relational store in `memory/sqlite_db.py` (connection lifecycle + full schema). Built across 3 prompts: (1) `connect_db(db_path)` — raw `sqlite3`, `str\|Path`, parent-dir creation, `row_factory = sqlite3.Row`, `PRAGMA foreign_keys = ON` per connection, private `_resolve_db_path()`; (2) `init_db(db_path)` creating the outline/planning tables (`Arcs`, `Chapters`, `Scenes`, `Beats`, `Threads`) via `CREATE TABLE IF NOT EXISTS`; (3) remaining tables (`Characters`, `CharacterEmotions`, `CommitIntent`, `RaptorNodes`) + deterministic-read indexes in `_INDEX_STATEMENTS`. | done | Hard CHECKs at the store level: planning-status vocabularies, `CommitIntent.status IN ('pending','committed')`, RaptorNodes.level, and PAD `BETWEEN -1.00 AND 1.00` on all three axes. `CommitIntent`/`RaptorNodes` verbatim from §2.1; `Scenes.ordering` present for `ordering ASC` reads. `Beats`/`Characters`/`CharacterEmotions` under-specified in §2.1 → kept minimal with in-code reasons (`Beats` prose/word_count/committed_at columns added in 04.02; `CharacterEmotions` re-keyed in 04.02). Indexes: chapters→arc FK, scenes(chapter_id,ordering), beats(scene_id,beat_index), threads.status, commitintent.status, raptornodes.parent_id/level (+characteremotions.character_id in 04.02). `init_db` idempotent. **⚠ Carryover:** signature changed from the 02.05 stub's `init_db(config, db_path)` to `init_db(db_path)`; `core/runtime.py:34` still calls `init_db(config, SQLITE_DB_PATH)` and will raise `TypeError` until runtime wiring is updated (import line unaffected). | Final-prompt done-check listed all 9 tables and exited 0 after a double `init_db`. Exact `uv run python -c ...` blocked by the Windows `.venv\lib64` removal error (same as 03.x); ran identical snippets under system `python`. Verified across prompts: foreign keys ON, `sqlite3.Row`, nested parent-dir creation, idempotent re-init preserving data, status + PAD CHECKs firing, FK enforcement on missing parent, all indexes present. Rerun under WSL should exercise the same code. |
| 04.02 | Persistent Memory | Relational write helpers in `memory/sqlite_db.py`. Built across 2 prompts: (1) `upsert_beat_commit(...)` — first write primitive, idempotent committed-beat upsert keyed by `beat_id`; added `Beats` committed-state columns `prose`/`word_count`/`committed_at`; (2) extended it with `pad_states` + `thread_updates` bundled into one atomic commit (private `_upsert_pad_states`/`_apply_thread_updates`); re-keyed `CharacterEmotions` to PK `(beat_id, character_id)` (+`beat_id` FK, +`idx_characteremotions_character`). | done | SQLite half of node_commit_transaction step 2. Explicit keyword signature (no untyped blob); committed/validated input only. Beat `ON CONFLICT(id) DO UPDATE` and PAD `ON CONFLICT(beat_id,character_id)` so replay updates in place — never duplicates; thread status applied idempotently by id under the existing CHECK (thread creation stays the planner's job). All writes in one explicit `BEGIN`/commit with `except: rollback; raise` → beat commit never partially applied. `word_count` defaults to `len(prose.split())` (backs `_commit_router` SUM); `committed_at` defaults to UTC ISO-8601. No numeric thresholds added. **Supersedes the minimal `CharacterEmotions` guess from 04.01** (was PK=`character_id`, current-state) — node 12 specifies PAD "upsert keyed by beat_id". Read helpers, `.jsonl`, Graphiti, crash replay, M10 orchestration deferred. Carryover from 04.01 (`runtime.py:34` signature mismatch) still open. | Done-check: synthetic beat commit (2 characters + 1 thread update) written twice → 1 beat row, exactly 1 PAD row per `(beat_id, character_id)` (2 total), thread status applied; exit 0. Exact `uv run python -c ...` blocked by Windows `.venv\lib64` removal; ran under system `python`. Follow-up confirmed whole-commit atomicity (out-of-bounds PAD fires CHECK, rolls back, leaves no beat/PAD row) and the single-beat replay (`COUNT WHERE id='b1'` == 1, prose updated in place). Rerun under WSL should exercise the same code. |
| 04.03 | Persistent Memory | Deterministic SQLite read helpers in `memory/sqlite_db.py`, built across 2 prompts. Prompt 1: `get_arc`, `get_chapters_for_arc`, `get_scenes_for_chapter_ordered` (`ordering ASC`), `get_beats_for_scene_ordered` (`beat_index ASC`), `get_remaining_{beats_for_scene,scenes_for_chapter,chapters_for_arc}`, `get_open_threads`. Prompt 2: `get_beat` (`strict=` opt-in raise), `get_latest_pad_for_character`, `get_latest_pad_for_scene`, `get_pending_commit_intents` (observational only), `get_raptor_nodes_by_level`, `get_raptor_nodes_by_parent` (`None`→roots via `IS ?`). Private `_read_one`/`_read_all`. | done | All helpers return plain dicts (never raw `sqlite3.Row`), init the DB if needed, close their own connections, and preserve strict exact-match SQL + deterministic ordering. Misses return `None` (single) / `[]` (list); only `get_beat(strict=True)` raises (`KeyError`). "Remaining" = status IN ('planned','active'). `get_latest_pad_for_character` orders by beat `committed_at`,`beat_index` DESC; `get_latest_pad_for_scene` takes each character's max `beat_index` in the scene. `CommitIntent` reads are read-only — no replay/review/recovery decisions (those land in later commit/crash increments). No context assembly/graph traversal/event-log/Chroma/style/runtime wiring. Graphiti, RAPTOR clustering, Chroma, and style baselines remain deferred stubs. Carryover from 04.01 (`runtime.py:34` signature mismatch) still open. | Done-check fixture read ordered scenes (`['s1','s2']`), remaining beats, open threads, committed beat (`get_beat`), latest PAD per character (`0.9` from latest beat) and per scene, Raptor nodes by level + by parent (root via `None`), and pending commit intents through public helpers; misses returned `None`/`[]`; exit 0. Exact `uv run` blocked by Windows `.venv\lib64` removal; ran under system `python`. Rerun under WSL should exercise the same code. |
| 04.04 | Persistent Memory | Append-only `.jsonl` event-log writer + forward/tail readers in `memory/event_log.py`, built across 2 prompts. Prompt 1: `write_event(log_path, payload)` creates the parent dir and appends exactly one JSON object per line in append mode (never truncates/rewrites); payload must be a JSON-serializable `Mapping` — non-mapping raises `TypeError`, non-serializable raises `ValueError`; encoded line + `\n` written in one call so a record is always one complete line. Prompt 2: `iter_events(log_path)` yields parsed JSON objects in file order (missing file yields nothing; whitespace-only/trailing blank lines skipped; non-blank malformed line raises `ValueError` naming the 1-based line number) and `tail_events(log_path, limit)` returns the last `limit` events in original chronological order via a `deque(maxlen=limit)` over `iter_events` (non-positive limit → `[]`). | done | Preserves the §2.4/§3.4 atomicity rule: PAD lives inside the `beat_commit` payload (`pad_states`); no standalone-PAD writer/reader is exposed. No checkpoint filtering added (would be observational-only if it were). Existing `init_event_log` (imported by `core/runtime.py:39`) kept unchanged; `append_event` kept as an honest stub noted as superseded by `write_event`. `_apply_event` is a Graphiti/branch stub (in `graphiti_client.py`), not added here. Crash replay, branch restore, snapshot lookup, event application, commit orchestration deferred. Carryover from 04.01 (`runtime.py:34` `init_db` signature mismatch) still open. | Prompt-1 micro-check: two synthetic `beat_commit` events → exactly 2 JSON-dict lines, PAD nested under `beat_commit`, `TypeError`/`ValueError` on bad payloads with file still at 2 lines. Prompt-2 done-check: three events written, `tail_events(p, 2)` returned the last two `['b2','b3']` in original order; `iter_events` full order `['b1','b2','b3']`; `tail(0)`/`tail(-1)` → `[]`; `tail(99)` → all three; missing file → empty; trailing blank line tolerated (still 3 events); malformed line raised `ValueError` with file + line number; no PAD-named helper present. Ran under system `python`; rerun under WSL should exercise the same code. |
| 04.05 | Persistent Memory | Provisional-claim store in `memory/provisional_store.py` (separate SQLite file, default `data/provisional_claims.db`), built across 2 prompts. Prompt 1: `init_provisional_store(path)` + idempotent `upsert_claim(path, *, claim_text, confidence, claim_id=None, source_ref=None, subject_id=None, entity_id=None, status='provisional', reviewer_note=None)` keyed by `claim_id` or a deterministic SHA-256 content key; `ON CONFLICT DO UPDATE` updates in place and preserves `created_at`; returns resolved id. Prompt 2: read helpers `list_pending_claims(path)`, `list_claims_by_confidence(path, *, min_confidence=None, max_confidence=None, status=None)` (caller-supplied inclusive band, optional status), `get_claim(path, claim_id)`; review transition `mark_claim_reviewed(path, claim_id, *, status, reviewer_note=None)` updating in place (status ∈ `reviewed`/`confirmed`/`rejected`, stamps `reviewed_at`, never deletes). Added `reviewed_at` column + tolerant `_ensure_schema` migration (ALTER TABLE if missing). All reads return plain dicts, ordered `confidence DESC, claim_id ASC`. | done | Physically separate from canonical truth — never writes to `sqlite_db.py` tables; self-contained connection, no import from `sqlite_db.py`. Confidence stored **verbatim**; band boundaries caller-supplied, no high/mid/low interpretation here (config-driven, owned by context assembly per Blueprint §5/§12). Missing store initializes cleanly → `[]`/`None`. No promotion to canonical SQLite/Graphiti (M10, chapter boundaries) and no UI routes. Removed an unused `Mapping` import. `ProvisionalStore.add` kept as an honest stub superseded by `upsert_claim`. Carryover from 04.01 (`runtime.py:34` `init_db` signature mismatch) still open. | Prompt-1 micro-check: same claim upserted twice → deterministic key matched, `COUNT(*) == 1`; conflict update changed confidence with `created_at` preserved; verbatim confidences stored. Prompt-2 done-check: temp nested store upserted a claim, listed pending mid-band (`min=0.5,max=0.8,status='provisional'`), marked it `confirmed` (row preserved, `reviewed_at` set, dropped from pending), re-review preserved the note, invalid status → `ValueError`, missing id → `KeyError`, reads returned plain dicts, missing store → `[]`/`None`, path separate from `fictionwriter.db`. Ran under system `python`; rerun under WSL should exercise the same code. |
| 04.T1 | Persistent Memory (test) | `tests/test_sqlite_db.py` — synthetic, file-local tests for the relational hub (`memory/sqlite_db.py`, exercising 04.01–04.03), built across 2 prompts; 14 tests total. Prompt 1 (schema/constraint/ordering): `init_db` creates all 9 documented tables under a nested `tmp_path` and is safe to call twice (no drop/wipe, data preserved); status `CHECK` constraints reject invalid enums on `Arcs`/`Threads`/`CommitIntent` (and accept the documented vocabulary); `CharacterEmotions` PAD `CHECK` rejects out-of-range values on each axis (incl. the hallucinated `999.0`) and accepts the inclusive `±1.00` edges; scenes read back by `ordering ASC` not insertion order. Prompt 2 (beat-commit round-trip): a synthetic `BEAT_COMMIT` (metadata + prose + one thread transition + PAD for two characters) written twice → exactly one `Beats` row for the `beat_id`; PAD idempotent per `(beat_id, character_id)` (2 rows, one each) and the thread UPDATE mutates the canonical row without duplicating it (still 2 threads, `t1`→`progressing`); public read helpers retrieve the committed beat, open threads (progressed `t1` excluded, `t2` remains), latest PAD per character + per scene, pending intents → documented `[]`, with miss behavior (`get_beat`→None / `strict=True`→KeyError, latest-PAD misses→None/`[]`). | done | Pure synthetic fixtures under `tmp_path`; never writes to real `data/`. FK enforcement is ON, so commit tests seed Arc→Chapter→Scene + two Characters + two Threads before `upsert_beat_commit`. **Resolved the long-open 04.01 carryover**: full suite surfaced `core/runtime.py:34` calling the old `init_db(config, …)` — fixed to `init_db(SQLITE_DB_PATH)` (permitted small relational-store contract fix). That made the real `init_db` build a genuine SQLite DB, so the stub-era assertion in `tests/test_observability.py` (post-reset SQLite artifact `read_text()==""`) was updated to assert a freshly initialized DB (`read_bytes().startswith(b"SQLite format 3\x00")`). No new runtime integration (INT-A·B) added. Out of scope/untouched: event log, provisional claims, Graphiti, RAPTOR, Chroma, style store, graph nodes. | `uv run pytest tests/test_sqlite_db.py -q` **WSL2: 14 passed in 0.24s**; full suite `uv run pytest -q` **WSL2: 32 passed in 1.93s** (green, carryover resolved). |
| 04.T2 | Persistent Memory (test) | Synthetic, file-local tests for the two non-relational M02 stores (exercising 04.04–04.05), built across 2 prompts. Prompt 1 `tests/test_event_log.py` (7 tests): three events append exactly three JSON-dict lines (equal to inputs); `iter_events` returns all in write order; `tail_events(log, 2)` returns the last two in original chronological order (`seq [2,3]`, not reversed); non-positive limit → `[]`; missing log iterates/tails empty; a `beat_commit` with `pad_states` nested in the payload round-trips as one ledger record with nested PAD intact; no public symbol writes standalone PAD events (only `write_event`). Prompt 2 `tests/test_provisional_store.py` (5 tests): upserting the same claim twice (deterministic content key) leaves one stored claim; `list_pending_claims` returns only unreviewed (`provisional`) rows (a confirmed claim drops out); confidence-band queries use caller-supplied inclusive thresholds + optional status filter (no high/mid/low hardcoding); `mark_claim_reviewed` for confirmed/rejected/reviewed updates in place, preserves the record, stores the reviewer note, and stamps `reviewed_at`; the provisional path is `provisional_claims.db`, distinct from `runtime.SQLITE_DB_PATH` (`fictionwriter.db`), with no canonical DB written in the fixture tree. | done | Pure synthetic fixtures under nested `tmp_path`; never writes to real `data/` or canonical `fictionwriter.db`. Out of scope/untouched: crash replay, branch restore, `_apply_event`, snapshot logic, SQLite relational writes, Alignment UI, ingestion, Graphiti promotion, M10 belief resolution. No production code changed. | Prompt-1: `uv run pytest tests/test_event_log.py -q` **WSL2: 7 passed**. Prompt-2 done-check: `uv run pytest tests/test_event_log.py tests/test_provisional_store.py -q` **WSL2: 12 passed in 0.35s**; full suite `uv run pytest -q` **WSL2: 44 passed in 12.63s** (green). |
| INT-A·B | Config/Startup × Persistent Memory | Integration of the resource lifecycle (`core/runtime.py`, M14) with the real relational store (M02), across 2 prompts. Prompt 1: `init_resources` initializes a genuine SQLite DB via `memory.sqlite_db.init_db(SQLITE_DB_PATH)` at the documented `data/fictionwriter.db` path; `reset_resources` deletes and recreates it. Module docstring + call-site comment updated to honestly state the SQLite store is real while Graphiti/RAPTOR/Chroma/style/snapshot initializers remain no-op stubs (event-log init only touches its `.jsonl`). Prompt 2: `tests/test_memory_integration.py` (4 tests, Config/Startup + SQLite only) — `init_resources(config)` builds a real DB (`SQLite format 3\0` header) with all 9 tables under a monkeypatched temp root; a synthetic beat commit (prose + 2-character PAD + one thread transition) written via `upsert_beat_commit` reads back through `get_beat`/`get_open_threads`/`get_latest_pad_for_character`; a replay stays idempotent (1 `Beats` row, 2 PAD rows); `reset_resources` removes the artifact and recreates a clean schema (prior beat gone), with a second `init_resources` a clean no-op. | done | **M02 relational + event-log + provisional slice is now built AND tested** (isolated tests 04.T1/04.T2 + this integration). Prompt-1 production wiring (`init_db(config, …)` → `init_db(SQLITE_DB_PATH)`) was already applied in 04.T1 as the permitted carryover fix; this increment formalized it and now tests it. No config-schema change (design documents a literal DB path, no key); no `sqlite_db.py` shim needed. Synthetic `config = object()`; never writes to real `data/`/`logs/`. Not wired/tested: end-to-end LangGraph, node execution, LLM calls, Graphiti, Chroma, real style metrics, branch replay. | Prompt-1 micro-check (WSL2) green. Prompt-2 done-check: `uv run pytest tests/test_memory_integration.py -q` **WSL2: 4 passed in 0.31s**; full suite `uv run pytest -q` **WSL2: 48 passed in 13.69s** (green). |

*(Append a row per increment. Keep "Current position" accurate - it is read first each session.)*

## Stub inventory (deliberate no-ops, wired later)
- Store init signatures called by `init_resources()` - the SQLite relational init is now **real** (`init_db`, wired in INT-A·B); the rest (Graphiti, RAPTOR, Chroma, style stores, snapshot archive) remain no-op placeholders until their modules land. The event-log init only touches its `.jsonl` artifact.
- `scan_startup_crash_sentinel()` - no-op until CommitIntent records and crash recovery land (13).
- `core.antislop.detect_slop()` and `core.antislop.resolve_slop()` - no-op passthroughs until anti-slop implementation.
- `memory.graphiti_client._apply_event()` - intentional early no-op for graph writes during crash-recovery scaffolding.

### Remaining M02 deferred store stubs (not yet implemented)
- `memory.graphiti_client._apply_event()` - temporal knowledge-graph edge writes / crash-replay application (§2.2).
- `memory.raptor.cluster_scenes()` - documented placeholder returning scene texts unmodified; real embed→similarity→Leiden→summarize pipeline pending (§2.3).
- `memory.chroma_client` - associative flavour HNSW vector index (§2.6).
- `memory.style_store` - frozen baseline + rolling-EMA stylometric profiles, author + per-character (§2.5).
