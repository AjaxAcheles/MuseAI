# Progress Ledger

The living record of what is real, what is a stub, and what a session left half-done. The agent updates this in-repo copy at the end of every session, and it is read at the start of every session.

## Current position
- **Last completed:** `10.01 (M07 Quality Gauntlet — fsm/nodes/node_programmatic_audit.py, model-free Stage-1 audit: structural checks (empty draft, unclosed quote, unclosed markdown span) + density-based passive-voice check against config.thresholds.passive_voice_density + best_seen_draft fewer-failures retention + stub-honest stylometric_distance provider seam; done).`
- **Out-of-band since:** `VIS-03 (M05 Streamlit visualizer — tests/frontends/m07_planning.py + shared.seed_planning_data, registered in run_all.py and documented in STREAMLIT_FRONTENDS.md; done; an interactive debugger exercising the real planner cascade, not an automated test; the prompt library labels it 07.13 — kept as VIS-03 here because 07.13 is the beat-planner rows).`
- **Next up:** `10.02`.
- **Plan of record:** `C:\Users\imarg\.claude\plans\clever-noodling-deer.md` (4 phases A-D; A done). Forward dependency (already closed as of `07.13`): all five `node_plan_*` templates list `continue_deliberation`.
- **Current session block:** S21 (10.01 done, 1/3. Build group 10 — Quality Gauntlet. `10.02`'s Operator notes say continue this same chat; `10.03` follows, then **RESTART before `10.04`**.)

> **ID note:** the `07.00`/`07.01`/`07.02` rows below are *persistence-prerequisite* sub-tasks tracked under the `07.00` umbrella (M02 schema+helpers, M14 config, M01 state). The **M05 planning-library build sequence** now reuses `07.0x` IDs starting from `07.01`; M05 rows carry Module `Hierarchical Planning Cascade (M05)` to disambiguate from the prerequisite rows.

## Status key
`done` - built, done-check passes | `stub` - placeholder with docstring, no real logic | `half-done` - started, not passing (see notes)

## Ledger
| ID | Module | Produced | Status | Notes / open items | Done-check |
|----|--------|----------|--------|--------------------|------------|
| 01.01 | Scaffold | Full directory tree, `.gitignore`, `.gitkeep` placeholders | done | Existing repo metadata was preserved. | Every directory in the merged tree exists; Git is initialized; `.gitignore` lists `_design/`. |
| 01.02 | Scaffold | `pyproject.toml`, `.env.example`, `uv.lock`, package markers, importable source stubs, no-op `app.py` entry point | done | Native Windows cannot install `falkordblite`; WSL is the intended runtime. User confirmed `app.py` boots in WSL. | Stubs import without error; `uv run app.py` starts and stops cleanly in WSL. |
| 01.03 | Scaffold | Root `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` | done | In-repo ledger starts future-session tracking. | `AGENTS.md`, `CLAUDE.md`, and `PROGRESS_LEDGER.md` exist at repo root; Next up reads `02.01`. |
| 02.01 | Config/Startup | Typed Pydantic v2 config models (`AppConfig`/endpoints/thresholds/runtime/logging) + `load_config()` with env secret overrides | done | Permissive parse; `extra='forbid'` fail-fast deferred to 02.02. `api_key` sourced from `{ENDPOINT}_API_KEY` env (defaults `""`). `base_url` `${...}` placeholders left literal (secrets-only override per scope). Design's `supports_inference_antislop` not in config.yaml, so not modeled. | `load_config('config.yaml')` returns `AppConfig`; nested endpoint/threshold/runtime fields are typed values; clean under `-W error::UserWarning`. |
| 02.02 | Config/Startup | `extra="forbid"` on all six config models — fail-fast on unknown/mistyped keys at parse time | done | Pydantic `ValidationError` reports the offending key's `loc` (e.g. `('bogus_top_level_key',)`, `('endpoints','planner','base_url')`). `RuntimeConfig` keeps `protected_namespaces=()` alongside forbid. | Temp config with a bogus top-level key and a nested `base_url`→`base_uri` typo each raise `ValidationError` locating the key; real `config.yaml` still loads. |
| 02.03 | Config/Startup | Shared node-event logger: `get_logger(node_name)` writes JSON lines to a rotating `logs/fsm.log`; `log_node_event()` emits node, pointer, duration, outcome, and optional error fields | done | Handler attaches once per logger name, `propagate=False`, and level resolves from strict `config.yaml` logging config. Rotation envelope remains the section 3.1 file-size housekeeping default. | Strict config load verified; three events append as three valid JSON lines; a tiny-cap done-check rotates to `fsm.log.1`/`fsm.log.2` while every retained line parses as one JSON object. |
| 02.04 | Config/Startup | Dedicated inference-boundary logger: `get_llm_io_logger()` writes JSON lines to rotating `logs/llm_io.log`; `log_llm_call()` records full request payload, final response text, and duration | done | Separate from the FSM node-event logger and not wired into `call_llm()` yet. Records only the final assembled response, not streaming chunks. | Simulated call wrote exactly one valid JSON line with request, response, and duration; temp check confirmed no `fsm.log` was created. |
| 02.05 | Config/Startup | Resource lifecycle: `init_resources(config)` creates runtime directories and placeholder store artifacts; `reset_resources(config)` wipes documented file-based artifacts and reinitializes them | done | Store initializers and `scan_startup_crash_sentinel()` are labelled no-op stubs pending their owning store/crash modules. Reset is not wired to an HTTP route. | Temp-root done-check: init created data/log directories and store artifacts; reset removed SQLite, Graphiti, Chroma, event-log, style JSON, and snapshot ZIP artifacts; init and reset each ran twice cleanly. |
| 02.T1 | Config/Startup (test) | Synthetic config-loader tests for valid typed load, unknown key, type mismatch, and missing endpoint secrets; loader now rejects missing or empty endpoint API secrets | done | Endpoint `api_key` is required and populated from `{ENDPOINT}_API_KEY`; absent or empty secrets fail config validation instead of silently becoming `""`. | `python -m pytest tests/test_config.py -q --basetemp .pytest_tmp` passed: 4 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_config.py -q` because uv cannot access/remove the WSL-style `.venv\lib64`; rerun in WSL should use the fixed loader. |
| 02.T2 | Config/Startup (test) | Synthetic observability/lifecycle tests for node logging, inference-I/O logging, resource init/reset, and crash-sentinel no-op behavior | done | Module A (Config/Startup/Observability) is complete: strict config, both durable loggers, resource lifecycle/reset, and focused synthetic tests are present. Store init signatures and crash-sentinel scan remain honest no-op stubs for later modules. | `python -m pytest tests/test_observability.py -q --basetemp .pytest_tmp_obs` passed: 3 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 7 passed. Local Codex Windows sandbox could not complete exact `uv run pytest ...` because uv cannot rebuild the locked WSL-style `.venv`; rerun in WSL should exercise the same tests. |
| 03.01 | Coordinator/State Machine | Strict Pydantic v2 `FSM_Pointer` schema with `arc_id`, `chapter_id`, `scene_id`, `beat_index`; strict Pydantic v2 `FailureObject` schema with `error_code`, `offending_text`, `suggested_fix`, `critic_source`; `failure_object_json_schema()` helper | done | `OrchestratorState` remains an honest stub for 03.02. `FailureObject` keeps open string fields because the design gives examples, not exhaustive enums. | `python -c "from fsm.state import FSM_Pointer, FailureObject, failure_object_json_schema; print(FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(FailureObject(error_code='PACING_ISSUE', offending_text='too fast', suggested_fix='slow down', critic_source='pacing')); print(bool(failure_object_json_schema()))"` passed and printed both model instances plus `True`. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv cannot remove the WSL-style `.venv\lib64`; rerun in WSL should exercise the same schema. |
| 03.02 | Coordinator/State Machine | `OrchestratorState` TypedDict with documented fields and annotated list reducers; `accumulate_or_reset()`; `make_initial_state()` with fresh mutable defaults and validated overrides | done | No graph wiring, resource initialization, node execution, routing, or commit-reset behavior is implemented here. | `python -c "from fsm.state import FSM_Pointer, make_initial_state, accumulate_or_reset; s = make_initial_state('proj_1', FSM_Pointer(arc_id='a1', chapter_id='c1', scene_id='s1', beat_index=0)); print(sorted(s.keys())); print(accumulate_or_reset([1], [2])); print(accumulate_or_reset([1], []))"` passed and reset printed `[]`; additional checks confirmed fresh mutable defaults and clear unknown-override rejection. Local Codex Windows sandbox could not complete the exact `uv run python -c ...` done-check because uv rebuilt `.venv` but failed to spawn `python`; rerun in WSL should exercise the same code. |
| 03.T1 | Coordinator/State Machine (test) | Isolated synthetic tests for `FSM_Pointer`, `FailureObject`, `OrchestratorState`, `accumulate_or_reset()`, and `make_initial_state()` defaults/overrides/reducer contract | done | M01 pass-1 state-types batch is tested. Tests use in-memory synthetic fixtures only; no graph, node, router, LLM, or memory-store integration is exercised. | `python -m pytest tests/test_state.py -q` passed: 8 passed. `python -m pytest -q --basetemp .pytest_tmp_all` passed: 18 passed. Local Codex Windows sandbox could not complete exact `uv run pytest tests/test_state.py -q` or `uv run pytest -q` because uv could not remove the active `.venv\lib64`; rerun in WSL should exercise the same tests. |
| 04.01 | Persistent Memory | SQLite relational store in `memory/sqlite_db.py` (connection lifecycle + full schema). Built across 3 prompts: (1) `connect_db(db_path)` — raw `sqlite3`, `str\|Path`, parent-dir creation, `row_factory = sqlite3.Row`, `PRAGMA foreign_keys = ON` per connection, private `_resolve_db_path()`; (2) `init_db(db_path)` creating the outline/planning tables (`Arcs`, `Chapters`, `Scenes`, `Beats`, `Threads`) via `CREATE TABLE IF NOT EXISTS`; (3) remaining tables (`Characters`, `CharacterEmotions`, `CommitIntent`, `RaptorNodes`) + deterministic-read indexes in `_INDEX_STATEMENTS`. | done | Hard CHECKs at the store level: planning-status vocabularies, `CommitIntent.status IN ('pending','committed')`, RaptorNodes.level, and PAD `BETWEEN -1.00 AND 1.00` on all three axes. `CommitIntent`/`RaptorNodes` verbatim from §2.1; `Scenes.ordering` present for `ordering ASC` reads. `Beats`/`Characters`/`CharacterEmotions` under-specified in §2.1 → kept minimal with in-code reasons (`Beats` prose/word_count/committed_at columns added in 04.02; `CharacterEmotions` re-keyed in 04.02). Indexes: chapters→arc FK, scenes(chapter_id,ordering), beats(scene_id,beat_index), threads.status, commitintent.status, raptornodes.parent_id/level (+characteremotions.character_id in 04.02). `init_db` idempotent. **⚠ Carryover:** signature changed from the 02.05 stub's `init_db(config, db_path)` to `init_db(db_path)`; `core/runtime.py:34` still calls `init_db(config, SQLITE_DB_PATH)` and will raise `TypeError` until runtime wiring is updated (import line unaffected). | Final-prompt done-check listed all 9 tables and exited 0 after a double `init_db`. Exact `uv run python -c ...` blocked by the Windows `.venv\lib64` removal error (same as 03.x); ran identical snippets under system `python`. Verified across prompts: foreign keys ON, `sqlite3.Row`, nested parent-dir creation, idempotent re-init preserving data, status + PAD CHECKs firing, FK enforcement on missing parent, all indexes present. Rerun under WSL should exercise the same code. |
| 04.02 | Persistent Memory | Relational write helpers in `memory/sqlite_db.py`. Built across 2 prompts: (1) `upsert_beat_commit(...)` — first write primitive, idempotent committed-beat upsert keyed by `beat_id`; added `Beats` committed-state columns `prose`/`word_count`/`committed_at`; (2) extended it with `pad_states` + `thread_updates` bundled into one atomic commit (private `_upsert_pad_states`/`_apply_thread_updates`); re-keyed `CharacterEmotions` to PK `(beat_id, character_id)` (+`beat_id` FK, +`idx_characteremotions_character`). | done | SQLite half of node_commit_transaction step 2. Explicit keyword signature (no untyped blob); committed/validated input only. Beat `ON CONFLICT(id) DO UPDATE` and PAD `ON CONFLICT(beat_id,character_id)` so replay updates in place — never duplicates; thread status applied idempotently by id under the existing CHECK (thread creation stays the planner's job). All writes in one explicit `BEGIN`/commit with `except: rollback; raise` → beat commit never partially applied. `word_count` defaults to `len(prose.split())` (backs `_commit_router` SUM); `committed_at` defaults to UTC ISO-8601. No numeric thresholds added. **Supersedes the minimal `CharacterEmotions` guess from 04.01** (was PK=`character_id`, current-state) — node 12 specifies PAD "upsert keyed by beat_id". Read helpers, `.jsonl`, Graphiti, crash replay, M10 orchestration deferred. Carryover from 04.01 (`runtime.py:34` signature mismatch) still open. | Done-check: synthetic beat commit (2 characters + 1 thread update) written twice → 1 beat row, exactly 1 PAD row per `(beat_id, character_id)` (2 total), thread status applied; exit 0. Exact `uv run python -c ...` blocked by Windows `.venv\lib64` removal; ran under system `python`. Follow-up confirmed whole-commit atomicity (out-of-bounds PAD fires CHECK, rolls back, leaves no beat/PAD row) and the single-beat replay (`COUNT WHERE id='b1'` == 1, prose updated in place). Rerun under WSL should exercise the same code. |
| 04.03 | Persistent Memory | Deterministic SQLite read helpers in `memory/sqlite_db.py`, built across 2 prompts. Prompt 1: `get_arc`, `get_chapters_for_arc`, `get_scenes_for_chapter_ordered` (`ordering ASC`), `get_beats_for_scene_ordered` (`beat_index ASC`), `get_remaining_{beats_for_scene,scenes_for_chapter,chapters_for_arc}`, `get_open_threads`. Prompt 2: `get_beat` (`strict=` opt-in raise), `get_latest_pad_for_character`, `get_latest_pad_for_scene`, `get_pending_commit_intents` (observational only), `get_raptor_nodes_by_level`, `get_raptor_nodes_by_parent` (`None`→roots via `IS ?`). Private `_read_one`/`_read_all`. | done | All helpers return plain dicts (never raw `sqlite3.Row`), init the DB if needed, close their own connections, and preserve strict exact-match SQL + deterministic ordering. Misses return `None` (single) / `[]` (list); only `get_beat(strict=True)` raises (`KeyError`). "Remaining" = status IN ('planned','active'). `get_latest_pad_for_character` orders by beat `committed_at`,`beat_index` DESC; `get_latest_pad_for_scene` takes each character's max `beat_index` in the scene. `CommitIntent` reads are read-only — no replay/review/recovery decisions (those land in later commit/crash increments). No context assembly/graph traversal/event-log/Chroma/style/runtime wiring. Graphiti, RAPTOR clustering, Chroma, and style baselines remain deferred stubs. Carryover from 04.01 (`runtime.py:34` signature mismatch) still open. | Done-check fixture read ordered scenes (`['s1','s2']`), remaining beats, open threads, committed beat (`get_beat`), latest PAD per character (`0.9` from latest beat) and per scene, Raptor nodes by level + by parent (root via `None`), and pending commit intents through public helpers; misses returned `None`/`[]`; exit 0. Exact `uv run` blocked by Windows `.venv\lib64` removal; ran under system `python`. Rerun under WSL should exercise the same code. |
| 04.04 | Persistent Memory | Append-only `.jsonl` event-log writer + forward/tail readers in `memory/event_log.py`, built across 2 prompts. Prompt 1: `write_event(log_path, payload)` creates the parent dir and appends exactly one JSON object per line in append mode (never truncates/rewrites); payload must be a JSON-serializable `Mapping` — non-mapping raises `TypeError`, non-serializable raises `ValueError`; encoded line + `\n` written in one call so a record is always one complete line. Prompt 2: `iter_events(log_path)` yields parsed JSON objects in file order (missing file yields nothing; whitespace-only/trailing blank lines skipped; non-blank malformed line raises `ValueError` naming the 1-based line number) and `tail_events(log_path, limit)` returns the last `limit` events in original chronological order via a `deque(maxlen=limit)` over `iter_events` (non-positive limit → `[]`). | done | Preserves the §2.4/§3.4 atomicity rule: PAD lives inside the `beat_commit` payload (`pad_states`); no standalone-PAD writer/reader is exposed. No checkpoint filtering added (would be observational-only if it were). Existing `init_event_log` (imported by `core/runtime.py:39`) kept unchanged; `append_event` kept as an honest stub noted as superseded by `write_event`. `_apply_event` is a Graphiti/branch stub (in `graphiti_client.py`), not added here. Crash replay, branch restore, snapshot lookup, event application, commit orchestration deferred. Carryover from 04.01 (`runtime.py:34` `init_db` signature mismatch) still open. | Prompt-1 micro-check: two synthetic `beat_commit` events → exactly 2 JSON-dict lines, PAD nested under `beat_commit`, `TypeError`/`ValueError` on bad payloads with file still at 2 lines. Prompt-2 done-check: three events written, `tail_events(p, 2)` returned the last two `['b2','b3']` in original order; `iter_events` full order `['b1','b2','b3']`; `tail(0)`/`tail(-1)` → `[]`; `tail(99)` → all three; missing file → empty; trailing blank line tolerated (still 3 events); malformed line raised `ValueError` with file + line number; no PAD-named helper present. Ran under system `python`; rerun under WSL should exercise the same code. |
| 04.05 | Persistent Memory | Provisional-claim store in `memory/provisional_store.py` (separate SQLite file, default `data/provisional_claims.db`), built across 2 prompts. Prompt 1: `init_provisional_store(path)` + idempotent `upsert_claim(path, *, claim_text, confidence, claim_id=None, source_ref=None, subject_id=None, entity_id=None, status='provisional', reviewer_note=None)` keyed by `claim_id` or a deterministic SHA-256 content key; `ON CONFLICT DO UPDATE` updates in place and preserves `created_at`; returns resolved id. Prompt 2: read helpers `list_pending_claims(path)`, `list_claims_by_confidence(path, *, min_confidence=None, max_confidence=None, status=None)` (caller-supplied inclusive band, optional status), `get_claim(path, claim_id)`; review transition `mark_claim_reviewed(path, claim_id, *, status, reviewer_note=None)` updating in place (status ∈ `reviewed`/`confirmed`/`rejected`, stamps `reviewed_at`, never deletes). Added `reviewed_at` column + tolerant `_ensure_schema` migration (ALTER TABLE if missing). All reads return plain dicts, ordered `confidence DESC, claim_id ASC`. | done | Physically separate from canonical truth — never writes to `sqlite_db.py` tables; self-contained connection, no import from `sqlite_db.py`. Confidence stored **verbatim**; band boundaries caller-supplied, no high/mid/low interpretation here (config-driven, owned by context assembly per Blueprint §5/§12). Missing store initializes cleanly → `[]`/`None`. No promotion to canonical SQLite/Graphiti (M10, chapter boundaries) and no UI routes. Removed an unused `Mapping` import. `ProvisionalStore.add` kept as an honest stub superseded by `upsert_claim`. Carryover from 04.01 (`runtime.py:34` `init_db` signature mismatch) still open. | Prompt-1 micro-check: same claim upserted twice → deterministic key matched, `COUNT(*) == 1`; conflict update changed confidence with `created_at` preserved; verbatim confidences stored. Prompt-2 done-check: temp nested store upserted a claim, listed pending mid-band (`min=0.5,max=0.8,status='provisional'`), marked it `confirmed` (row preserved, `reviewed_at` set, dropped from pending), re-review preserved the note, invalid status → `ValueError`, missing id → `KeyError`, reads returned plain dicts, missing store → `[]`/`None`, path separate from `fictionwriter.db`. Ran under system `python`; rerun under WSL should exercise the same code. |
| 04.T1 | Persistent Memory (test) | `tests/test_sqlite_db.py` — synthetic, file-local tests for the relational hub (`memory/sqlite_db.py`, exercising 04.01–04.03), built across 2 prompts; 14 tests total. Prompt 1 (schema/constraint/ordering): `init_db` creates all 9 documented tables under a nested `tmp_path` and is safe to call twice (no drop/wipe, data preserved); status `CHECK` constraints reject invalid enums on `Arcs`/`Threads`/`CommitIntent` (and accept the documented vocabulary); `CharacterEmotions` PAD `CHECK` rejects out-of-range values on each axis (incl. the hallucinated `999.0`) and accepts the inclusive `±1.00` edges; scenes read back by `ordering ASC` not insertion order. Prompt 2 (beat-commit round-trip): a synthetic `BEAT_COMMIT` (metadata + prose + one thread transition + PAD for two characters) written twice → exactly one `Beats` row for the `beat_id`; PAD idempotent per `(beat_id, character_id)` (2 rows, one each) and the thread UPDATE mutates the canonical row without duplicating it (still 2 threads, `t1`→`progressing`); public read helpers retrieve the committed beat, open threads (progressed `t1` excluded, `t2` remains), latest PAD per character + per scene, pending intents → documented `[]`, with miss behavior (`get_beat`→None / `strict=True`→KeyError, latest-PAD misses→None/`[]`). | done | Pure synthetic fixtures under `tmp_path`; never writes to real `data/`. FK enforcement is ON, so commit tests seed Arc→Chapter→Scene + two Characters + two Threads before `upsert_beat_commit`. **Resolved the long-open 04.01 carryover**: full suite surfaced `core/runtime.py:34` calling the old `init_db(config, …)` — fixed to `init_db(SQLITE_DB_PATH)` (permitted small relational-store contract fix). That made the real `init_db` build a genuine SQLite DB, so the stub-era assertion in `tests/test_observability.py` (post-reset SQLite artifact `read_text()==""`) was updated to assert a freshly initialized DB (`read_bytes().startswith(b"SQLite format 3\x00")`). No new runtime integration (INT-A·B) added. Out of scope/untouched: event log, provisional claims, Graphiti, RAPTOR, Chroma, style store, graph nodes. | `uv run pytest tests/test_sqlite_db.py -q` **WSL2: 14 passed in 0.24s**; full suite `uv run pytest -q` **WSL2: 32 passed in 1.93s** (green, carryover resolved). |
| 04.T2 | Persistent Memory (test) | Synthetic, file-local tests for the two non-relational M02 stores (exercising 04.04–04.05), built across 2 prompts. Prompt 1 `tests/test_event_log.py` (7 tests): three events append exactly three JSON-dict lines (equal to inputs); `iter_events` returns all in write order; `tail_events(log, 2)` returns the last two in original chronological order (`seq [2,3]`, not reversed); non-positive limit → `[]`; missing log iterates/tails empty; a `beat_commit` with `pad_states` nested in the payload round-trips as one ledger record with nested PAD intact; no public symbol writes standalone PAD events (only `write_event`). Prompt 2 `tests/test_provisional_store.py` (5 tests): upserting the same claim twice (deterministic content key) leaves one stored claim; `list_pending_claims` returns only unreviewed (`provisional`) rows (a confirmed claim drops out); confidence-band queries use caller-supplied inclusive thresholds + optional status filter (no high/mid/low hardcoding); `mark_claim_reviewed` for confirmed/rejected/reviewed updates in place, preserves the record, stores the reviewer note, and stamps `reviewed_at`; the provisional path is `provisional_claims.db`, distinct from `runtime.SQLITE_DB_PATH` (`fictionwriter.db`), with no canonical DB written in the fixture tree. | done | Pure synthetic fixtures under nested `tmp_path`; never writes to real `data/` or canonical `fictionwriter.db`. Out of scope/untouched: crash replay, branch restore, `_apply_event`, snapshot logic, SQLite relational writes, Alignment UI, ingestion, Graphiti promotion, M10 belief resolution. No production code changed. | Prompt-1: `uv run pytest tests/test_event_log.py -q` **WSL2: 7 passed**. Prompt-2 done-check: `uv run pytest tests/test_event_log.py tests/test_provisional_store.py -q` **WSL2: 12 passed in 0.35s**; full suite `uv run pytest -q` **WSL2: 44 passed in 12.63s** (green). |
| INT-A·B | Config/Startup × Persistent Memory | Integration of the resource lifecycle (`core/runtime.py`, M14) with the real relational store (M02), across 2 prompts. Prompt 1: `init_resources` initializes a genuine SQLite DB via `memory.sqlite_db.init_db(SQLITE_DB_PATH)` at the documented `data/fictionwriter.db` path; `reset_resources` deletes and recreates it. Module docstring + call-site comment updated to honestly state the SQLite store is real while Graphiti/RAPTOR/Chroma/style/snapshot initializers remain no-op stubs (event-log init only touches its `.jsonl`). Prompt 2: `tests/test_memory_integration.py` (4 tests, Config/Startup + SQLite only) — `init_resources(config)` builds a real DB (`SQLite format 3\0` header) with all 9 tables under a monkeypatched temp root; a synthetic beat commit (prose + 2-character PAD + one thread transition) written via `upsert_beat_commit` reads back through `get_beat`/`get_open_threads`/`get_latest_pad_for_character`; a replay stays idempotent (1 `Beats` row, 2 PAD rows); `reset_resources` removes the artifact and recreates a clean schema (prior beat gone), with a second `init_resources` a clean no-op. | done | **M02 relational + event-log + provisional slice is now built AND tested** (isolated tests 04.T1/04.T2 + this integration). Prompt-1 production wiring (`init_db(config, …)` → `init_db(SQLITE_DB_PATH)`) was already applied in 04.T1 as the permitted carryover fix; this increment formalized it and now tests it. No config-schema change (design documents a literal DB path, no key); no `sqlite_db.py` shim needed. Synthetic `config = object()`; never writes to real `data/`/`logs/`. Not wired/tested: end-to-end LangGraph, node execution, LLM calls, Graphiti, Chroma, real style metrics, branch replay. | Prompt-1 micro-check (WSL2) green. Prompt-2 done-check: `uv run pytest tests/test_memory_integration.py -q` **WSL2: 4 passed in 0.31s**; full suite `uv run pytest -q` **WSL2: 48 passed in 13.69s** (green). |
| 05.01 | LLM Inference Boundary | `llm/tokenizer.py` real token-counting API (`count_tokens`, `count_message_tokens`, `count_payload_tokens`) plus `tests/test_tokenizer.py`. Routes only by `tokenizer_family`: `tiktoken` with model encoding and `cl100k_base` tokenizer fallback for unknown models, lazy `hf_auto`, and documented `char_heuristic`. If the `tiktoken` wheel is unavailable or partially installed, the `tiktoken` route degrades to the same deterministic char heuristic instead of crashing import-time sizing. | done | No provider/model-name special casing. Message counting uses deterministic structural overhead and is explicitly heuristic, not exact backend accounting. Hugging Face tokenizer loading is optional and raises a clear `hf-tokenizer` extra error when unavailable. | `uv run pytest tests/test_tokenizer.py` **WSL2: 6 passed in 0.74s**. After the fallback hardening, `python -m pytest tests/test_tokenizer.py -q --basetemp .pytest_tmp_tokenizer` passed: 6 passed. User manually ran `uv run python -c "from llm.tokenizer import count_tokens; print(count_tokens('hello world', 'char_heuristic'))"` and reported output `3`; user also reported `uv run pytest -q` **61 passed**. |
| 05.02 | LLM Inference Boundary | `llm/gbnf_compiler.py` JSON Schema → GBNF compiler plus `tests/test_gbnf_compiler.py`. Supports strict object schemas, required fields, `additionalProperties: false`, primitive JSON types, homogeneous arrays, enums, simple unions/nullable fields, and internal `$defs`/`definitions` refs; keeps `_json_schema_to_gbnf` compatibility alias. | done | Provider-neutral compiler only; no endpoint payload fields, parser, or validation loop. Unsupported schema features fail closed with `ValueError` instead of loose JSON: external `$ref`, `allOf`, `not`, tuple arrays/`prefixItems`, `patternProperties`, unrestricted `additionalProperties`, and unsupported constraint keywords such as `pattern`. | `uv run pytest tests/test_gbnf_compiler.py` **WSL2: 7 passed in 0.31s**. User reported full suite `uv run pytest -q` **61 passed** after tokenizer/compiler work. |
| 05.03 | LLM Inference Boundary | First-pass `llm/call_llm.py` async inference boundary plus `tests/test_call_llm.py`. Public API accepts endpoint/config objects, messages, stream/on-token callbacks, generation params, `extra_body`/`extra_headers`, optional adapter/client overrides, injectable retry delays, and returns `LLMResponse`. Default adapter is chat-completions-compatible behind an adapter seam; URL resolution treats root/version roots generically and leaves explicit inference paths unchanged. Second pass added bounded transient retry/backoff, tokenizer-based token accounting, `LLMCallError`, attempt counts, and redacted durable LLM I/O logging via `core.llm_io_logger`. | done | Production code avoids provider/model-name branches and does not read secrets directly from env. Live endpoint tests are deliberately deferred to `05.T2` / `INT-A-C`. Manual verification completed during 05.04 (the patched focused tests now pass). | `python -m pytest tests/test_call_llm.py -q` **7 passed** (system interpreter; `uv run` blocked by the documented Windows `.venv\lib64` removal error). Import done-check had already passed. |
| 05.04 | LLM Inference Boundary | Provider-neutral structured-output contract in `llm/call_llm.py` plus `tests/test_structured_output.py`, **built across 2 prompts**. Prompt 1 (helpers): `extract_first_json_object(text)` strips a wrapping markdown fence then returns the first balanced `{...}`, ignoring braces/quotes inside quoted strings and escaped quotes (`None` when unbalanced); `validate_structured_text(text, schema_model)` strict-parses JSON + `model_validate`, raising `StructuredOutputError` (subclass of `ValueError`); `validate_with_salvage(text, schema_model)` runs strict → extraction → one final validation (≤2 attempts, no loop). Prompt 2 (bounded structured call): `call_llm_structured(messages, endpoint, *, schema_model, validate_retry_cap, …, call=call_llm)` — a new async wrapper that leaves the plain-text `call_llm()` API untouched. It selects the request option from `endpoint.grammar_constraint_strategy` only (`"gbnf"` → compile `schema_model.model_json_schema()` via `json_schema_to_gbnf()` into a provider-neutral `extra_body["grammar"]`; `"json_mode"` → `response_format={"type":"json_object"}`; unknown → `UnsupportedGrammarStrategyError`), validates each response with Pydantic, retries up to `validate_retry_cap`, then runs `validate_with_salvage()` once (logging `structured_output_salvage_used`) or raises `LLMCallError` (hard failure with `attempt_count`). `call` is an injectable seam for tests. | done | Operates on text + Pydantic models + the configured endpoint only — no network/provider/model-name branch, no Ollama `format` / OpenAI-only payload. `validate_retry_cap` is a **required** keyword sourced from `config.runtime.model_validate_retry_cap` (default 3, `core/config_loader.py:74`) — no hidden constant; config key **not duplicated** (exists from group 02). Validation failures are never swallowed (never returns `None`). GBNF grammar is a hint; Pydantic still guards the FSM if an endpoint ignores it. `FailureObject` not hardcoded; tests use a local synthetic `extra='forbid'` model. No fuzzy free-text parser. **Unsupported schema features** (inherited from the 05.02 GBNF compiler, which fails closed with `ValueError` at structured-call setup): external `$ref`, `allOf`, `not`, tuple arrays/`prefixItems`, `patternProperties`, unrestricted `additionalProperties`, and constraint keywords like `pattern`. Not yet wired into `node_adversarial_critics` (M07). | `python -m pytest tests/test_structured_output.py -q` **14 passed** (6 helper + 8 structured-call: first-valid returns w/ 1 call; retries to cap then succeeds (3 calls); salvage after cap on fenced text; hard `LLMCallError` w/ `attempt_count==2` when unsalvageable; gbnf injects `extra_body["grammar"]` w/ `response_format None`; json_mode sets `response_format`; unknown strategy raises before any call; non-positive cap rejected). `uv run pytest` blocked by the documented Windows `.venv\lib64` removal error; ran under the system interpreter as in prior 0x increments. `tests/test_call_llm.py` + `tests/test_gbnf_compiler.py` still **14 passed** after the new `json_schema_to_gbnf` import (no regression). |

| 05.05 | LLM Inference Boundary | Strict Jinja2 prompt loader in `prompts/prompt_loader.py` (replaces the `load_prompt` `NotImplementedError` stub) plus `tests/test_prompt_loader.py`. `PromptLoader(template_dir=None)` defaults its root to the `prompts/` package dir; `template_name_for_node(node_name)` validates a canonical `^node_[a-z0-9]+(_[a-z0-9]+)*$` name then returns `<node_name>.xml.j2`; `load()` returns the compiled `jinja2.Template`; `render(node_name, context)` renders it. Jinja `Environment` uses `StrictUndefined` (missing var → `UndefinedError`, never blanked), `autoescape=False` (prompts are raw XML for the model, not HTML), `FileSystemLoader` rooted at the template dir. New exceptions `PromptTemplateError` / `InvalidNodeNameError(ValueError)` / `PromptTemplateNotFoundError(FileNotFoundError)`. | done | No prompt prose embedded in Python; content stays in `*.xml.j2` templates (Architecture_Map §4 split 2). The node-name regex is the path-traversal guard — dots/slashes/backslashes/uppercase cannot pass, so a name can never escape `prompts/`. No node logic added; `fsm/nodes/` untouched; no model/endpoint/provider assumption. **Template-content stub (honest):** the 11–14 `node_*.xml.j2` prompt templates the design enumerates (e.g. `node_draft_prose`, `node_plan_beat_pad`, the three `node_adversarial_critics_*`) are **not authored here** — they remain deferred M04 content, written when their owning nodes (M05/M06/M07/M08) land. The loader is ready for them; `prompts/` currently holds only `prompt_loader.py` + `__init__.py` + `.gitkeep` (no templates yet). Tests author throwaway templates in `tmp_path`. | `python -m pytest tests/test_prompt_loader.py -q` **14 passed** (render from temp template; missing var → `UndefinedError`; missing template → `PromptTemplateNotFoundError`; 8 invalid/traversal names → `InvalidNodeNameError`; traversal cannot escape the root; XML tags + angle-bracket values preserved verbatim with no `&lt;` escaping; default dir resolves to the `prompts/` package). `uv run pytest` blocked by the documented Windows `.venv\lib64` removal error; ran under the system interpreter as in prior 0x increments. |

| 05.T1 | LLM Inference Boundary (test) | Consolidation + coverage audit of the deterministic, no-network M04 suite: `tests/test_tokenizer.py`, `tests/test_gbnf_compiler.py`, `tests/test_structured_output.py`, `tests/test_prompt_loader.py`. Reviewed all four (already provider-neutral, fast, no live endpoint, no localhost/model names). Only change: **added a 7-case parametrized `test_unsupported_schema_features_fail_closed`** to `tests/test_gbnf_compiler.py` covering `allOf`, `not`, `prefixItems`, `patternProperties`, unrestricted `additionalProperties`, `items`-less arrays, and external `$ref` — the prior file tested only `pattern`. No production code touched; no other test file needed new cases (all checklist items already covered). | done | 48 M04 tests total: tokenizer 6, gbnf 14, structured-output 14, prompt-loader 14. Strict validation was **not** weakened and no failure is skipped. The tokenizer now degrades to deterministic char-heuristic counting if `tiktoken` is unavailable, so the earlier system-Python `tiktoken` import artifact is no longer a red test path. | Two earlier `uv run pytest tests/test_tokenizer.py tests/test_gbnf_compiler.py tests/test_structured_output.py tests/test_prompt_loader.py` attempts were blocked by `.venv\lib64`; fallback then showed **47 passed, 1 failed** before the tokenizer fallback existed. After hardening, focused tokenizer fallback verification passed: `python -m pytest tests/test_tokenizer.py -q --basetemp .pytest_tmp_tokenizer` -> 6 passed. |

| 05.T2 | LLM Inference Boundary (test) | `tests/test_llm_live_endpoint.py` — live, config-driven smoke tests that exercise the **real** endpoint/model from `config.yaml` (no fake backend, no local-server assumption, no provider/model special-casing), **built across 2 prompts**. A `live_endpoint` fixture loads `config.yaml` via the real `load_config`, injects placeholder secrets (`monkeypatch.setenv`) only for endpoints lacking a real `{NAME}_API_KEY` so local unauthenticated servers can load, selects the preferred default endpoint (`drafter`, else first configured), and now also carries `validate_retry_cap` from `config.runtime.model_validate_retry_cap`. `_run_or_skip` converts `LLMCallError`/`httpx.HTTPError`/`OSError` into an explicit `pytest.skip` naming endpoint/base_url/model + missing-secret note + error (never the secret). **Prompt 1** (plain `call_llm()`): plain-text smoke (non-empty `text`, `model_name == configured model`, no prose assertion) and streaming callback (`on_token=chunks.append`; final text non-empty unless no chunks exposed; `chunks` match `streamed_chunks`; buffering endpoints don't fail). **Prompt 2** (structured `call_llm_structured()` from 05.04): tiny provider-neutral `_LiveAnswer(answer:str, ok:bool, extra='forbid')`; asks for a tiny JSON object and validates the returned object is a `_LiveAnswer` with non-empty `answer` and bool `ok` (shape only, never exact content); retry cap stays from config; an unsupported/absent `grammar_constraint_strategy` skips via `UnsupportedGrammarStrategyError` with a capability-named reason — never faked, never bypassing `call_llm_structured`. Opt-out via `MUSEAI_SKIP_LIVE_LLM`. | done | No production code changed. Three `@pytest.mark.integration` tests. The structured path is **provider-neutral**: strategy comes from the endpoint (`gbnf`→grammar / `json_mode`→`response_format`), nothing hardcoded. **Live success paths are unverified in this environment** — no server at the configured `localhost:11434`, the Windows `uv` blocker forces the 3.14 fallback (no `tiktoken`), and the configured `drafter` declares `grammar_constraint_strategy="none"` (config.yaml uses `"json_schema"`/`"none"`, neither of which `call_llm_structured` supports — only `gbnf`/`json_mode`), so the structured test correctly hits the unsupported-capability skip. By design the harness skips, never fakes; rerun under WSL2/`uv` with a reachable server and a `gbnf`/`json_mode` endpoint to exercise the 200 + structured-validate paths. Secret-leak check passed (only env var **names** appear). **Carryover note for INT-A-C:** the default adapter resolves the no-`/v1` `localhost:11434` base_url to `/chat/completions`, not Ollama's OpenAI-compat `/v1/chat/completions` — a dialect/path question for integration, not changed here. | Two `uv run pytest tests/test_llm_live_endpoint.py -q` attempts blocked by `.venv\lib64`. Fallback (system Python 3.14): **3 skipped**, all explicit — plaintext + streaming: `live endpoint 'drafter' (http://localhost:11434, model=cryptidbleh/…) … no real DRAFTER_API_KEY is set: inference boundary failed: No module named 'tiktoken'`; structured: `endpoint 'drafter' declares grammar_constraint_strategy='none', which is not a supported structured-output capability`. Confirms config-driven endpoint selection, genuine call/strategy attempts, useful skip reasons, and no key leakage. Rerun under WSL2/`uv` (3.12, tiktoken present) with a reachable supported-strategy server for the success paths. |

| INT-A-C | Config/Startup × LLM Inference Boundary | `tests/test_llm_config_integration.py` — one config-driven integration test proving the real `load_config` selects a configured endpoint/model, `llm.call_llm` calls that endpoint, and the M14 LLM I/O logger records the call without leaking the `api_key`. Mirrors the 05.T2 selection policy (prefer the default generation endpoint `drafter`, else first configured; no provider/host/Ollama/model hardcoding). A `live_endpoint` fixture loads via the real loader and injects a placeholder secret only for endpoints lacking a real `{NAME}_API_KEY`; `MUSEAI_SKIP_LIVE_LLM` opts out; `_run_or_skip` converts `LLMCallError`/`httpx.HTTPError`/`OSError` into an explicit skip naming endpoint/base_url/model (never the secret). An `io_log_capture` fixture attaches a temporary `FileHandler` to the real `llm_io` singleton logger (which has no path param), capturing exactly this call's record to `tmp_path` — the genuine production logging path, isolated, without weakening the leak assertion. | done | No production code changed (the temp-log capture is a test-side handler on the real logger, not a production path shim). Model-agnostic: asserts `response.text` non-empty, `response.model_name == endpoint.model_name`, `response.endpoint_base_url == endpoint.base_url`, a durable record was written (configured model name present in log text), and `endpoint.api_key not in log_text` — never asserts generated prose. **Live success path unverified in this environment** (same constraints as 05.T2): no server at the configured `localhost:11434`, the Windows `uv` blocker forces the Python 3.14 fallback (no `tiktoken`), so the call cannot complete and the harness skips by design rather than faking a backend. The `api_key` rides in the `Authorization` header (not the logged body) and body-side secret keys are redacted by `_redact_secrets`, so the leak assertion is meaningful when the success path runs. Rerun under WSL2/`uv` (3.12, `tiktoken` present) against a reachable configured endpoint to exercise the 200 + log-write + no-leak path. | `uv run pytest tests/test_llm_config_integration.py` blocked by the documented Windows `.venv\lib64` removal error. Fallback (system Python 3.14): **1 skipped** — explicit reason `no configured live endpoint was available: 'drafter' (http://localhost:11434, model=cryptidbleh/…) could not complete a call; no real DRAFTER_API_KEY is set: inference boundary failed: No module named 'tiktoken'`. Confirms real config-driven endpoint selection, a genuine call attempt, and the required explicit "no configured live endpoint was available" skip reason. Rerun under WSL2/`uv` with a reachable server for the success path. |

| 06.01 | Context Assembly & Budgeting | `fsm/nodes/node_assemble_context.py`: full first-pass, unpruned layered context package (`relational`, `summaries`, `temporal`, `flavour`, `coreference_candidates`, `macro_constraints`, `meta`), drop-order metadata, and `_safe_store_read()` adapter seam that degrades only documented not-yet-built stores to empty unavailable layers. Relational reads use the 04.03 SQLite helpers around the active `fsm_pointer`; summaries read persisted `RaptorNodes` by root/level; provisional coreference candidates read raw pending claims from the separate provisional store; temporal attempts the deferred Graphiti client and merges any future raw temporal coreference links into `coreference_candidates`; flavour attempts the deferred Chroma/HNSW client; the node overwrites `state["active_context_package"]` and returns the state. | done | Model-free and read-only at the node boundary: no LLM import/call, no token budgeting, no layer trimming, no coreference confidence tiering, no store writes, and no edits outside `fsm/nodes/node_assemble_context.py` plus this ledger entry. `memory/graphiti_client.py` temporal reads and `memory/chroma_client.py` flavour reads currently raise `NotImplementedError`, so the adapter records `unavailable` and returns empty valid layers until those M02 stores are built. Macro constraints remain an explicit empty reserved layer. Budgeting/pruning is deliberately deferred to `06.02`; coreference confidence tiering is deliberately deferred to `06.03`. | Exact `uv run python -c "from fsm.nodes.node_assemble_context import node_assemble_context; print(node_assemble_context)"` is blocked on this Windows host by the existing uv cache / `.venv\lib64` permission failure (`.venv\lib` in use). Fallback checks passed under system Python: import prints the function; a synthetic temp SQLite + provisional fixture invokes the node once and returns the same state with `active_context_package` populated; relational data, seeded `RaptorNodes`, and seeded provisional coreference candidates are present; temporal and flavour layers are empty with unavailable meta; `llm` is not imported. `python -m py_compile fsm/nodes/node_assemble_context.py` passed, and an `rg` scan found no LLM or write-helper references. |
| 06.02 | Context Assembly & Budgeting | Backend-agnostic token sizing and drop-priority pruning in `fsm/nodes/node_assemble_context.py`, plus the named `context.token_budget` config key in `config.yaml` and `core/config_loader.py`. The node resolves the existing typed drafter role (`config.endpoints.drafter`), serializes each context layer deterministically, counts it through `llm.tokenizer.count_tokens()` using the drafter endpoint's `tokenizer_family` and model name, records per-layer counts/package total/configured budget in `meta["token_sizing"]`, then prunes only when over budget: `flavour` first, then tiered `summaries.by_level` entries in least-critical order (`beat`, `scene`, `chapter`, `arc`) with a recompute after every drop. | done | Relational truth is never dropped or truncated; if the package remains over budget after flavour and summary tiers are gone, `meta["over_budget"]` remains true instead of corrupting canonical facts. Pruning metadata records `pruned_layers`, `initial_token_total`, `final_token_total`, and mirrored token-sizing fields for downstream observers/tests. No provider branches, model-name branches, LLM calls, store writes, coreference confidence tiering, or unrelated config edits. `context.token_budget` is a required positive typed config key with a provisional uncalibrated default in `config.yaml`. Coreference tiering remains deferred to `06.03`. | `python -m py_compile fsm/nodes/node_assemble_context.py` passed. Exact `uv run python -c "from fsm.nodes.node_assemble_context import node_assemble_context; print(node_assemble_context)"` could not run in this Windows-hosted session: attempt 1 failed because uv could not open its AppData cache; attempt 2 through `wsl -e bash` failed because no WSL distribution is installed. Fallback synthetic check under system Python passed: above-budget package pruned `flavour` first, then `summaries.by_level.beat`, `scene`, and `chapter` with recomputation stopping once under budget; relational stayed intact; `meta["pruned_layers"]` and `final_token_total` were recorded; already-under-budget package remained unpruned; relational-only over-budget condition was recorded with `over_budget=True`. `rg` found no LLM/inference call references in the node. Manual WSL command to run: `uv run python -c "from fsm.nodes.node_assemble_context import node_assemble_context; print(node_assemble_context)"`. |
| 06.03 | Context Assembly & Budgeting | Config-driven three-tier coreference injection and whole-node graceful-degradation hardening in `fsm/nodes/node_assemble_context.py`, plus named context-band keys `context.coreference_high_confidence` and `context.coreference_mid_confidence` in `config.yaml` and `core/config_loader.py`. After raw package sizing/pruning, the node classifies provisional candidates by configured confidence: high-band entries become machine-readable confirmed facts in `relational["coreference_facts"]`; mid-band entries remain in `coreference_candidates` as explicitly unconfirmed `epistemic_belief` objects; low-band entries are excluded from retained package layers. Final token sizing is recomputed after deterministic tiering. | done | The context-injection bands are separate from ingestion-time `thresholds.coreference_confidence_floor`; no reuse of that key, no hardcoded band thresholds in node logic, no LLM calls, no confidence tiering in stores, no low-confidence leakage, and no store writes. `meta["coreference_resolution"]` records ordering (`after_budgeting_pruning`), configured band values, confirmed fact count, epistemic belief count, and excluded low-confidence count. Config validates both bands as normalized probabilities and requires high >= mid. Graphiti temporal and Chroma flavour reads still degrade honestly through `_safe_store_read` to empty layers with `available=False` / not-yet-implemented reasons until those M02 stores are built. The coreference bands are provisional and uncalibrated per `Open_Problems.md` threshold-calibration guidance. | `python -m py_compile fsm/nodes/node_assemble_context.py core/config_loader.py` passed. Synthetic micro-check with high/mid/low candidates and config bands `0.85`/`0.50` passed: high claim retained only as confirmed fact, mid retained only as unconfirmed Epistemic Belief, low claim id absent from retained facts/beliefs, meta counts `1/1/1`, and meta band values came from config. Full fallback node check with Graphiti/Chroma stubbed and temp SQLite + provisional stores seeded passed: `active_context_package` was produced, relational facts and `RaptorNodes` summaries were present, tiered coreference retained high fact + mid belief and excluded low, flavour/temporal recorded unavailable, final budgeting/pruning meta was present, and no LLM/write references were found by `rg`. `scripts/audit.py` and `scripts/scan_literals.py` are absent; manual scans found only structural literals. Exact `uv run python -c "from fsm.nodes.node_assemble_context import node_assemble_context; print(node_assemble_context)"` could not run after two attempts in this Windows-hosted session: uv AppData cache permission failure, then no installed WSL distribution. Manual WSL command remains that exact `uv run python -c ...` smoke plus the seeded-node fixture if desired. |
| 06.T1 | Context Assembly & Budgeting (test) | `tests/test_context_assembly.py` with deterministic, no-network tests for backend-agnostic layer token sizing, tokenizer-family routing, package totals, under-budget no-op behavior, drop-priority pruning, relational-truth preservation under budget pressure, and config-read context budget/coreference bands. Tests inject a synthetic `app_config` and monkeypatch store-read seams so no live model, network endpoint, or memory store is contacted. | done | No production code changed. The repo `.venv\lib64` path remains locked on this Windows host, so the done-check used an isolated uv project environment under `%TEMP%` while still running through `uv`. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_context_assembly.py` passed: 7 passed. |

| 06.T2 | Context Assembly & Budgeting (test) | `tests/test_context_assembly.py` extended (same file from 06.T1) with deterministic, no-network tests for graceful degradation against the stubbed M02 stores and three-tier coreference-confidence classification. Degradation: with the **real** (un-monkeypatched) Graphiti/Chroma `.query()` stubs raising the documented `NotImplementedError`, `build_context_package` still returns a valid package — `flavour` is `[]`, `temporal` is the empty `{records, coreference_candidates}` shape, and `meta` records both layers `available=False` with a "not yet implemented" reason; meanwhile the relational hub, persisted `RaptorNodes` summaries, and the separate provisional-claim store still populate from seeded real fixtures (temp SQLite via `init_db` + raw inserts, provisional via `upsert_claim`); an unexpected non-degrade `RuntimeError` from a store read propagates (the seam absorbs only the not-built signal). Three-tier: high/mid/low synthetic candidates classify to confirmed fact / unconfirmed Epistemic Belief / excluded using config-injected bands; mid is an `epistemic_belief` with `is_confirmed=False` and never a fact; the low-confidence link's markers/id appear nowhere in the serialized package; `meta["coreference_resolution"]` records per-tier counts; lowering the high band reclassifies the mid candidate to a fact (proves bands are config-driven). | done | No production code changed. **The degradation tests intentionally exercise the stubbed Graphiti/Chroma stores** (real `NotImplementedError` path, not monkeypatched) to prove `_safe_store_read` degrades only the documented not-built condition. Synthetic state + temp SQLite/provisional fixtures + injected `SimpleNamespace` config; no network, no LLM, `char_heuristic` tokenizer. Touched only `tests/test_context_assembly.py`. | `uv run pytest tests/test_context_assembly.py` blocked by the documented Windows `.venv\lib64` permission error (`failed to remove file ... lib64: Access is denied`). Isolated uv env (as in 06.T1): `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_context_assembly.py -q` → **14 passed in 0.66s** (7 from 06.T1 + 7 new). |

| INT-B-D | Persistent Memory × Context Assembly & Budgeting | `tests/test_context_memory_integration.py` (2 tests) — integration of M02 stores with the M03 `node_assemble_context`, no inference. Seeds a small synthetic project into a **real** temp SQLite relational hub (planner-owned Arcs/Chapters/Scenes/Characters/Threads/RaptorNodes via FK-ordered raw inserts since no write helper owns those yet; committed beats + bundled PAD + a thread open→progressing transition via the real `upsert_beat_commit`) and a **real** temp provisional store (`upsert_claim`) with high/mid/low-band coreference claims. Loads config through the real `load_config` (placeholder endpoint secrets injected via env; nothing hardcoded) and resolves the drafting endpoint's `tokenizer_family`/`model_name`. Invokes the real async `node_assemble_context` and asserts: relational layer reflects ordered scenes, both committed beats, and the open thread (progressed thread excluded); summaries reflect seeded RaptorNodes; sizing routed through the config-resolved `tokenizer_family`/`model_name` (a `count_tokens` spy delegating to the real tokenizer records the routing) and `meta.token_sizing` mirrors the config budget; three-tier coreference with config bands → high=confirmed fact, mid=flagged unconfirmed Epistemic Belief, low fully excluded from the serialized package; temporal (Graphiti) and flavour (Chroma) layers record `available=False` / "not yet implemented" without raising. A second test forces a tiny budget (reusing the real-resolved tokenizer family, not hardcoded) and asserts flavour-then-summary-tier pruning ran while relational truth was preserved and the budgeting decision recorded in `meta`. `httpx.AsyncClient` and `llm.call_llm.call_llm`/`call_llm_structured` are guarded to raise, proving the node is model-free. | done | **Graphiti temporal and Chroma flavour reads still degrade to empty `unavailable` layers because those M02 stores remain deferred stubs** — a later M02 pass (real `graphiti_client`/`chroma_client`) must revisit this integration to exercise the populated temporal/flavour paths and any temporal coreference-link merge. Created only the new test file; no production code changed. Provider-neutral and deterministic (real `tiktoken` encoding loaded locally; if absent it degrades to the char heuristic, family routing unchanged). Planner-owned parent rows seeded by raw SQL only because no write helper owns them yet (thread/scene/arc creation is the planner's job, M-not-yet-built). | `uv run pytest tests/test_context_memory_integration.py` blocked by the documented Windows `.venv\lib64` permission error. Isolated uv env (as in 06.T1/06.T2): `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_context_memory_integration.py -q` → **2 passed in 4.09s**; combined with the 06.T1/06.T2 file: `tests/test_context_assembly.py tests/test_context_memory_integration.py` → **16 passed in 1.26s**. |

| VIS-01 | M02-M06 visualization frontends | `tests/frontends/` Streamlit pages for M02 memory stores, M03/M01 state schemas, M04 inference-boundary helpers, M05 prompt-loader UI, M06 context assembly, plus a `run_all.py` launcher and shared temp-store seed helpers. Every page calls real module functions against session-scoped temp files; no production stores are touched. | done | Out-of-band visualization utility, not a numbered build increment; `Next up` remains `07.01`. Streamlit was already present in `pyproject.toml`/`uv.lock`, so no dependency files were changed in this session. M06 honestly surfaces Graphiti/Chroma as unavailable via existing degradation metadata because those stores remain deferred stubs. UI config-shaped defaults read `config.yaml` context defaults directly without requiring endpoint secrets. | In-memory syntax compile passed for all 7 frontend files. Isolated uv env import smoke passed for `m02_memory`, `m03_state`, `m04_inference`, `m05_prompt_loader`, `m06_context_assembly`, and `run_all`. Temp-store smoke seeded real SQLite/provisional stores and built a context package: relational arc `arc-1`, one confirmed coreference fact, temporal/flavour unavailable as expected. Streamlit launcher started at `http://127.0.0.1:8501`; localhost probe returned HTTP 200. Stub scan over `tests/frontends` found no `STUB`/`TODO`/`FIXME`/`NotImplementedError`/`asyncio.gather` markers. |

| VIS-02 | M02-M06 visualization frontend UX | Shared Streamlit UX helpers (`info_icon`, `section_header`, `output_label`, page intro/workspace helpers) plus guided workflow layouts across M02-M06. Pages now present configure/run/output flows with hover help on testing areas, inputs, and outputs, while still calling real module APIs against temp stores. `STREAMLIT_FRONTENDS.md` documents the hover-help convention and corrected several stale behavior notes. | done | Out-of-band visualization utility; no production modules, dependency files, or real data artifacts changed. `Next up` remains `07.01`. Numeric literals introduced in UI code are structural layout/CSS values or deterministic synthetic fixture values; tunable context defaults still read from `config.yaml`. | In-memory syntax compile passed for all 7 frontend files. Isolated uv env import smoke passed for `m02_memory`, `m03_state`, `m04_inference`, `m05_prompt_loader`, `m06_context_assembly`, and `run_all`. Temp-store M06 smoke passed: seeded real SQLite/provisional stores, assembled package with relational arc `arc-1`, one confirmed coreference fact, and temporal/flavour unavailable metadata. Streamlit launcher started at `http://127.0.0.1:8502`; localhost probe returned HTTP 200. Manual stub scan over `tests/frontends` found no `STUB`/`TODO`/`FIXME`/`NotImplementedError`/`asyncio.gather`; Streamlit signatures confirmed `help=` support on button, form submit, and metric. |

| 07.01 | Persistent Memory / Planning (M02, written by M05/M17) | Five planning proposal-surface tables added to the existing idempotent schema-init path in `memory/sqlite_db.py`: `PlanningSnapshot`, `PlanningNode`, `PlanningAnnotation`, `PlanningRevision`, `PlannerToolCallTrace` — column names/types/`CHECK` enums/FKs verbatim from `Data_Structures.md` §2.7. Plus six lookup indexes (`PlanningNode(snapshot_id,level)`, `PlanningNode(parent_id)`, `PlanningAnnotation(snapshot_id)`, `PlanningAnnotation(target_node_id)`, `PlanningRevision(snapshot_id)`, `PlannerToolCallTrace(snapshot_id,planner_level)`) in the existing `_INDEX_STATEMENTS` style. | done | Appended to `_SCHEMA_STATEMENTS` after `RaptorNodes`; the nine narrative tables' DDL untouched. Tables kept in the same DB file but documented as a **proposal surface, not committed truth**. Nullable circular ref preserved: `PlanningSnapshot.active_revision_id` → `PlanningRevision(revision_id)` declared forward (stays NULL until first revision); SQLite resolves FK parents at write time, not CREATE time. Hard `CHECK` enums verbatim for snapshot `status`, node/annotation/trace `level`/`planner_level`/`target_level`, annotation `note_type`/`scope`/`priority`/`status`. **No write/read helpers yet (that is 07.02); no planner loop/validator/library logic.** `init_db` docstring updated to list the planning tables honestly. Touched only `memory/sqlite_db.py` (+ this ledger row). | Temp nested DB init through `init_db` (run twice = idempotent): `sqlite_master` confirms all 5 planning tables present, all 9 narrative tables still present; a bad `PlanningNode.level` (`'BOGUS_LEVEL'`) was rejected by the `CHECK` and rolled back; a snapshot inserted with NULL `active_revision_id` was accepted (circular ref non-blocking); all six `idx_planning*`/`idx_plannertool*` indexes present. Ran under system `python` (the documented Windows `.venv\lib64` blocker still applies to `uv run`); rerun under WSL2/`uv` to exercise the same path. |

| 07.02 | Persistent Memory / Planning (M02, written by M05/M17) | Idempotent, replay-safe write/read helpers over the five §2.7 planning tables in `memory/sqlite_db.py`. **Snapshot:** `create_planning_snapshot` (upsert keyed by `snapshot_id`, preserves `created_at` on replay), `transition_snapshot_status` (sets `approved_at` when status→`approved`; raises `ValueError` if asked to set `approved` with `has_unresolved_conflict=True`), `set_snapshot_active_revision`, `get_planning_snapshot`. **Node:** `upsert_planning_node` (keyed by `node_id`), `update_planning_node_status`, `set_planning_node_locked`, `get_planning_nodes` (optional `level`), `get_planning_nodes_by_parent` (`None`→roots via `IS ?`). **Annotation:** `insert_planning_annotation` (append-only, `ON CONFLICT DO NOTHING`), `update_annotation_status` (status+`planner_response`+`resolved_at`), `get_annotations_for_node`, `get_annotations_for_snapshot`. **Revision:** `insert_planning_revision` (insert `ON CONFLICT DO NOTHING` + advance owning snapshot's `active_revision_id` in one txn), `get_revisions_for_snapshot` (newest-first default). **Trace:** `insert_planner_tool_call_trace` (`ON CONFLICT DO NOTHING`), `get_traces_for_snapshot` (optional `planner_level`/`loop_index`). Shared `_planning_write` (single `BEGIN`/commit/`rollback`), `_fetch_one`, `_now_iso`. | done | Persistence primitives only — **no annotation compiler, conflict detection, planner loop, validators, or tool registry** (those are the M05 library, later). No promotion into narrative tables/committed prose; no thresholds/caps/bands. All writes idempotent per logical op: snapshot/node upsert by PK; annotation/revision/trace insert `ON CONFLICT DO NOTHING` so replay never duplicates and a revision never silently overwrites a prior one (§2.7 lifecycle); `locked_pinned`/`approved` are persisted state only (gate enforcement stays M05/M01). Reads return plain dicts (never raw `sqlite3.Row`), init DB if needed; single-row miss → `None`, list miss → `[]`. Enum vocabularies enforced by the table `CHECK`s, not re-listed in Python. Touched only `memory/sqlite_db.py` (+ this ledger row). | Temp nested DB: snapshot→node→annotation→revision (confirmed snapshot `active_revision_id` advanced to `r1`)→trace, each read back through public helpers; a full replay of the same logical writes left row counts unchanged (`{snapshot:1,node:2,annotation:1,revision:1,trace:1}`) and preserved snapshot `created_at`; status/locked transitions, the `approved`+`approved_at` path, and the unresolved-conflict `ValueError` gate all fired; a second revision preserved the first (`['r2','r1']` newest-first / `['r1','r2']` oldest-first), advanced `active_revision_id` to `r2`, kept the `r2→r1` parent chain; missing single-row→`None`, missing list→`[]`. `python -m py_compile memory/sqlite_db.py` clean. Ran under system `python` (Windows `.venv\lib64` blocker still applies to `uv run`); rerun under WSL2/`uv` to exercise the same path. |

| 07.00 | Persistent Memory / Planning (M02 schema+helpers; planning tables written by M05 later) | **Planning-persistence prerequisite for build-07**, completed across this session's three prompts in `memory/sqlite_db.py`, with a new round-trip test `tests/test_planning_store.py`. (1) Five §2.7 planning proposal-surface tables (see `07.01` row). (2) Idempotent planning-table write/read helpers — snapshot/node/annotation/revision/trace (see `07.02` row). (3) **This prompt:** plan-time narrative outline writers `upsert_arc_plan`, `upsert_chapter_plan`, `upsert_scene_plan` (sets `ordering`), `upsert_beat_plan` — they persist validated planner *structure* into the canonical `Arcs`/`Chapters`/`Scenes`/`Beats` rows. **Changed files:** `memory/sqlite_db.py`, `tests/test_planning_store.py`. | done | **Plan-time outline writers did not previously exist — added here** (the only prior narrative write was the commit-time `upsert_beat_commit`, kept distinct). All four upsert by primary key (replay-safe). The beat plan-writer writes structural columns only (`id`/`scene_id`/`beat_index`/`status`) and never `prose`/`word_count`/`committed_at`, so a plan-time replay after a commit cannot clobber committed prose (proven by test). **Design decision (user-confirmed):** the narrative tables have no column for chapter obligations (dramatic function / emotional shift / thread progress / scene-planning constraints) or the tailored beat PAD behavioral-constraint string, so — per `LangGraph_Nodes.md` 83/117 (node_plan_chapter writes "Chapter rows … and PlanningNode rows at level='chapter'"; node_plan_beat writes "Beat Nodes … including the tailored PAD behavioral constraint string") — `upsert_chapter_plan`/`upsert_beat_plan` persist that rich detail into a paired `PlanningNode` (`level='chapter'`/`'beat'`, same DB) as JSON in `purpose`. **No DDL change to the nine narrative tables** (honored the STOP-and-report gate). If any obligation/PAD field is passed without `snapshot_id`+`node_id`, the writer raises `ValueError` rather than silently dropping it. No planner loop, validator, registry, node, or annotation-compiler logic. Touched only `memory/sqlite_db.py` + the new test. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_planning_store.py -q` → **8 passed in 2.66s** (planning-surface round-trip snapshot→node→annotation→revision[active_revision_id advances]→trace; replay idempotence; revision history preserved not overwritten; approved-gate `ValueError` on unresolved conflict; outline round-trip arc→chapter[obligations in PlanningNode JSON]→scene[ordering ASC]→beat[PAD string in PlanningNode JSON]; outline replay idempotent + committed prose preserved; obligations-without-node raises; plan-node parent/child traversal). Full suite `uv run pytest -q` → **125 passed, 4 skipped, 2 failed**; the 2 failures are pre-existing live-endpoint tests in `tests/test_server_connections.py` hitting an unreachable `localhost:11434` Ollama (no server / model absent) — unrelated to this change. Repo `.venv\lib64` still locked on this Windows host, so used the isolated `%TEMP%` uv env as in 06.T1/06.T2/INT-B-D. |

| 07.0a | Config/Startup (M14) | Build-07 planning prerequisite: the `planning.*` config block + typed `PlanningConfig` + the two §3a cross-field safety rules, built across two prompts. **Changed files:** `config.yaml`, `core/config_loader.py`, `tests/test_config.py`. **Prompt 1 — `config.yaml`:** new top-level `planning:` (`execution_mode`=`macro_outline_before_draft`, `approval_mode`=`off`, per-level cap maps `planner_max_deliberation_loops` 4/3/3/2/2 and `planner_max_tool_calls_per_loop` 8/6/5/4/3, and `planner_required_checks` per-level validator lists) — values verbatim from §3a, annotated as proposed defaults. **Prompt 1 — `config_loader.py`:** `PlanningConfig(BaseModel, extra="forbid")` with the five typed fields; enum validators (`execution_mode ∈ {rolling, macro_outline_before_draft}`, `approval_mode ∈ {off, macro_outline}`) and an exact-five-level-keys validator (`{global,arc,chapter,scene,beat}`) over all three maps; `planning: PlanningConfig` added to `AppConfig`. **Prompt 2 — `config_loader.py`:** top-level `@model_validator(mode="after")` `_validate_planning_safety_rules` on `AppConfig` enforcing the two cross-sub-model §3a rules — (a) `approval_mode=='macro_outline'` only when `execution_mode=='macro_outline_before_draft'`, else raise; (b) `runtime.headless_mode and planning.approval_mode!='off'` → raise (a headless run must never park at an unclearable approval gate; design §5 lines 219–220). **Prompt 2 — `tests/test_config.py`:** added a `planning` block to the `valid_config_yaml()` fixture + typed-value assertions; new tests for both unsafe combinations and a real-`config.yaml` regression guard. | done | **Scoped to the five §3a keys + the two cross-field rules.** §3a *also* defines `planner_deliberation_enabled`/`planner_tool_calling_enabled`/`planner_deliberation_intensity` and a positive-cap / intensity-enum rule; because `PlanningConfig` is `extra="forbid"`, those toggles aren't in `config.yaml` yet (adding them without model fields would fail the load) — deferred to a later increment, intentionally NOT added here. `thresholds.pad_ewma_alpha` untouched. `headless_mode`/`approval_mode` validated where they live (not moved). `extra="forbid"` and existing validators were not weakened to pass tests. Error messages name the failing rule (operator's first clue). **Prompt-1's expected regression is now RESOLVED:** `planning` is a required `AppConfig` field, so the Prompt-1 fixture gap (`test_valid_synthetic_config_loads_typed_values` failing for lack of a `planning` block) was fixed by adding the block to `valid_config_yaml()` in this prompt. No planner routing / node behavior added — config validation only. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_config.py -q` → **7 passed** (valid block loads + exposes typed planning values; `approval_mode='macro_outline'` with `execution_mode='rolling'` raises; `headless_mode=true` with `approval_mode='macro_outline'` raises; real `config.yaml` still loads; original unknown-key / type-mismatch / missing-secret tests still pass). Full suite (isolated `%TEMP%` uv env) → **128 passed, 4 skipped, 2 failed**; the 2 failures are the pre-existing live-endpoint tests in `tests/test_server_connections.py` hitting an unreachable `localhost:11434` Ollama (404, no server) — unrelated to config (a third test in that file passes, confirming the real config with `planning` loads). Repo `.venv\lib64` still locked on this Windows host, so used the isolated `%TEMP%` uv env as in 06.T1/06.T2/INT-B-D. |

| 07.0b | Coordinator/State Machine (M01) | Build-07 planning prerequisite: planning-subsystem state shape in `fsm/state.py` + tests in `tests/test_state.py`. **`OrchestratorState`** gains the eleven §1.x planning fields verbatim — `planning_execution_mode: str`, `approval_mode: str`, `macro_outline_ready/macro_outline_approved/awaiting_planning_approval: bool`, `planning_snapshot_id/active_planning_revision_id: str\|None`, `planner_loop_index/planner_tool_call_count: int`, `planner_deliberation_trace: Annotated[list[dict], operator.add]`, `planning_block_reason: str\|None`. **`FSM_Pointer`** gains `beat_id: str = ""` (current-beat *identity*, alongside `beat_index` *position*; safe default so existing `extra="forbid"`/`strict=True` construction sites keep working). **`make_initial_state`** seeds all eleven with the §1.x "Init on run" defaults (modes default to the system default, both booleans `False`, FK/reason fields `None`, counters `0`, trace `[]`). | done | State shape only — no planner loop/validator/node logic. `planner_deliberation_trace` uses `operator.add` (append-only observability), **distinct** from `accumulate_or_reset` (which resets on an empty contribution) — documented inline. Approval-waiting is a safe boundary: `awaiting_planning_approval` + `planning_block_reason`, never reusing `pause_requested`/`hard_stop_asserted` (§1.1 / Module_Ability_Specification §5). Did not change existing non-planning fields, the `accumulate_or_reset` reducer, or the unknown-override-key guard. Mode fields stay overridable via the existing `**overrides` path; `config.yaml` is **not** read here (Build 14 snapshots the real `config.planning.*` at graph entry). Stale `STUB:` docstring marker corrected (module has been functional since 03.02). `beat_id` left with a default (not required) so only `fsm/state.py` + its test needed changing. Touched only `fsm/state.py`, `tests/test_state.py`. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_state.py -q` → **12 passed** (all eleven planning defaults present and correct; `planning_execution_mode`/`approval_mode` overridable while unknown override keys still raise; `operator.add` accumulates and is NOT reset by an empty contribution whereas `accumulate_or_reset` returns `[]`; `FSM_Pointer(...)` still constructs with default `beat_id=""` and carries identity when `beat_id="beat-1"` given). Full suite (isolated `%TEMP%` uv env) → **132 passed, 4 skipped, 2 deselected** (the 2 deselected are the pre-existing unrelated live-endpoint 404 tests); no regressions in other `FSM_Pointer`/state construction sites. Repo `.venv\lib64` still locked on this Windows host, so used the isolated `%TEMP%` uv env. |

| 07.F1 | Persistent Memory / Planning (M02) | Manual replay-safety fix for the build-07 planning persistence helpers. `create_planning_snapshot` now refreshes only stable identity fields on replay and preserves lifecycle fields; `transition_snapshot_status` preserves the first approval timestamp and clears it when leaving `approved`; `insert_planning_revision` advances `active_revision_id` only when the revision insert actually happened; `get_planning_nodes_by_parent` is snapshot-scoped; shared read/write paths use a per-process schema-init guard that reinitializes if the DB file is removed. | done | No DDL/config/runtime/planner-loop changes. Public interface change: `get_planning_nodes_by_parent(db_path, snapshot_id, parent_id)`. Existing planning output is unchanged except for preventing state rewind, cross-snapshot root mixing, stale approval timestamps, and repeated schema-init overhead. Touched only `memory/sqlite_db.py`, `tests/test_planning_store.py`, and this ledger. | `python -m py_compile memory\sqlite_db.py` passed. `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env uv run pytest tests/test_planning_store.py tests/test_config.py tests/test_state.py -q` passed: **33 passed in 4.13s**. Initial sandboxed uv run was blocked by user-level uv cache permissions, then passed with escalated cache access. |

| 07.01 | Hierarchical Planning Cascade (M05) | `fsm/planning_actions.py` — strict Pydantic v2 `PlannerAction` (the action schema every planner level's LLM returns per deliberation turn) + `planner_action_json_schema()` helper. `model_config = ConfigDict(extra="forbid")`; `action_type: Literal["call_tool","revise_plan","finalize_plan","raise_conflict"]`; payload fields verbatim from `Data_Structures.md` §1.4 (`tool_name`/`tool_args`; `revised_plan`/`change_summary`; `final_plan`/`self_check`; `conflicts`/`rationale`), all `\| None = None`. Docstring opens with the `Module: M05` tag. | done | **ID note:** distinct from the `07.00` persistence-prerequisite sub-rows (also labeled 07.01/07.02); this begins the M05 planning-library build sequence. Schema-only — **no** deliberation loop, tool registry/permission matrix, validators, or node logic (later M05 increments), and **no** `call_llm` wiring (M04 parses PlannerAction as a generic structured output per Module_Ability_Specification §4 when the planner nodes land). The four `action_type` values are exhaustive: `Literal` + `extra='forbid'` reject unknown action types and unknown fields (no custom validator added). **`self_check` is advisory-only** — documented in the module/class docstrings that the harness validators (`planner_required_checks`, M05 `fsm/planning_validators.py`) are the authoritative acceptance gate and `self_check` is never trusted for accepting a `finalize_plan`. Mirrors `fsm/state.py`'s `FailureObject`/`failure_object_json_schema` style. `fsm/__init__.py` is empty (no ownership header) so no ownership line was added; touched only `fsm/planning_actions.py` + this ledger row. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env PYTHONPATH=<repo> uv run python <check>` passed: `from fsm.planning_actions import PlannerAction; print(PlannerAction)` printed the class; one valid instance of each of the four `action_type` values constructed; an unknown `action_type` (`"explode"`) raised `ValidationError`; an unexpected extra field raised `ValidationError` (`extra='forbid'`); `planner_action_json_schema()` returned a schema with `properties`. (Exact `uv run` without the isolated `%TEMP%` env is still blocked by the documented Windows `.venv\lib64` permission error; used the same isolated-env workaround as 06.T1/06.T2/INT-B-D, plus `PYTHONPATH=<repo>` so the script outside the repo could import `fsm`.) |

| 07.02 (P1) | Hierarchical Planning Cascade (M05) | `fsm/planning_validators.py` — `ValidationResult` (Pydantic v2, `extra="forbid"`: `passes: bool`, `failed_checks: list[str]`, `details: dict`) + the eight deterministic, programmatic validators from `Context_Injection_Algorithms.md` Phase 1: `validate_schema`, `validate_required_fields`, `validate_thread_coverage`, `validate_user_annotations`, `validate_continuity_warnings`, `validate_depth`, `validate_ordering`, `validate_no_drafting`. Module-level `PLANNING_LEVELS`, `_PROSE_FIELD_NAMES`, and a presence-only `_DEPTH_CONTRACT` (chapter: `obligations` required, final `scenes` forbidden; scene: `entry_state`/`exit_state` required). | done | **ID note:** Prompt 1 of the M05 `07.02` validators increment; distinct from the `07.00` persistence-prerequisite `07.02` sub-row. Pure & side-effect-free: no LLM calls, no store reads, no network, never mutates the plan — harness-authoritative (`self_check` advisory; Module_Ability_Specification §5 — required checks cannot be skipped). Tunable expectations (`required_fields`, `required_threads`, `hard_annotations`, `continuity_facts`, depth markers) are **passed in by the caller** from the compiled constraint package / config — no numeric thresholds hardcoded; checks stay structural (presence/ordering/coverage). Does **not** import `planning_actions`. **Deferred to Prompt 2 (next up):** per-level required-check *selection* (`planner_required_checks[level]`) and the `run_validators` aggregator from `LangGraph_Nodes.md` Phase B — not built here per the prompt's Do-NOT. `fsm/__init__.py` empty (no ownership header) so untouched; touched only `fsm/planning_validators.py` + this ledger row. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env PYTHONPATH=<repo> uv run python <check>` passed: a well-formed scene plan passed `validate_schema`/`validate_required_fields`/`validate_ordering`/`validate_no_drafting`; a malformed plan failed each (missing `scene_id`; gapped ordering `[0,2]` → `not_monotonic_gapless`; a `prose` field → `validate_no_drafting` fail with `prose_fields=['prose']`). Spot-checks also confirmed `validate_depth` (scene requires entry+exit), `validate_thread_coverage` (covered vs uncovered), `validate_continuity_warnings` (no-contradiction vs same-(subject,attribute)-different-value contradiction), and `ValidationResult` rejecting an extra field (`extra="forbid"`). Same isolated-`%TEMP%`-uv-env + `PYTHONPATH=<repo>` workaround as 07.01 (documented Windows `.venv\lib64` blocker). |

| 07.02 (P2) | Hierarchical Planning Cascade (M05) | `fsm/planning_validators.py` (extended) — `REQUIRED_CHECK_REGISTRY` (config check-name → uniform `(plan, level, constraints, continuity) -> ValidationResult` callable) + `run_validators(level, plan, constraints, continuity, config)`. Generic-validator check-names route to the eight Prompt-1 validators (with aliases: `schema`, `required_fields`, `thread_coverage`, `user_annotations`/`annotation_satisfaction`, `continuity`/`continuity_warnings`, `depth`, `ordering`, `no_drafting`); the level-specific semantic names from `config.yaml` §3a are implemented as deterministic structural checks — `arc_coverage`, `major_promise_payoff`, `escalation`, `thread_distribution`, `chapter_function`, `pacing`, `scene_function`, `entry_exit_state`, `draftability`, `pad_grounding` (presence / coverage / ordering / declared-promise-has-payoff / beat-has-PAD-target+behavioural-string). | done | **Harness-authoritative & deterministic:** `run_validators` is the single source of truth for whether a finalize is allowed and **ignores any planner `self_check`**; pure, side-effect free, no LLM/store/network. Reads exactly `config.planning.planner_required_checks[level]` and runs **every** named check — `passes` only if all pass; `failed_checks` lists every failing config check-name; `details` keyed per check. **Fail-closed:** unknown level, missing `planning`/`planner_required_checks`, or a configured check-name with no registered implementation **raises `ValueError`** (resolved up front, before any check runs) — never a silent skip/default-pass. Semantic checks guess no numeric thresholds (presence/coverage/ordering only); tunables (`required_threads`, `hard_annotations`, `required_keys/fields`, continuity `facts`) are read from the passed-in `constraints`/`continuity`. No config/`PlanningConfig` edits (07.0a owns those); no loop/persistence. Touched only `fsm/planning_validators.py` + this ledger row. | `uv run python -c "from fsm.planning_validators import run_validators, ValidationResult; print(run_validators)"` printed the function. Synthetic-config done-check (per-level `planner_required_checks` mirroring §3a): each level's valid plan → `passes=True`; invalids listed the right `failed_checks` (global → `['arc_coverage','major_promise_payoff']`; scene contradiction+missing state → `['continuity','entry_exit_state']`; beat → `['draftability','pad_grounding']`); a `prose` field via a `no_drafting` required list → `passes=False ['no_drafting']`; an unknown configured check-name raised `ValueError` (no silent pass). Also smoke-tested against the **real** `config.yaml` via `load_config` (all six endpoint secrets injected): `planner_required_checks` loads as a dict, `beat` checks `['schema','draftability','continuity','pad_grounding']`, a valid beat plan → `passes=True`. Same isolated-`%TEMP%`-uv-env + `PYTHONPATH=<repo>` workaround (documented Windows `.venv\lib64` blocker). |

| 07.03 (P1) | Hierarchical Planning Cascade (M05) | `fsm/planning_tools.py` — `PERMISSION_MATRIX` (the §1.5 per-level grid as inspectable data: level → {tool_name: `full`/`limited`}), `permitted(level, tool_name)` + `access_for(level, tool_name)`, the `DISPATCH` table (16 tool names → callables), `_safe_store_read` graceful-degradation seam, and a permission-checked `call_tool(level, tool_name, args, *, db_path)`. Read tools route to `memory/sqlite_db.py` 04.03 helpers (`get_arc`/`get_chapters_for_arc`/`get_scenes_for_chapter_ordered`/`get_beats_for_scene_ordered`/`get_open_threads`/`get_latest_pad_for_character`/`get_latest_pad_for_scene`); write tools route to the 07.00 plan-time writers (`upsert_arc_plan`/`upsert_chapter_plan`/`upsert_scene_plan`/`upsert_beat_plan`); continuity/RAPTOR/Chroma query tools route through the seam over `GraphitiClient().query`/`RaptorStore().summarize`/`ChromaClient().query`. | done | **No shell/filesystem/network tools** — the registry's only side effects are the wrapped M02 reads/writes (Module_Ability_Specification §5 registry-scoping). `_safe_store_read` degrades **only** on `NotImplementedError` (the documented not-built signal) → `{available: False, data: empty, reason: "... not yet implemented"}`; any other exception propagates (a real bug is never masked). **Honesty:** three §1.5 categories have no M02 helper yet and degrade rather than fake one — `read_project_metadata`/`read_world_rules` (no project/world tables) and `write_global_plan` (07.00 added arc/chapter/scene/beat writers only; the prompt's write-routing list omits global). `call_tool` fails closed (`ValueError`) on a tool not permitted for the level or with no dispatch impl. **Deferred per the prompt:** per-loop cap counting (07.04, owned by the deliberation loop) and PlannerToolCallTrace logging (Prompt 2, next up) are **not** here. `fsm/__init__.py` empty → untouched; touched only `fsm/planning_tools.py` + this ledger row. | `UV_PROJECT_ENVIRONMENT=%TEMP%\museai-codex-uv-env PYTHONPATH=<repo> uv run python <check>` passed all assertions: `permitted()`/`access_for()` match §1.5 for global (reads arcs, not beats/chapters/scenes; `read_character_state`=limited; `write_global_plan` only; chroma denied) and beat (reads beats; no `write_global_plan`; `query_raptor_summaries`=limited) and scene (chroma=full); a permitted `read_arcs` over a temp SQLite fixture (seeded via `upsert_arc_plan`) returned the arc row (`available=True`, `id='a1'`); `read_open_threads` returned a list; `query_raptor_summaries`/`query_chroma_flavour`/`query_continuity_facts` each returned `{available: False, data: [], reason: "... not yet implemented"}` without raising; `call_tool('global','read_beats',...)` raised `ValueError` (fail closed). Same isolated-`%TEMP%`-uv-env + `PYTHONPATH=<repo>` workaround (documented Windows `.venv\lib64` blocker). |

| 07.03 (P2) | Hierarchical Planning Cascade (M05) | `fsm/planning_tools.py` (extended) — `PlanningToolRegistry(db_path, *, trace_summary_max_chars=…)` with `call(level, tool_name, args, snapshot_id, loop_index)`, `permitted`/`access_for` methods, `_summary`/`_record_trace` helpers, module `_truncate` + `_DEFAULT_TRACE_SUMMARY_MAX_CHARS`. `call` validates against the §1.5 matrix, executes the mapped tool (or surfaces its degradation marker), and writes a `PlannerToolCallTrace` (via the 07.00 `insert_planner_tool_call_trace`) for **every** attempt — returning a result dict with `outcome` (`executed`/`degraded`/`rejected`), `available`, `data`, `reason`, `trace_id`. | done | **Rejection (§1.5):** unknown tool (not in `DISPATCH`) and tool-not-permitted-for-level are both rejected **without executing** and still traced (`success=0`, `result_summary=None`, `error="rejected: …"`). **Trace safety:** arg/result summaries are secret-**redacted** (reuses `llm.call_llm._redact_secrets` — the single redaction convention; `api_key`→`[REDACTED]`) and length-**bounded** with an explicit elision marker (`…[+N chars truncated]`) — never raw/unbounded; verified `sk-LEAK-123` never persists. **Cap source:** no dedicated config key exists for the summary cap and this increment may touch only `planning_tools.py`, so the cap is an **injectable constructor param** with a clearly-labelled provisional default (`_DEFAULT_TRACE_SUMMARY_MAX_CHARS=1000`); a real config key (e.g. `planning.tool_trace_summary_max_chars`) should be added in a config increment and passed by the loop. **Per-loop cap enforcement is intentionally deferred to the loop (07.04)** — `call` surfaces `outcome` + the trace so the loop counts/caps; not enforced here. RAPTOR/Chroma/Graphiti query tools still **degrade to empty `unavailable`** until those M02 stores are built. Side-effects bounded to the wrapped M02 read/write + trace insert; **no shell/fs/network** (asserted by source scan). A real execution fault is traced then re-raised (never masked as "unavailable"). Touched only `fsm/planning_tools.py` + this ledger row. | `uv run python -c "from fsm.planning_tools import PlanningToolRegistry; print(PlanningToolRegistry)"` printed the class. Temp-SQLite done-check (snapshot `snap1` for the trace FK, seeded arc): permitted `read_arcs` → `executed`, returned the arc row, trace written; disallowed `read_beats`@global → `rejected` (not executed, traced `success=0`/`result_summary=None`); unknown `no_such_tool` → `rejected`; `query_raptor_summaries`/`query_chroma_flavour` → `degraded` `available=False` + traced; an arg bundle with `api_key='sk-LEAK-123'` + a 5000-char blob persisted with `api_key`→`[REDACTED]`, blob truncated with the elision marker, and `sk-LEAK-123` **absent**; exactly **6** traces persisted (1 exec + 2 reject + 2 degrade + 1 oversize); source scan found no `subprocess`/`os.system`/`socket`/`requests`/`urllib`/`open(`. Same isolated-`%TEMP%`-uv-env + `PYTHONPATH=<repo>` workaround (documented Windows `.venv\lib64` blocker). |

| 07.04 | Hierarchical Planning Cascade (M05) | `fsm/planning_loop.py` — the complete bounded, harness-owned planner deliberation loop `run_planner_loop(*, level, snapshot_id, target_node, constraints, continuity, registry, config, planner_decider, deterministic_baseline=None)` plus the injected-seam contracts (`DeliberationState`, `PlannerDecider`, `DeterministicBaseline`) and a structured `LoopOutcome` (`outcome` ∈ `finalized`/`needs_clarification`/`fallback_baseline`/`escalate`; `plan`, `validation`, `best_valid_plan`, `baseline_source`, `conflicts`, `records`, `loops_used`, `tool_call_count`, `wasted_turns`). One `PlannerAction` per turn; dispatch on `action_type`: `call_tool` (intra-loop de-dup → permission/cap check → `registry.call(...)`, else refused), `revise_plan` (set plan, `run_validators`, remember best-valid, early-finalize on no-op-over-valid), `finalize_plan` (accept **only** if `run_validators` passes — `self_check` ignored), `raise_conflict` (immediate `needs_clarification`). **Fallback ladder** on cap exhaustion (Context_Injection_Algorithms.md Phase 1 order): best validated candidate → injected `deterministic_baseline(level, target_node, constraints)` (then validated) → `escalate` with the last `ValidationResult`. Bounded by `planner_max_deliberation_loops[level]` (tracked `planner_loop_index`); executed tool calls bounded by `planner_max_tool_calls_per_loop[level]` (single running counter). | done | **ID note:** the full M05 `07.04` deliberation-loop increment (built across two prompts: skeleton, then fallback ladder + cost-hygiene); distinct from the `07.00` persistence-prerequisite rows. **Pure & harness-authoritative:** returns a `LoopOutcome`, **persists nothing**, triggers no escalation side effects, and **never calls `call_llm`/network** — the planner node (a later sub-batch) maps outcome→snapshot/revision writes + routing. Acceptance depends solely on `run_validators`; `self_check` is never read. The injected `planner_decider` / `deterministic_baseline` seams keep it testable without a live model. **Never-persist-invalid invariant:** every non-`escalate`/non-`needs_clarification` return carries a plan that passed `run_validators` (best-valid stores its passing `ValidationResult`; deterministic baseline is validated before return; an invalid baseline → `escalate`); `raise_conflict` → `needs_clarification` is never overridden by the ladder. **Cost-hygiene guards (Open_Problems.md deliberation cost center):** (a) *bounded accumulated context* — only a most-recent window of tool results is threaded back to the decider, size from `config.planning.planner_max_accumulated_tool_results` via `getattr` (provisional default 20, `max_accum<=0`→empty; a real `planner_max_*` config key should be added in a config increment since this increment may touch only `planning_loop.py`); (b) *early-finalize* on a no-op/no-improvement revision over an already-valid plan; (c) *intra-loop tool de-dup* — identical `(tool_name, args)` reuses the cached result (incl. an `unavailable` marker) without consuming the tool budget; (d) *wasted-turn accounting* — decider error (caught, loop survives), refused over-cap/unpermitted call, and no-op invalid revision are counted in `wasted_turns`. Cap reading fail-closed (`ValueError` on missing `planning`/unconfigured `level`). **Public rename from the skeleton prompt:** `run_deliberation_loop`→`run_planner_loop`, `DeliberationOutcome`→`LoopOutcome`, `status`→`outcome` (no callers yet — planner nodes unbuilt). Toggles `planner_deliberation_enabled`/`tool_calling_enabled`/`intensity` remain absent from `PlanningConfig` (`extra="forbid"`) so are not read. `fsm/__init__.py` empty → untouched; touched only `fsm/planning_loop.py` + this ledger row. | `python -m py_compile fsm/planning_loop.py` clean. Exact done-check `from fsm.planning_loop import run_planner_loop, LoopOutcome; print(LoopOutcome)` → `<class 'fsm.planning_loop.LoopOutcome'>`. Synthetic-decider done-check (system `python`, `PYTHONPATH=<repo>`; `SimpleNamespace` config + fake registry): (a) valid finalize → `finalized`, `validation.passes`; (b) always-invalid within cap → best-valid candidate, else injected baseline, else `escalate`, and **never** returns an invalid plan as final (empty-dict baseline → `escalate`); (c) `raise_conflict` → `needs_clarification`; (d) caps bound `loops_used` (==3 at cap 3); (e) no-op revision over an already-valid plan early-finalizes (`loops_used=2` with budget 5, `early_finalize` record present); (f) repeated identical `call_tool` reuses prior result (`registry.calls==1`, `tool_call_count==1`, 2 `reused` records); bonus: accumulated window capped at `accum=2`; a raising decider → 3 `wasted_turns` and `escalate` (loop survives). Repo `.venv\lib64` still locked on this Windows host, so ran under system `python`; rerun under WSL2/`uv` to exercise the same code. |

| 07.05 (P2) | Hierarchical Planning Cascade (M05) | `fsm/planning_annotations.py` (extended) — hard-vs-hard **conflict detection** in `compile_planning_constraints` (new `_detect_hard_conflicts` + `_MUTUALLY_EXCLUSIVE_NOTE_TYPES`) and a new `compute_revision_diff(previous_nodes, new_nodes, annotation_outcomes=None)` (+ helpers `_node_summary`, `_normalize_annotation_outcomes`, field sets `_NODE_CONTENT_FIELDS`/`_NODE_POSITION_FIELDS`). Detection is deterministic & schema-grounded: mutually-exclusive §2.7 `note_type` directives on the **same** planning node (`pin`↔`remove`, `pin`↔`move`, `remove`↔`move`) or a hard `remove`/`move` aimed at a `locked_pinned` node. On detection the compiler **marks** the involved hard annotations `status='needs_clarification'` (via the 07.00 `update_annotation_status`, `planner_response`=reason), **excludes** both from the emitted package, and adds a blocking signal to the result: `needs_clarification: bool`, `block_reason: "unresolved_hard_conflict"|None`, `hard_conflicts: [{source_node_id, annotation_ids, note_types, reason, newly_marked}]`, `unresolved_conflict_annotation_ids`. The compile loop was restructured to compute scope-applicability first then partition by status, so a **pre-existing** applicable `needs_clarification` annotation keeps the block across re-runs (no re-detection needed). `compute_revision_diff` returns serializable `diff_json`: `added_nodes`/`removed_nodes` (compact summaries), `modified_nodes` (content `changed_fields`), `moved_nodes` (`parent_id`/`ordering` `position_change`), `unchanged_node_count`, normalized `annotation_outcomes`. | done | **ID note:** Prompt 2 of the M05 `07.05` annotation-compiler increment (Prompt 1 = collect/inherit/precedence/emit, same row series). **Detect/mark/signal only:** the graph edge-block in approval mode (`planning_block_reason='unresolved_hard_conflict'`, Module_Ability_Specification §5 / A13) is **M01/Build 14** and the surfacing UI is **M17/Build 19** — not wired here. **Never feeds a contradictory hard pair to the planner** (both excluded). **One store mutation only** — annotation `status`→`needs_clarification` via the 07.00 helper; no canonical-narrative/prose writes, no LLM, deterministic. **History preserved:** marking never deletes (idempotent update in place); `compute_revision_diff` never mutates inputs and lists removed nodes explicitly (the actual `PlanningRevision` insert + `active_revision_id` advance stays the planner node's job via 07.00, never a silent overwrite — §2.7). Semantic/textual contradiction (the Marcus-style continuity example) is **out of scope** — it needs structured directives or a model check, documented honestly in the module docstring rather than faked. Behavior change vs P1: a pre-existing applicable `needs_clarification` annotation now raises the block instead of being silently skipped (meta count renamed `skipped_needs_clarification`→`unresolved_needs_clarification`). `fsm/__init__.py` empty → untouched; touched only `fsm/planning_annotations.py` + this ledger row. | `python -m py_compile fsm/planning_annotations.py` clean. Exact done-check `from fsm.planning_annotations import compile_planning_constraints, compute_revision_diff; print(compile_planning_constraints)` prints the function. Done-check (system `python`, `PYTHONPATH=<repo>`; real temp SQLite via `init_db` + 07.00 write helpers): two contradictory hard annotations on chapter `c5` (`pin` `H_PIN` + `remove` `H_REMOVE`, subtree scope) targeting scene `s` → `needs_clarification=True`, `block_reason='unresolved_hard_conflict'`, conflict group lists both ids, **neither** emitted in `hard_annotations`, both persisted as `status='needs_clarification'` with a `planner_response`; a non-conflicting global hard rule `H_OK` **is** emitted; re-run still blocks via the pre-existing unresolved pair (`unresolved_needs_clarification=2`, `hard_conflicts=0`, history preserved); a separate non-conflicting snapshot compiles cleanly (`needs_clarification=False`, hard `['K1']`/soft `['K2']`); `compute_revision_diff` over synthetic node sets returned the expected added(`n4`)/removed(`n3`)/modified(`n1.summary`)/moved(`n2.ordering`) + sorted `annotation_outcomes`, `json.dumps`-serializable, inputs unmutated. Repo `.venv\lib64` still locked on this Windows host, so ran under system `python`; rerun under WSL2/`uv` to exercise the same code. |
| 07.05 (P1) | Hierarchical Planning Cascade (M05) | `fsm/planning_annotations.py` — `compile_planning_constraints(snapshot_id, target_node, *, db_path)` (the annotation compiler) plus helpers `_ancestor_ids`, `_applies_to_target`, `_entry`. Reads a snapshot's PlanningNodes + PlanningAnnotations via the 07.00 helpers (`get_planning_nodes`, `get_annotations_for_snapshot`, `get_planning_snapshot`), then **collect** (annotations attached to the target node), **inherit** (ancestor/global-scope annotations honoring §2.7 `scope`: `global`→all, `subtree`→node+descendants, `children`→direct children, `sibling_sequence`→same-parent siblings, `this_node`→only that node), **precedence** (`priority=='hard'`→`hard_annotations` bucket ranked above `soft_preferences`; soft sorted high→normal→low then created_at; constraints from `locked_pinned` nodes flagged `from_pinned_node` and collected into `pinned_annotations` as protected), and **emit** a serializable package `{snapshot_id, target_node_id, target_level, target_locked_pinned, hard_annotations, soft_preferences, pinned_annotations, meta{counts, snapshot_status, ancestor_node_ids, conflict_detection:"deferred"}}`. `target_node` accepts a PlanningNode dict or a `node_id` string. | done | **Pure read:** no store mutation, no LLM, no canonical-narrative writes; package is JSON-serializable and carries only structured annotation fields (no conversation text). The hard bucket is named **`hard_annotations`** so `fsm/planning_validators.run_validators` (`validate_user_annotations`) consumes it directly; hard ranks above soft by living in its own bucket. Active-status filter = `{pending, applied, partially_applied}`; `rejected`/`superseded` skipped, and `needs_clarification` annotations are deliberately **omitted** (never sent to the planner per Phase 1 step 4). **Did not invent** `required_threads`/`required_fields`/`required_keys` (those are merged in at the planner node from config/open-threads, not annotation-derived) and used only the §2.7 CHECK vocabularies for `scope`/`priority`/`status`. **Deferred to Prompt 2 per the prompt's Do-NOT:** hard-vs-hard contradiction detection (marking annotations `needs_clarification`, never emitting a contradictory hard pair) — `meta.conflict_detection="deferred"` marks the seam. `fsm/__init__.py` empty (no ownership header) → untouched; touched only `fsm/planning_annotations.py` + this ledger row. | `python -m py_compile fsm/planning_annotations.py` clean. Synthetic micro-check (system `python`, `PYTHONPATH=<repo>`; real temp SQLite via `init_db` + 07.00 write helpers): node tree global `g`→arc `a`→chapter `c` (locked_pinned)→scene `s` (target), with sibling chapter `c2`. Annotations: A1 (g, global, hard), A2 (a, subtree, normal), A3 (c, subtree, high), A4 (c2, this_node, normal), A5 (c, children, normal), A6 (g, global, hard, `rejected`), A7 (g, global, hard, `needs_clarification`). Result: `hard_annotations=['A1']` (inherited, `origin='inherited'`); `soft_preferences=['A3','A2','A5']` (high first); sibling note **A4 absent**; **A6/A7 excluded** (`skipped_inactive=1`, `skipped_needs_clarification=1`); A3/A5 flagged `from_pinned_node` and in `pinned_annotations`; `target_locked_pinned=False`, `target_level='scene'`; package `json.dumps`-serializable; passing a `node_id` string yielded the same buckets. Repo `.venv\lib64` still locked on this Windows host, so ran under system `python`; rerun under WSL2/`uv` to exercise the same code. |

| 07.06 (data) | Hierarchical Planning Cascade (M05) | `prompts/pad_regions.json` — static, documented PAD-region behavioural-floor lookup. Keys are the eight PAD octants by axis sign (`P{+\|-}A{+\|-}D{+\|-}`) plus `neutral`; each region carries a compact `behaviour` descriptor, a few concrete `physical_cues`, and a `register` tone note. Top-level `schema_version` and a self-describing `_doc` explain the axis-sign→region mapping (P± pleasant/unpleasant, A± activated/calm, D± in-control/overwhelmed). | done | **ID note:** the static-data prerequisite for the `07.06` planner-node increment, **not** the node wiring itself (that remains Next up). This is step (2)'s lookup in `node_plan_beat`'s PAD Grounded Translation Pipeline (`LangGraph_Nodes.md` §4): a small-tier LLM adapts the matched string to scene intent in step (3), and falls back to it verbatim on failure. **Descriptive baseline data only** — no model/provider names, thresholds, EWMA weights, or endpoint routing (those live in `config.yaml`); no prose, character names, or story-specific content; no Python. Dominance axis disambiguates same-(P,A) states (e.g. `P-A+D+` anger/forceful vs `P-A+D-` fear/overwhelmed), per Module_Ability_Specification §5 cross-combo disambiguation. Valid JSON (no comments/trailing commas). Touched only `prompts/pad_regions.json` + this ledger row. | `python -c "import json,pathlib; d=json.loads(pathlib.Path('prompts/pad_regions.json').read_text()); ..."` → `ok` with all eight octants + `neutral` present, each region carrying `behaviour`/`physical_cues`/`register`, and `schema_version`/`_doc` set. Ran under system `python` (the documented Windows `.venv\lib64` blocker still applies to `uv run`); the file is pure JSON data so the parse is interpreter-independent — rerun under WSL2/`uv` exercises the identical lookup. |

| 07.06 | Hierarchical Planning Cascade (M05) | `fsm/pad_translation.py` (new) + `prompts/pad_regions.json` (additive `neutral_band` constant). **`smooth_pad_target(prior, proposed, alpha)`** — pure per-axis EWMA (`alpha*proposed + (1-alpha)*prior`) returning a `PADTarget(pleasure,arousal,dominance)` clamped to [-1,1]; `alpha` is a caller-supplied param (read from `config.thresholds.pad_ewma_alpha`), never read/defaulted here. **`load_pad_regions()`** caches the JSON; **`region_key_for_pad()`** maps a coordinate to a region by axis sign, using a neutral dead-zone whose half-width (`neutral_band`) is read from the data file (no hardcoded band in logic). **`translate_pad_to_behaviour(pad_target, context, *, adapt_fn=None)`** (async) runs the grounded ladder: static `compose_baseline_string()` floor -> optional awaited `adapt_fn` -> static fallback; returns a frozen `BehaviourTranslation(text, rung, region_key)` with rung `static`/`adapted`/`fallback`. **`build_pad_adapt_fn(config, *, endpoint_role="pad_translator", template_node="node_plan_beat_pad", call=None, loader=None)`** builds a default adapter that wraps `call_llm` on the role-resolved small-tier endpoint and renders its prompt via the M04 `PromptLoader` (no prompt prose in Python). | done | **EWMA alpha reuses the existing `thresholds.pad_ewma_alpha`** — no `planning.pad_ewma_alpha` or any duplicate key added (Do-NOT honored). **Static + fallback rungs are LLM-free** (pure `pad_regions.json` lookup; no network/store); only the **injected `adapt_fn` seam** may reach a model, and the pipeline is fully usable static-only (`adapt_fn=None`). On *any* adapter error — including a missing `node_plan_beat_pad.xml.j2` template (still deferred M04 content) or a blank/non-str result — the ladder degrades to the static floor and marks `rung='fallback'`; `node_plan_beat` (a later increment) owns the WARNING log, the permit decision, and persistence. **Scope reconciliation:** step 3 required the neutral-band width to live "in the data file — not in logic", so a documented top-level `neutral_band` (provisional `0.15`) + `_neutral_band_doc` were added to `prompts/pad_regions.json` (both files are in the prompt's touch list); the reader raises if it is absent rather than defaulting. No beat-planning/loop/persistence logic here; `call_llm`/`PromptLoader` imports are lazy so the static path pulls no HTTP stack. `fsm/__init__.py` empty -> untouched. **No `tests/test_pad_translation.py` added** (outside the "Touch only" list; done-check optional). Touched only `fsm/pad_translation.py`, `prompts/pad_regions.json`, + this ledger row. | Exact `uv run python -c "from fsm.pad_translation import smooth_pad_target, translate_pad_to_behaviour; print('ok')"` blocked by the documented Windows `.venv\lib64` permission error (`failed to remove file ... lib64: Access is denied`). `python -m py_compile fsm/pad_translation.py` clean; the same import printed `ok` under system `python`. Behavioural check (system `python`, no network): `smooth_pad_target` deterministic and alpha-sensitive (`alpha=0`->prior, `alpha=1`->proposed, `0.35`->`pleasure 0.35`, clamps to 1.0, mapping/sequence coercion agree, `alpha=1.5`->`ValueError`); `region_key_for_pad` resolves near-origin to `neutral` and `(-0.8,0.7,0.6)`->`P-A+D+`; `translate_pad_to_behaviour(adapt_fn=None)` returns the composed static baseline with `rung='static'`; a raising adapter and a blank-result adapter both fall back to the identical static string with `rung='fallback'` and no error; a successful adapter yields `rung='adapted'` with the tailored string. Rerun under WSL2/`uv` exercises the identical code. |

| 07.07 (P1) | Hierarchical Planning Cascade (M05) | `fsm/planning_node_support.py` (new) — `make_planner_decider(node_name, base_context, config, *, endpoint_role="planner", loader=None, call_structured=None, schema_model=PlannerAction)` returns an `async decider(loop_state)`. Each turn the decider: (1) merges `base_context` with the running `DeliberationState` (`current_plan`, `best_valid_plan`, `tool_results`, compiled `constraints`→`compiled_constraints`, `continuity`, `last_validation`, `last_refusal`, `level`, `loop_index`) into one render context — dynamic loop fields override static base keys on collision; (2) renders `node_name` via the M04 `PromptLoader` (`StrictUndefined`); (3) calls `call_llm_structured` on the role-resolved endpoint constrained to the `PlannerAction` schema and returns the parsed action. New `PlannerDeciderError(RuntimeError)` + `_state_field` helper (tolerates dataclass or mapping loop state). | done | **ID note:** Prompt 1 of the M05 `07.07` planner-node increment; distinct from the `07.00` persistence-prerequisite rows. **Decider proposes only** — performs **no** validation, tool execution, or persistence (the loop owns those, `fsm/planning_loop.py` + `planning_validators.py` + `planning_tools.py`); `self_check` stays advisory (acceptance is the harness validators' job). **Level-agnostic:** every per-level difference is carried by `node_name`, `base_context`, and the resolved endpoint — the same factory builds global/arc/chapter/scene/beat deciders. **No hardcoded endpoint/provider/port/model:** endpoint resolved by role from `config.endpoints.<endpoint_role>` and `validate_retry_cap` from `config.runtime.model_validate_retry_cap`, both resolved eagerly at factory build so a bad `endpoint_role` fails fast (not mid-loop). On an unsalvageable structured-output failure (`LLMCallError` from M04 after retries+salvage) the decider raises `PlannerDeciderError` — recoverable: the loop already treats a raising decider as a non-finalizing/wasted turn so its caps + fallback ladder apply; the node is never crashed. `loader`/`call_structured`/`schema_model` are injectable test seams defaulting to the real M04 components. **Integration mismatch flagged (not fixed here, out of scope):** this decider is `async`, but the loop's `PlannerDecider` seam is typed/called **synchronously** (`fsm/planning_loop.py` `action = planner_decider(state)`); wiring the async decider into the loop (making the loop `await`) is **07.07 (P2)**. **Outcome→persistence/routing mapping deferred to P2** per the prompt. The real `node_plan_<level>.xml.j2` templates remain deferred M04 content (render raises until authored → recoverable). `fsm/__init__.py` empty (no ownership header) → untouched; touched only `fsm/planning_node_support.py` + this ledger row. | Exact `uv run python -c "from fsm.planning_node_support import make_planner_decider, PlannerDeciderError; print('ok')"` blocked by the documented Windows `.venv\lib64` permission error (`Access is denied`). `python -m py_compile fsm/planning_node_support.py` clean; the same import printed `ok` under system `python`. Micro-check (system `python`, stub loader + stub `call_llm_structured`, synthetic `SimpleNamespace` config + `DeliberationState`-like state): happy path rendered for `node_name='node_plan_chapter'`, merged `base_context` (`arc_summary`) with dynamic loop fields (`level='chapter'`, `loop_index=1`, `tool_results`, `compiled_constraints` from `constraints`), routed the config endpoint + `validate_retry_cap==3`, and returned the canned `finalize_plan` `PlannerAction`; a stub raising `LLMCallError` surfaced as `PlannerDeciderError` ("could not obtain a valid …") without crashing; the same factory built a `node_plan_beat` decider via `node_name` alone (level-agnostic). Rerun under WSL2/`uv` exercises the identical code. |

| 07.07 (P2) | Hierarchical Planning Cascade (M05) | `fsm/planning_node_support.py` (extended) — `persist_loop_outcome(outcome, *, level, snapshot_id, target_node, persist_plan_fn, db_path, event_log_path, prior_nodes=None, annotation_outcomes=None, revision_id=None)` + frozen `PersistResult` + helpers `_target_node_id`/`_derive_revision_id`/`_active_revision_id` and module constants `_PERSIST_OUTCOMES`/`_CLARIFICATION_SNAPSHOT_STATUS`/`_CLARIFICATION_BLOCK_REASON`. Maps `LoopOutcome.outcome`: **`finalized`/`fallback_baseline`** → assert a validator-passed plan then, in dependency order, (1) `persist_plan_fn(plan)` (node's level-specific narrative writes), (2) `upsert_planning_node` for each `plan["planning_nodes"]`, (3) `insert_planning_revision` with `compute_revision_diff(prior_nodes, get_planning_nodes(...), annotation_outcomes)` (advances `active_revision_id`), (4) `write_event` mirror; **`needs_clarification`** → no plan, `transition_snapshot_status(...,'revision_requested')`, return `unresolved_hard_conflict` block + conflicts; **`escalate`** → nothing written, return the last `ValidationResult`. Returns a `PersistResult` the node folds into state (`planning_snapshot_id`, `active_planning_revision_id`, `planning_block_reason`, conflicts/validation). | done | **ID note:** Prompt 2 of the M05 `07.07` planner-node increment (P1 = decider factory, same file). **Centralizes the never-persist-invalid invariant:** persist branches raise `ValueError` unless `plan` is present **and** `validation.passes` — an unvalidated or `plan=None` finalize persists nothing (proven). **History-preserving:** a new `PlanningRevision` is inserted and `active_revision_id` advances — never a silent overwrite (07.00 `insert_planning_revision` is `ON CONFLICT DO NOTHING` + advance); `needs_clarification`/`escalate` write **no** plan and **no** revision. **Replay-safe/crash-ordered:** every store write is idempotent (node upsert by `node_id`, revision idempotent by a content-derived `revision_id` from the diff) and the append-only event-log mirror is written last as the recoverable record, so a crash mid-sequence leaves no partial *divergent* canonical truth — a replay converges (verified: a second identical finalize keeps exactly 1 revision with the same id). **Conflict annotations are NOT double-written** — the compiler (07.05) already marks them `needs_clarification`; this only moves the *snapshot* to `revision_requested` (no literal `needs_clarification` snapshot status exists in the §2.7 enum; that state is per-annotation) and surfaces the `unresolved_hard_conflict` block reason. **Writes validated planning structure + the proposal surface only** — never prose, never promotes a proposal node into committed narrative. **No LangGraph route / approval-gate edge wired here — that is Build 14** (Do-NOT honored). **Wiring notes (added params, flagged):** `db_path`/`event_log_path` are explicit keyword params (the prompt's core signature omits them) so temp fixtures and the node supply paths — no hardcoded store/log path; optional `revision_id` lets the node override the content-derived default. **Still pending from 07.07 (carried to Next up):** the async decider is not yet awaited by the loop (`planning_loop.py` calls `planner_decider(state)` synchronously), and the real `node_plan_<level>` LangGraph node/route is unbuilt. `fsm/__init__.py` empty → untouched; touched only `fsm/planning_node_support.py` + this ledger row. | Exact `uv run python -c "from fsm.planning_node_support import make_planner_decider, persist_loop_outcome; print('ok')"` blocked by the documented Windows `.venv\lib64` permission error (`Access is denied`). `python -m py_compile fsm/planning_node_support.py` clean; the same import printed `ok` under system `python`. Done-check (system `python`, temp `init_db` SQLite + `.jsonl` event log + synthetic `persist_plan_fn`, `create_planning_snapshot('snap1')`): **(1)** a `finalized` outcome called `persist_plan_fn` once, upserted 2 `PlanningNode`s, wrote exactly 1 `PlanningRevision`, advanced `active_revision_id` to the new `rev_…` id, and appended exactly 1 `planning_commit` event referencing that revision; a replay of the identical finalize kept **1** revision with the **same** id (idempotent). **(2)** `needs_clarification` wrote no plan (no `persist_plan_fn` call), set snapshot `status='revision_requested'`, returned `planning_block_reason='unresolved_hard_conflict'` + conflicts, and added no revision. **(3)** `escalate` wrote nothing and returned the last `ValidationResult` (`failed_checks=['depth']`). **(4)** a `finalized` outcome with `validation.passes=False` raised `ValueError` ("validator-passed") and persisted nothing; a `plan=None` finalize also raised. Rerun under WSL2/`uv` exercises the identical code. |

| 07.08 (P1) | Hierarchical Planning Cascade (M05) | `prompts/node_plan_global.xml.j2` (new) — the strict Jinja2 XML prompt template for Level-1 (global) planning, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`) and re-rendered every deliberation turn. XML sections: `role`, `planning_objective`, `context`, `compiled_constraints`, `deliberation_state`, `output_contract`. Instructs the model to return **exactly one `PlannerAction`** (`call_tool`/`revise_plan`/`finalize_plan`/`raise_conflict`, §1.4) and describes the required global-plan content per `node_plan_global` + the Phase B **Global** required checks: premise/logline, central conflict, ending target, major promises paired with intended payoffs, and the ordered arc set covering premise→ending with density-weighted `word_allocation` toward `target_word_count`. Render slots serve every turn: compiled constraints (`hard_annotations`/`soft_preferences`), accumulated `tool_results`, `current_plan`, `last_validation`, `last_refusal`, `loop_index`. A top `{# #}` comment documents the 11 expected variables (base-context group supplied by the node; loop-state group supplied by the decider). | done | **ID note:** Prompt 1 of the M05 `07.08` global-planner increment (P2 = the `node_plan_global` node logic). **All creative/prompt prose lives in the template — none in Python** (Architecture_Map §4 split 2); no provider/model names, no tuning constants. **Model proposes; harness disposes** is stated explicitly: the output contract marks `self_check` advisory-only and says the harness re-runs the required checks and decides acceptance. **Tight `StrictUndefined` contract:** every documented top-level variable is required (omitting any raises `UndefinedError`), while nested dict access uses `.get('key', [])` and `{% if %}` guards so the decider's defaults (e.g. `compiled_constraints={}`, `current_plan=None`, `last_validation=None`) render safely — the JSON shape examples are wrapped in `{% raw %}` so no literal brace is parsed as Jinja. Variables referenced are exactly those the 07.07 decider passes each turn plus the base-context the (unbuilt) `node_plan_global` node will supply; **the node logic is NOT written here** (P2). No tool-name list is hardcoded as authoritative — `call_tool` is described generically and the `PlanningToolRegistry` permission matrix remains the gate. Touched only `prompts/node_plan_global.xml.j2` + this ledger row. | Exact `uv run python -c` render blocked by the documented Windows `.venv\lib64` permission error (`Access is denied`). Under system `python`: a full synthetic context (all 11 vars) rendered a 5234-char prompt with no leaked Jinja (`{%`/`{{` absent), surfacing genre/target/premise/world rules, the hard requirement + preference, the empty-`existing_arcs` "fresh book" branch, all four `action_type`s, and the `major_promises`/`intended_payoff`/`word_allocation` plan shape. A second-turn context (populated `current_plan`, a `tool_results` entry, a failed `last_validation` → "arc_coverage, major_promise_payoff", a `last_refusal`, non-empty `existing_arcs`) rendered the failed-checks list, the tool result, the "do not repeat it" refusal, and the "Continuation pass" branch. Omitting any of `premise_seed`/`compiled_constraints`/`current_plan`/`last_validation`/`world_rules`/`loop_index` raised `UndefinedError` (tight contract). Rerun under WSL2/`uv` exercises the identical render. |

| 07.08 (P2) | Hierarchical Planning Cascade (M05) | `fsm/nodes/node_plan_global.py` — replaced the `NotImplementedError` stub with the real Level-1 planner. `async node_plan_global(state, *, decider=None, registry=None) -> state`: (1) resolve/create the run `PlanningSnapshot` (`snap_{project_id}`, idempotent `create_planning_snapshot`) + an idempotent placeholder global `PlanningNode` (`{snapshot}:global`) so the compiler can resolve the target; (2) `compile_planning_constraints` — on a hard-vs-hard `needs_clarification` signal, set `planning_block_reason` + record a trace and **return without running the loop**; (3) assemble global `base_context` (genre/target_word_count/premise_seed from `project_metadata`, `world_rules`, `existing_arcs` — degrade to safe defaults for not-yet-built stores); (4) build the M04-backed decider via `make_planner_decider("node_plan_global", base_context, config)` and `run_planner_loop(level="global", …, planner_decider=decider, deterministic_baseline=_build_global_baseline(...))`; (5) `persist_loop_outcome(..., persist_plan_fn=_global_persist_plan_fn(...))`, folding `planning_snapshot_id`/`active_planning_revision_id`/`planning_block_reason` into state and extending `planner_deliberation_trace` with the loop records. Helpers: `_build_global_baseline` (validator-shaped 3-act floor), `_global_persist_plan_fn` (writes the global plan into the global PlanningNode), `_sync_decider_from_async` (thread bridge), `_resolve_config`. | done | **ID note:** Prompt 2 of the M05 `07.08` global-planner increment (P1 = the `.xml.j2` template). **Model contact only through the injected decider seam — the node makes NO direct `call_llm`** (verified: no `llm` import, no `call_llm(`/`call_llm_structured(` invocation; the sole textual mention is the docstring stating it makes none), so it is fully testable with a synthetic decider. **Never persists an invalid plan / never bypasses validators:** acceptance is entirely `run_planner_loop` + `persist_loop_outcome` (which asserts `validation.passes`); a finalize that fails `arc_coverage`/`major_promise_payoff` falls to the deterministic baseline (validator-shaped: `arcs[].arc_id` + `promises[].payoff`), and an `escalate` writes no plan/revision and sets `planning_block_reason='planning_escalation'` (provisional reason; the real recovery route is Build 14). No prose written; no graph route/approval edge wired (Build 14). **Honest design note:** there is no narrative "Story" table (narrative tables start at `Arcs`, written later by `node_plan_arc`), so the validated global plan is persisted as the global `PlanningNode` (proposal surface) with full plan JSON in `purpose` — matching the 07.00 convention; arcs are realized into `Arc` rows by `node_plan_arc`. **Two carryovers flagged (out of this file's scope, recorded in Next up):** (a) `run_planner_loop` calls `planner_decider(state)` **synchronously** but `make_planner_decider` returns an **async** decider — bridged here by a self-contained worker-thread shim (`_sync_decider_from_async`) so production works today; remove the shim once the loop is made async (07.07 carryover). (b) **Template↔validator key mismatch:** the P1 template describes the plan as `major_promises`/`intended_payoff` and arcs with `title` (no id), but the authoritative validators key on `promises`/`payoff` and `arcs[].arc_id` — a template-faithful LLM plan fails validation and falls to the baseline until the template (P1) or validators (07.02) are reconciled; correctness is preserved (baseline guarantees a valid plan), only LLM-path quality is affected. Touched only `fsm/nodes/node_plan_global.py` + this ledger row. **No `tests/test_node_plan_global.py` added** (outside the "Touch only" file; done-check optional). | Exact `uv run python -c "import asyncio; from fsm.nodes.node_plan_global import node_plan_global; print(node_plan_global)"` blocked by the documented Windows `.venv\lib64` permission error. `python -m py_compile` clean; the exact import printed the function under system `python`. Done-check (system `python`, temp `init_db` SQLite + `.jsonl` event log, synthetic typed `SimpleNamespace` config, injected sync deciders): **(1)** a finalize-valid decider → global `PlanningNode` written with `status='planned'` and the plan JSON in `purpose`, exactly 1 `PlanningRevision`, snapshot `active_revision_id` advanced and folded into `state['active_planning_revision_id']`, exactly 1 `planning_commit` event at `level='global'`, `planning_block_reason` None, loop records appended to the trace. **(2)** an always-invalid decider (empty arcs/promises) → loop fell back to the deterministic baseline and persisted a **valid** plan (every arc has `arc_id`, the promise has a `payoff`), never the invalid one. **(3)** an `escalate` outcome (stubbed loop) → no revision written, `planning_block_reason='planning_escalation'`. **(4)** no `call_llm` import/invocation in the node source. Rerun under WSL2/`uv` exercises the identical code. |

| 07.09 (A) | Hierarchical Planning Cascade (M05) | **Phase A of the global-planner overhaul (plan `clever-noodling-deer.md`) — made the LLM path actually work** (it previously always emitted the deterministic baseline for two independent reasons). **A1 `llm/call_llm.py`:** added a `json_schema` branch to `_build_structured_options` returning the OpenAI envelope `{"type":"json_schema","json_schema":{name, schema, strict:true}}` (the adapter already forwards `response_format`); unblocks the `planner` endpoint (which declared `grammar_constraint_strategy="json_schema"`, previously raising `UnsupportedGrammarStrategyError` every turn) plus `critic`/`pad_translator`/`craft_consultant`. **A2 `prompts/node_plan_global.xml.j2`:** aligned the plan shape to the authoritative validators — `promises`/`payoff` (was `major_promises`/`intended_payoff`) and `arcs[].arc_id` (added; kept rich `title`/`function`/`word_allocation`); updated the objective prose to match. **A3 `fsm/planning_loop.py` + `fsm/nodes/node_plan_global.py`:** made `run_planner_loop` `async` and the decider call await-if-awaitable (`inspect.isawaitable`), broadened the `PlannerDecider` type to sync-or-awaitable; the node now `await`s the loop and the 07.08 `_sync_decider_from_async` thread-bridge shim + `threading`/`asyncio` imports are removed. | done | **Resolves two 07.08 carryovers** (template↔validator key mismatch; async-decider/sync-loop bridge). The await-if-awaitable seam keeps existing synthetic **sync** deciders working while the production **async** decider is awaited directly — minimal blast radius (only `node_plan_global` calls the loop; no committed test file did). Never-persist-invalid is unchanged (validators/persist untouched). No graph route/approval gate (Build 14). Files: `llm/call_llm.py`, `tests/test_structured_output.py`, `prompts/node_plan_global.xml.j2`, `fsm/planning_loop.py`, `fsm/nodes/node_plan_global.py`. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; ran under system `python`. **A1:** `python -m pytest tests/test_structured_output.py -q` → **15 passed** (new `json_schema` test asserts the envelope shape: type/name/strict/schema; the unknown-strategy test still raises on `mystery_mode`). **A2:** re-render via `PromptLoader` with full synthetic ctx → `promises`/`payoff`/`arc_id` present, `major_promises`/`intended_payoff` gone, no leaked Jinja, tight `StrictUndefined` contract intact (omitting any documented var raises). **A3:** `py_compile` clean; node done-check with a **sync** decider and an **async** decider both persisted a valid plan (global `PlanningNode` status `planned`, 1 revision, 1 commit event) through the awaited async loop, and source no longer contains `_sync_decider_from_async`/`threading`/`import asyncio`. Rerun under WSL2/`uv` exercises the identical code. |

| 07.09 (B) | Hierarchical Planning Cascade (M05) | **Phase B of the global-planner overhaul — quality/room-to-think.** **B1 (loop budget + "think" action):** `config.yaml` `planner_max_deliberation_loops.global` 4→6; added `continue_deliberation` as the fifth `PlannerAction.action_type` `Literal` (`fsm/planning_actions.py`, reusing the existing optional `rationale` — no new field, `extra='forbid'` intact); `fsm/planning_loop.py` dispatch handles it as a productive non-finalizing turn — **not** counted in `wasted_turns`, **not** charged against the per-loop tool budget, with the `rationale` appended to `records` and threaded into the accumulated tool-result window so the next render carries the thinking; documented in `prompts/node_plan_global.xml.j2`. **B2 (config-driven baseline):** `fsm/nodes/node_plan_global._build_global_baseline(base_context, config)` now reads `planning.baseline_act_count` + `planning.baseline_word_weights` (new keys in `config.yaml` + typed `PlanningConfig` with `act_count>=1` and non-negative/non-zero-sum weight validators, both with model defaults so existing configs/fixtures still load); produces a variable number of arcs with **distinct** functions (so it also passes the upcoming arc_diversity check), uneven config-weighted `word_allocation`, and a non-tautological promise/payoff. | done | continue_deliberation is additive/backward-compatible across the shared schema; **forward dependency:** the four other `node_plan_*` templates must list it once authored (noted in Current position). Baseline stays deterministic and validator-valid (`schema`/`arc_coverage`/`major_promise_payoff`); thresholds are config-driven, none hardcoded (config-key-guard). Files: `fsm/planning_actions.py`, `fsm/planning_loop.py`, `prompts/node_plan_global.xml.j2`, `config.yaml`, `core/config_loader.py`, `fsm/nodes/node_plan_global.py`. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; ran under system `python`. **B1:** schema accepts `continue_deliberation` (reuses `rationale`) and still rejects unknown action types; loop test — two `continue_deliberation` turns then a finalize → `outcome='finalized'`, `wasted_turns==0`, `tool_call_count==0`, both notes in `records` and threaded into the window the decider saw on the finalize turn; template re-renders with the new action and no leaked Jinja. **B2:** real `config.yaml` loads (`baseline_act_count=3`, weights `[.25,.5,.25]`, global cap 6); baseline at 3 acts → validator-valid, uneven words `[22500,45000,22500]`, distinct functions, non-circular promise; at 5 acts/no weights → 5 distinct arcs, equal split, valid. `python -m pytest tests/test_config.py -q` → **7 passed** (defaulted fields don't break the fixture). Rerun under WSL2/`uv` exercises the identical code. |

| 07.09 (C) | Hierarchical Planning Cascade (M05) | **Phase C of the global-planner overhaul — semantic taste.** **C1 (`fsm/planning_validators.py`):** added two pure, deterministic quality-proxy checks + registered them in `REQUIRED_CHECK_REGISTRY` — `arc_diversity` (every arc has a non-empty, normalized-distinct `function`; rejects templated/undifferentiated outlines) and `promise_payoff_distinct` (each promise's `payoff` is non-empty and not a normalized restatement of the `promise`; rejects tautologies). Wired both into `config.yaml` `planner_required_checks.global` and nudged the LLM in `prompts/node_plan_global.xml.j2` to make arc functions distinct + payoffs non-circular. **C2 (creative second pass):** new `prompts/node_plan_global_consult.xml.j2` (craft-consultant persona) + `fsm/planning_node_support.run_creative_consult(...)` (renders the consult template, calls `call_llm_structured` on the `craft_consultant` endpoint constrained to a strict `GlobalPlanRevision` model, returns the revised plan as a dict; injectable seams) + a config-gated pass in `fsm/nodes/node_plan_global.py` (`planning.creative_second_pass_enabled`, new bool key default false): after the loop yields a validated plan, the node optionally consults, **re-runs `run_validators`**, and adopts the revision via `dataclasses.replace(outcome, plan=…, validation=…)` only if it passes — else keeps the original; best-effort (any error keeps the original). | done | **C1 design note (simplification vs plan):** validators are pure and do **not** receive `config`, so rather than plumb a config "distinct ratio" into them, the checks are threshold-free structural rules (all-distinct functions; non-tautological payoff) — no new config threshold keys; a near-duplicate/embedding-similarity version is deferred (needs the chroma stub + an impure/async validator). The config-driven baseline (B2) was built to pass these, so the expanded 5-check global set stays satisfiable. **C2:** the consultant is a structured hint only — it can never grade itself in (the node re-validates and never persists an unvalidated revision); model contact stays behind the injected seam; gate defaults off until calibrated. `await`-if-awaitable on the consult seam so sync test consults work. Files: `fsm/planning_validators.py`, `config.yaml`, `prompts/node_plan_global.xml.j2`, `prompts/node_plan_global_consult.xml.j2` (new), `fsm/planning_node_support.py`, `core/config_loader.py`, `fsm/nodes/node_plan_global.py`. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; ran under system `python`. **C1:** a good plan passes all five global checks; duplicate (case/space-insensitive) arc functions → `arc_diversity` fails; tautological promise/payoff → `promise_payoff_distinct` fails; arc missing function → `arc_diversity` fails; the config-driven baseline (3 acts and 5 acts) still passes the expanded set; real `config.yaml` loads with the new check names. **C2:** consult template renders tightly; node test — (a) a passing richer revision is adopted + persisted, (b) a failing revision (duplicate functions) is rejected and the original validated plan persisted, (c) an erroring consult keeps the original with no crash, (d) gate off → consult skipped. `python -m pytest tests/test_config.py tests/test_structured_output.py -q` → **22 passed**. Rerun under WSL2/`uv` exercises the identical code. |

| 07.09 (D) | Hierarchical Planning Cascade (M05) | **Phase D of the global-planner overhaul — polish (documentation, design-respecting).** In `fsm/nodes/node_plan_global.py`: (1) documented that two contradictory HARD annotations are **intentionally not auto-reconciled** (Data_Structures §2.7 `requires_user_resolution`) — the node surfaces the conflict (block reason + structured `hard_conflicts` in the deliberation trace) for the user / Build-14 recovery and runs no planning on contradictory input; (2) noted that `existing_arcs` supports the continuation/append pass and a true "revise existing arcs in place" mode is a **deferred** follow-up (its own increment); (3) documented that `escalate` is expected to be rare — the deterministic baseline is the validated floor, so it fires mainly on store failure or a baseline that cannot validate. No behavioural code change. | done | Completes the `clever-noodling-deer.md` plan (Phases A-D). Polish-only; comments capture intentional design decisions rather than adding features (auto-reconciliation would violate §2.7; an LLM "middle-path" suggestion was deliberately not added). Touched only `fsm/nodes/node_plan_global.py` + this ledger row. **Open follow-ups (recorded in Next up):** the four other `node_plan_*` templates must list `continue_deliberation` once authored; a near-duplicate/embedding `arc_diversity` check awaits the chroma store + an impure/async validator path; `existing_arcs` in-place revision; LangGraph route + macro-approval gate (Build 14). | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; ran under system `python`. End-to-end node regression: finalize-valid persists through the expanded 5-check validator set (1 revision, 1 commit event); an invalid decider falls back to the config baseline which passes `arc_diversity`/`promise_payoff_distinct`; a seeded hard-vs-hard conflict (`pin`+`remove` on the global node) blocks with `planning_block_reason='unresolved_hard_conflict'`, writes no revision, and surfaces the conflicts in the trace. Broader regression `python -m pytest tests/test_config.py tests/test_structured_output.py tests/test_gbnf_compiler.py tests/test_prompt_loader.py tests/test_call_llm.py -q` → **57 passed**. Rerun under WSL2/`uv` exercises the identical code. |

| 07.10 (P1) | Hierarchical Planning Cascade (M05) | `prompts/node_plan_arc.xml.j2` (new) — the strict Jinja2 XML prompt template for Level-2 (arc) planning, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`) and re-rendered every deliberation turn. XML sections: `role`, `planning_objective`, `context`, `compiled_constraints`, `deliberation_state`, `output_contract`. Instructs the model to return **exactly one `PlannerAction`** (now five action types incl. `continue_deliberation`) and describes the arc-plan content per `node_plan_arc` + the Phase B **Arc** required checks: arcs realizing the approved global plan (each a unique `arc_id`, a DISTINCT `function` — the no-redundant-function check, `character_milestones`, and the `chapters` chapter-stubs it decomposes into), a top-level `milestones` list with non-decreasing integer `tension` (the escalation relationship), and a `thread_distribution` map (subplot threads' open/progress/close across arcs). Render slots: the approved `global_plan` (+ its arc slots), `compiled_constraints` (hard/soft), accumulated `tool_results`, `raptor_root_summary` (empty when that store is stubbed), `current_plan`, `last_validation`, `last_refusal`, `loop_index`, `open_threads`. A top `{# #}` comment documents the 10 expected variables (base-context group supplied by the node; loop-state group by the decider). | done | **ID note:** Prompt 1 of the M05 `07.10` arc-planner increment (P2 = the `node_plan_arc` node logic). **Plan shape pre-aligned to the authoritative arc validators** (avoiding this session's Risk #1 key mismatch): read `fsm/planning_validators.py` first — `escalation` reads top-level `milestones[].tension` (non-decreasing ints), `thread_distribution` reads a non-empty `thread_distribution` (or addressed thread ids); the template's described plan keys match exactly. All creative/prompt prose lives in the template — none in Python; no provider/model names. **Model proposes; harness disposes** stated (self_check advisory). `continue_deliberation` carried forward per [[planner-loop-room-to-think]] — and the other planner levels (chapter/scene/beat) still need it when authored. Tight `StrictUndefined` contract; nested dict access via `.get(...)`/`{% if %}` guards so the decider's defaults render safely; JSON examples wrapped in `{% raw %}`. The `node_plan_arc` node logic is **not** written here (P2). Touched only `prompts/node_plan_arc.xml.j2` + this ledger row. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; the render is pure Jinja (interpreter-independent) and ran under system `python`. Full synthetic context (all 10 vars) rendered a 6592-char prompt realizing the global arc slots (`Discovery`/`War of maps`), surfacing the validator-facing plan keys (`milestones`/`tension`/`thread_distribution`), chapter stubs (`chapter_id`), the DISTINCT-function instruction, the hard constraint, the empty-`raptor_root_summary` branch, and all five action types with no leaked Jinja. A second-turn context rendered the failed-checks list (`escalation, thread_distribution`), a `continue_deliberation` "think" note from `tool_results`, and the "do not repeat it" refusal. Omitting any of the 10 documented vars raises at render (tight contract; `global_plan` raises `TypeError` via `tojson` on `StrictUndefined`, the rest `UndefinedError` — all loud). Rerun under WSL2/`uv` exercises the identical render. |

| 07.10 (P2) | Hierarchical Planning Cascade (M05) | `fsm/nodes/node_plan_arc.py` — replaced the `NotImplementedError` stub with the real Level-2 planner. `async node_plan_arc(state, *, decider=None, registry=None) -> state`: (1) resolve/create the `PlanningSnapshot` and the **arc anchor = the global `PlanningNode`** (reads the approved global plan from its `purpose` JSON; creates a minimal global anchor only if global was not yet planned — never fabricates a global plan); (2) `compile_planning_constraints` against the anchor — on a hard-vs-hard `needs_clarification`, set `planning_block_reason` + trace and **return without the loop**; (3) assemble `base_context` = approved `global_plan` + `genre` + `open_threads` (`get_open_threads`) + `raptor_root_summary` (degrades to "" while RAPTOR clustering is stubbed); (4) `make_planner_decider("node_plan_arc", …)` + `run_planner_loop(level="arc", …, deterministic_baseline=_build_arc_baseline(...))`; (5) `persist_loop_outcome(..., persist_plan_fn=_arc_persist_plan_fn(...))` which writes each arc as an `Arcs` row + a `level='arc'` `PlanningNode` (parented to the global node, content in `purpose`) and each chapter stub as a `Chapters` row + `level='chapter'` `PlanningNode` with a **monotonic, gapless `ordering` within its arc**; on `finalized`/`fallback_baseline` advance `fsm_pointer.arc_id` to the first planned arc via `model_copy`. | done | **ID note:** Prompt 2 of the M05 `07.10` arc-planner increment (P1 = the `.xml.j2` template). (The prompt's "07.09"/"07.10" labels were stale vs this session's numbering; 07.09 was the global-planner overhaul.) **Model contact only through the injected decider seam — no direct `call_llm`** (verified: no `llm` import, no `call_llm(` invocation). **Never persists an invalid plan / never bypasses validators:** acceptance is `run_planner_loop` + `persist_loop_outcome`; a finalize failing `escalation`/`thread_distribution` falls to the deterministic baseline (validator-shaped: top-level `milestones[].tension` non-decreasing + non-empty `thread_distribution`, realizing the approved global arc slots with DISTINCT functions); `escalate` writes no plan/revision, sets `planning_block_reason='planning_escalation'`, and does **not** advance the pointer. No prose; no graph route/approval gate (Build 14). **Anchor choice:** arc planning is anchored at the global `PlanningNode` (arcs realize the global plan and are its `PlanningNode` children) — reuses the real global node, no fabricated nodes. **Character-state reads not added** — the P1 arc template has no `character_state` slot; `open_threads` is the subplot/thread-distribution input it consumes, so adding character state would be dead base-context data. Touched only `fsm/nodes/node_plan_arc.py` + this ledger row. **No `tests/test_node_plan_arc.py` added** (outside the touched file; done-check optional). | Exact `uv run python -c "from fsm.nodes.node_plan_arc import node_plan_arc; print(node_plan_arc)"` blocked by the documented Windows `.venv\lib64` error; `py_compile` clean and the exact import printed the function under system `python`. Done-check (system `python`, temp `init_db` SQLite + `.jsonl` event log + seeded snapshot/global-node with a 2-arc approved global plan, synthetic typed config, injected sync/async deciders): **(1)** a valid finalize wrote `Arcs` rows `a1`/`a2`, two `level='arc'` PlanningNodes, chapter stubs as `Chapters` rows + `level='chapter'` PlanningNodes with gapless ordering (`a1`→`[0,1]`, `a2`→`[0]`), exactly 1 `PlanningRevision` + 1 `planning_commit` event, and advanced `fsm_pointer.arc_id` to `a1`; **(2)** an always-invalid decider fell back to the deterministic baseline, which realized the global slots (`snap_proj1:arc:a1`/`:a2`) and advanced the pointer; **(3)** a stubbed (async) `escalate` wrote no revision, set the escalation block reason, and left the pointer un-advanced; **(4)** no `call_llm` import/invocation in the node source. Rerun under WSL2/`uv` exercises the identical code. |

| 07.11 (P1) | Hierarchical Planning Cascade (M05) | `prompts/node_plan_chapter.xml.j2` (new) — the strict Jinja2 XML prompt template for Level-3 (chapter) planning, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`) and re-rendered every deliberation turn. XML sections: `role`, `planning_objective`, `context`, `compiled_constraints`, `deliberation_state`, `output_contract`. Instructs the model to return **exactly one `PlannerAction`** (five action types incl. `continue_deliberation`) and describes the chapter-plan content per `node_plan_chapter` + the built chapter required checks: the ACTIVE chapter's `dramatic_function`, `expected_emotional_shift` + `pacing`, `obligations` ({`thread_obligations` toward each thread's target resolution, `causal_prerequisites`, `causal_deliverables`} — the causal-order + thread-obligation content), `annotation_outcomes` recording every hard requirement id as `"applied"`, and `scene_planning_constraints` (the handoff `node_plan_scene` consumes). **Explicitly forbids final scene plans**: no `scenes` key, no per-scene settings/participants/entry-exit/beats, no prose fields anywhere; scene count/order/setting are the Scene Planner's runtime decisions. Render slots: approved `global_plan` + `arc_plan` + `active_chapter` stub, `genre`, `recent_chapter_summaries` (RAPTOR; empty-safe), `open_threads`, `compiled_constraints` (hard **with ids**, soft), `tool_results`, `current_plan`, `last_validation`, `last_refusal`, `loop_index`. A top `{# #}` comment documents the 12 expected variables (6 base-context supplied by the node; 6 loop-state by the decider). | done | **ID note:** Prompt 1 of the M05 `07.11` chapter-planner increment (P2 = the `node_plan_chapter` node logic — NOT written here). **Plan shape pre-aligned to the authoritative chapter validators** (this session's carried-forward Risk #1): `config.yaml` chapter checks are `[schema, no_drafting, chapter_function, pacing, annotation_satisfaction]`; `chapter_function` reads top-level `dramatic_function` (first alias), `pacing` reads `pacing`/`expected_emotional_shift`, and `annotation_satisfaction` requires an `annotation_outcomes` map with every compiled hard annotation id → `"applied"` — so this template (unlike global/arc, whose levels don't run that check) **renders hard-annotation ids** (`[id: …]`) beside their text. Shape also honors the `_DEPTH_CONTRACT` chapter row (`obligations` required, `scenes` forbidden) and `no_drafting` (no prose fields at any depth), future-proofing against those checks joining the configured list. THREAD_PARADOX needs no special slot — the design injects it as a hard constraint via the compiled package; the objective says forbidden-outcome hard requirements are absolute and the chapter is structured around them. All creative/prompt prose lives in the template — none in Python; no provider/model names; no authoritative tool list (the `PlanningToolRegistry` matrix stays the gate); model proposes / harness disposes stated (`self_check` advisory). `continue_deliberation` carried per [[planner-loop-room-to-think]] — scene/beat templates still need it when authored. Tight `StrictUndefined` contract; nested access via `.get(...)`/`{% if %}` guards; JSON examples in `{% raw %}`; injected dicts pass `| tojson | xmlsafe`. Touched only `prompts/node_plan_chapter.xml.j2` + this ledger row. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error (`Access is denied`); the render is pure Jinja (interpreter-independent) and ran under system `python`. Full synthetic turn-0 context (all 12 vars) rendered a 9575-char prompt via `PromptLoader.render("node_plan_chapter", ctx)` with no leaked Jinja, surfacing all validator-facing keys (`dramatic_function`/`pacing`/`expected_emotional_shift`/`annotation_outcomes`/`obligations`/`thread_obligations`/`causal_prerequisites`/`causal_deliverables`/`scene_planning_constraints`), the "must NOT emit final scene plans" + no-`scenes`-key rules, the hard-annotation id (`[id: ann_7]`), all five action types, and the active-chapter/thread context. A second-turn context rendered the failed-checks list (`pacing, annotation_satisfaction`), a `continue_deliberation` think note from `tool_results`, the draft plan, and the "do not repeat it" refusal. Omitting **any** of the 12 documented vars raised loudly (`UndefinedError`; the three `tojson`-piped dicts raise `TypeError` via `StrictUndefined` — all loud). Rerun under WSL2/`uv` exercises the identical render. |

| 07.11 (P2) | Hierarchical Planning Cascade (M05) | `fsm/nodes/node_plan_chapter.py` — replaced the `NotImplementedError` stub with the real Level-3 planner. `async node_plan_chapter(state, *, decider=None, registry=None) -> state`: (1) resolve/create the `PlanningSnapshot` (mode = `state['planning_execution_mode']`, captured at run start — **never re-read from config at node time**) and the **active chapter target**: the first unplanned chapter `PlanningNode` (purpose lacking a real `dramatic_function`), walking arcs/chapters in `ordering`, preferring `fsm_pointer.chapter_id` when it names an unplanned one — this is how one invocation per chapter sweeps the macro scope (self-loop routing is Build 14); (2) `compile_planning_constraints` against the chapter node — on hard-vs-hard `needs_clarification`, set `planning_block_reason` + trace and return WITHOUT the loop (a THREAD_PARADOX arrives as an ordinary hard constraint; no special code path); (3) `base_context` = exactly the six P1-template vars (`arc_plan`/`active_chapter`/`global_plan`/`genre`/`recent_chapter_summaries`/`open_threads`); (4) `make_planner_decider("node_plan_chapter", …)` + `run_planner_loop(level="chapter", …, deterministic_baseline=_build_chapter_baseline(...))`; (5) `persist_loop_outcome(..., persist_plan_fn=_chapter_persist_plan_fn(...), annotation_outcomes=plan['annotation_outcomes'])` — writes the `Chapters` row + chapter obligations + scene-planning constraints via the 07.00 `upsert_chapter_plan` (atomic), then upgrades the chapter PlanningNode `purpose` to the FULL validated plan JSON (superset of the obligations JSON; matches the global/arc full-plan-in-purpose convention the scene planner will read) — **writes no Scene rows**; on success advance `fsm_pointer` (`chapter_id` + its `arc_id`); (6) **last macro level:** when `state['planning_execution_mode']=="macro_outline_before_draft"` and no unplanned chapter remains, set `macro_outline_ready=True`; when additionally `state['approval_mode']=="macro_outline"` (and not already approved), set `awaiting_planning_approval=True` + `planning_block_reason="awaiting_macro_approval"` + snapshot status via the 07.00 `transition_snapshot_status`. In `rolling` mode readiness/pause state is never touched. | done | **ID note:** Prompt 2 of the M05 `07.11` chapter-planner increment (the prompt's "set 07.10 / advance to 07.11" labels were stale vs this ledger's numbering; 07.10 is the arc planner). **Reads execution/approval mode from the STATE snapshot fields, never config at node time** (Data_Structures §1.1: they are run-start snapshots). **Only SETS approval-gate state and returns — never blocks/waits** (Module_Ability_Spec §5; the edge that holds drafting on `awaiting_planning_approval` is Build 14; `routes/plan.py` Build 19 flips approval); 07.0a config validation guarantees headless cannot coexist with `approval_mode!="off"`, so headless always takes the no-pause branch. **Snapshot "awaiting-approval" value design note:** the §2.7 status CHECK vocabulary (`draft/user_annotated/revision_requested/revised/approved/rejected/superseded`) has **no dedicated awaiting value** — the pause lives in state; the node transitions the snapshot to `'draft'` (the §2.7 pre-approval presentation status the approval surface acts on; idempotent when already draft), and a `macro_outline_approved` guard keeps a re-run from re-pausing or clearing the approval stamp (non-approved transitions NULL `approved_at`). **Writes obligations/constraints, never final scenes** (chapter depth contract forbids `scenes`; done-check asserts 0 Scene rows). **Model contact only through the injected decider seam — no direct `call_llm`** (verified: no `llm` import, no `call_llm(`/`call_llm_structured(`). **Never persists an invalid plan:** acceptance is `run_planner_loop` + `persist_loop_outcome`; the deterministic baseline is validator-shaped for all five chapter checks — hard annotations are satisfied by a real mechanism (each hard requirement's text forwarded verbatim as a binding scene-planning constraint, only then recorded `applied`); `escalate` writes no revision, sets `planning_block_reason='planning_escalation'`, advances nothing. Persist identity is harness-owned (rows keyed by the TARGET chapter, never the plan's claimed `chapter_id`). `_recent_chapter_summaries` degrades to `[]` while RAPTOR summarization is a stub (documented; no invented level convention). Touched only `fsm/nodes/node_plan_chapter.py` + this ledger row. **No `tests/test_node_plan_chapter.py` added** (outside the "Touch only" file, as in 07.08/07.10 P2). | Exact `uv run python -c "from fsm.nodes.node_plan_chapter import node_plan_chapter; print(node_plan_chapter)"` blocked by the documented Windows `.venv\lib64` error; `py_compile` clean and the exact import printed the function under system `python`. Done-check (system `python`, temp `init_db` SQLite + `.jsonl` per scenario, seeded snapshot/global/arc nodes + 2 chapter stubs mimicking `_arc_persist_plan_fn`, synthetic typed config, injected sync/async deciders): **(1)** valid finalize → `Chapters` row `planned` with the dramatic function as description; chapter PlanningNode `purpose` = full plan JSON (obligations incl. `thread_obligations`/causal fields + `scene_planning_constraints`); **0 `Scenes` rows**; 1 `PlanningRevision`; pointer → `a1_ch1`; readiness untouched while a stub remains. **(2)** second invocation completed the scope → pointer `a1_ch2`, 2 revisions, `macro_outline_ready=True`, `awaiting_planning_approval=True`, `planning_block_reason='awaiting_macro_approval'`, snapshot transitioned `revised→draft` (transition observable); the assertion line running proves the node returned rather than paused. **(2b)** re-run after `approved` + `macro_outline_approved=True` → no re-pause, approval stamp preserved, no extra revision. **(3)** `rolling` mode planned both chapters (2 revisions, 0 scenes) with readiness/pause fields untouched and snapshot left `draft`. **(4)** always-invalid decider (plan carrying `scenes`) → fell back to the deterministic baseline: persisted plan has **no `scenes` key**, a real `dramatic_function`, and — with a seeded hard annotation — `annotation_outcomes={'ann_hard_1':'applied'}` + the hard text forwarded as a scene-planning constraint. **(5)** stubbed `escalate` loop → 0 revisions, `planning_block_reason='planning_escalation'`, pointer un-advanced, readiness untouched. **(6)** no `call_llm` import/invocation in the node source. Rerun under WSL2/`uv` exercises the identical code. |

| 07.12 (P1) | Hierarchical Planning Cascade (M05) | `prompts/node_plan_scene.xml.j2` (new) — the strict Jinja2 XML prompt template for Level-4 (scene) planning, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`) and re-rendered every deliberation turn. XML sections: `role`, `planning_objective`, `context`, `compiled_constraints`, `deliberation_state`, `output_contract`. Instructs the model to return **exactly one `PlannerAction`** (five action types incl. `continue_deliberation`) and to realize ONE scene under the chapter's scene-planning constraints, per the design's Scene checks: `scene_function` (which chapter obligation it advances), `setting` + `participants`, `entry_state`/`exit_state` (entry chained from the previous planned scene's exit, or the chapter's causal prerequisites for the first scene; exit must MEANINGFULLY differ — the **no-dead-scene** rule stated as "cut it, merge it, or give it a real turn"), `conflict_turn`, `asserted_facts` (`{subject, attribute, value}` — with the rationale that declared facts let the harness catch contradictions cheaply, hidden ones surface expensively in drafting), `continuity_constraints` (the handoff to the Beat Planner), and `word_budget` (the real `Scenes` column `upsert_scene_plan` writes, which the beat planner's scene-advancement logic later reads). **Explicitly forbids prose and beats**: structural plan only — no beat list, no dialogue, no prose fields (beats are the Beat Planner's job). Render slots: `chapter_plan` (full approved plan; its `scene_planning_constraints` also itemized in their own XML block), `planned_scenes` (empty-safe first-scene branch), `genre`, `continuity_facts` (empty-safe while fact stores are stubbed), plus the standard 6 loop-state slots; tool guidance notes RAPTOR/Chroma results may be empty while stubbed. A top `{# #}` comment documents the 10 expected variables (4 base-context by the node; 6 loop-state by the decider). | done | **ID note:** Prompt 1 of the M05 `07.12` scene-planner increment (P2 = the `node_plan_scene` node logic — NOT written here). **Plan shape pre-aligned to the authoritative scene validators** (`config.yaml` scene checks `[schema, no_drafting, continuity, scene_function, entry_exit_state]`): `scene_function` reads top-level `scene_function` (first alias), `entry_exit_state` requires non-empty `entry_state`+`exit_state` (also the `_DEPTH_CONTRACT` scene row), and `continuity` compares the plan's declared `asserted_facts` list against the handed-in continuity facts (same `{subject, attribute, value}` shape — so the template teaches that exact key shape AND supplies `continuity_facts` in it). Setting/participants and no-dead-scene are design-listed Scene checks not yet in the configured validator list — instructed in the template anyway (future-proof, same posture as chapter's causal-order). All creative/prompt prose lives in the template — none in Python; no provider/model names; no authoritative tool list (the `PlanningToolRegistry` Scene column stays the gate); model proposes / harness disposes (`self_check` advisory). `continue_deliberation` carried per [[planner-loop-room-to-think]] — only the beat template still needs it. Tight `StrictUndefined` contract; nested access via `.get(...)`/`{% if %}` guards; JSON examples in `{% raw %}`; injected dicts pass `| tojson | xmlsafe`. Touched only `prompts/node_plan_scene.xml.j2` + this ledger row. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; the render is pure Jinja (interpreter-independent) and ran under system `python`. Full synthetic turn-0 context (all 10 vars) rendered an 8608-char prompt via `PromptLoader.render("node_plan_scene", ctx)` with no leaked Jinja, surfacing all validator-facing keys (`scene_function`/`setting`/`participants`/`entry_state`/`exit_state`/`conflict_turn`/`asserted_facts`/`continuity_constraints`/`word_budget`), the NO-DEAD-SCENE rule, the no-prose/no-beat rules, the chapter's scene-planning constraints (incl. the word-budget guidance), the prior scene's exit state for chaining, the continuity fact, the hard requirement, and all five action types. Empty-store contexts rendered the first-scene and no-continuity-facts branches. A second-turn context rendered the failed-checks list (`entry_exit_state, continuity`), a `continue_deliberation` think note, the draft plan, and the "do not repeat it" refusal (one micro-check assertion initially failed on an apostrophe — Jinja `tojson` escapes `'` to `'`; test artifact, not a template defect; assertion adjusted). Omitting **any** of the 10 documented vars raised loudly (`UndefinedError`; the `tojson`-piped `chapter_plan` raises `TypeError` via `StrictUndefined` — all loud). Rerun under WSL2/`uv` exercises the identical render. |

| 07.12 (P2) | Hierarchical Planning Cascade (M05) | `fsm/nodes/node_plan_scene.py` — **newly created** (the only planner node missing from the 01.01 scaffold; this closes that gap). `async node_plan_scene(state, *, decider=None, registry=None) -> state`, the first-class runtime Scene Planner: (1) resolve/create the `PlanningSnapshot` + the **chapter anchor** = the ACTIVE chapter's PlanningNode (`fsm_pointer.chapter_id` → `{snap}:chapter:{id}`), reading the approved chapter plan from its full-plan `purpose`; if the chapter is unplanned (no node, or an arc-written stub without a `dramatic_function`), record a `no_planned_chapter` trace and return without the loop (re-routing is Build 14); (2) `compile_planning_constraints` against the chapter anchor (the scene node does not exist yet — scene planning inherits the chapter's compiled package); hard-vs-hard → block + return; (3) `base_context` = exactly the four P1-template vars (`chapter_plan` / `planned_scenes` — lean summaries read from the chapter node's scene PlanningNode children so entry-state chains from the last exit-state / `genre` / `continuity_facts`); (4) `make_planner_decider("node_plan_scene", …)` + `run_planner_loop(level="scene", …, deterministic_baseline=_build_scene_baseline(...))`; (5) `persist_loop_outcome(..., persist_plan_fn=_scene_persist_plan_fn(...))` — writes the `Scenes` row via the 07.00 `upsert_scene_plan` (description/`word_budget`/`ordering`/status) + a `level='scene'` PlanningNode parented to the chapter node with the full validated plan JSON (harness-final `scene_id`/`ordering` folded in) in `purpose`; on success advance `fsm_pointer.scene_id` to the allocated id. Runs just-in-time in BOTH execution modes. | done | **ID note:** Prompt 2 of the M05 `07.12` scene-planner increment (the prompt's "set 07.11 / advance to 07.12" labels were stale vs this ledger's numbering). **`ordering` is allocated explicitly and race-safely at WRITE time**: the persist fn re-reads `get_scenes_for_chapter_ordered` and assigns `max(existing)+1` (or `0` when none) — never hardcoded, never colliding, strictly monotonic and gapless across repeated just-in-time planning. **Scene identity is harness-owned:** a missing plan `scene_id` — or one colliding with an existing scene — is replaced with a fresh derived `{chapter_id}_s{n}` id (bumped past collisions), because the 07.00 writer upserts by id and overwriting a previously planned scene would be silent data loss. `word_budget` coerced to a non-negative int (0 on garbage). **Model contact only through the injected decider seam — no direct `call_llm`** (verified: no `llm` import, no `call_llm(` invocation). **Never persists an invalid plan:** acceptance is `run_planner_loop` + `persist_loop_outcome`; the deterministic baseline is validator-shaped for all five scene checks (entry chained from the last planned scene's exit or the chapter's causal prerequisites; exit differs — no dead scene; `asserted_facts` stays `[]` — the baseline asserts no continuity it cannot verify; chapter scene-planning constraints forwarded as `continuity_constraints`); `escalate` writes no scene/revision, sets `planning_block_reason='planning_escalation'`, pointer untouched. `_continuity_facts` degrades to `[]` (documented) while the Graphiti fact store is a stub. No prose/beats written; no graph/approval wiring (Build 14). `fsm/nodes/__init__.py` untouched — its ownership header is a module-summary docstring with no per-file lines (M05 already listed), so there was no line format to extend. Touched only `fsm/nodes/node_plan_scene.py` + this ledger row. **No `tests/test_node_plan_scene.py` added** (outside the "Touch only" files, as in 07.08/07.10/07.11 P2). | Exact `uv run python -c "from fsm.nodes.node_plan_scene import node_plan_scene; print(node_plan_scene)"` blocked by the documented Windows `.venv\lib64` error; `py_compile` clean and the exact import printed the function under system `python`. Done-check (system `python`, temp `init_db` SQLite + `.jsonl` per scenario, seeded snapshot/global/arc + PLANNED chapter node with full-plan purpose, synthetic typed config, injected sync/async deciders): **(1)** two invocations with a decider that deliberately reuses `scene_id="s_alpha"` → orderings **[0, 1]** (strictly monotonic, gapless), first scene `s_alpha` untouched, second reallocated to `a1_ch1_s2`, `word_budget=1200` persisted, scene PlanningNodes parented to the chapter node with `ordering`+`exit_state` in `purpose`, exactly 1 revision per scene (2 total), `fsm_pointer.scene_id` advanced each time. **(2)** always-invalid decider (missing entry/exit + a `prose` field) → baseline persisted: **no `prose` key**, non-empty entry/exit states, chapter constraints forwarded as `continuity_constraints`, 1 revision, pointer advanced. **(3)** stubbed `escalate` loop → 0 scenes, 0 revisions, `planning_block_reason='planning_escalation'`, pointer unchanged. **(4)** unplanned chapter anchor → decider never called (0 loop entries), no writes, `no_planned_chapter` trace recorded. **(5)** no `call_llm` import/invocation in the node source. Rerun under WSL2/`uv` exercises the identical code. |

| 07.13 (P1) | Hierarchical Planning Cascade (M05) | `prompts/node_plan_beat.xml.j2` (new) — the strict Jinja2 XML prompt template for Level-5 (beat) planning, the last of the five planner templates, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`) and re-rendered every deliberation turn. Same six XML sections as its siblings. Instructs the model to return **exactly one `PlannerAction`** (five action types incl. `continue_deliberation`) and to plan ONE draftable beat of the active scene: `immediate_objective` (one beat, one objective), `physical_constraints` (concrete, blockable staging), `entry_condition`/`exit_condition` (chained from the previous beat's exit or the scene's entry_state; the beat sequence walks the scene entry→exit through the conflict_turn), `asserted_facts`, and a DRAFTABILITY guarantee (executable with no further planning). **The affect target is harness-computed, never model-computed:** an `<affect_target>` block renders the node-supplied `pad_target` + `pad_behavioral_constraint` with the instruction to copy both VERBATIM into the plan's `pad_target`/`behavioral_constraint` keys and stage the beat so a character in that state would plausibly behave that way ("Do NOT compute, adjust, or invent affect coordinates"). Structural only: no prose, no dialogue lines, no narration. A top `{# #}` comment documents the 12 expected variables (6 base: `scene_plan`/`planned_beats`/`pad_target`/`pad_behavioral_constraint`/`genre`/`continuity_facts`; 6 loop-state by the decider). | done | **ID note:** Prompt 1 of the M05 `07.13` beat-planner increment (P2 = the node logic). **Plan shape pre-aligned to the authoritative beat validators** (`config.yaml` beat checks `[schema, no_drafting, draftability, continuity, pad_grounding]`): `draftability` reads top-level `immediate_objective` + `physical_constraints` (first aliases), `pad_grounding` reads `pad_target` + `behavioral_constraint`, `continuity` reads `asserted_facts`. **One beat per plan** (not a beats list): the validators key on top-level singular fields and the cascade advances beat-by-beat (`node_commit_transaction` re-triggers for the next beat), so a list-shaped plan would fail `draftability` — the singular shape is both validator-correct and design-correct. `continue_deliberation` carried per [[planner-loop-room-to-think]] — **all five planner templates now list it; that forward dependency is closed**. Tight `StrictUndefined` contract; nested `.get(...)`/`{% if %}` guards; JSON examples in `{% raw %}`; injected dicts `| tojson | xmlsafe`. No provider/model names; no prompt prose in Python. Touched only `prompts/node_plan_beat.xml.j2` + this ledger row. | Exact `uv run` blocked by the documented Windows `.venv\lib64` error; the render is pure Jinja and ran under system `python`. Full synthetic turn-0 context (all 12 vars) rendered an 8602-char prompt with no leaked Jinja, surfacing all validator-facing keys, the harness-computed PAD values (`"arousal": 0.55`, the constraint string), the VERBATIM-echo + never-compute instructions, the DRAFTABILITY rule, the no-prose rule, prior-beat chaining, the continuity fact, the hard requirement, and all five action types. Empty-store contexts rendered the first-beat and no-facts branches. A second-turn context rendered the failed-checks list (`draftability, pad_grounding`), a think note, the draft plan, and the refusal. Omitting **any** of the 12 documented vars raised loudly (`UndefinedError`; `tojson`-piped `scene_plan`/`pad_target` raise `TypeError` via `StrictUndefined`). One micro-check assertion adjusted for a line-wrapped instruction (test artifact). Rerun under WSL2/`uv` exercises the identical render. |

| 07.13 (P2) | Hierarchical Planning Cascade (M05) | `fsm/nodes/node_plan_beat.py` — replaced the `NotImplementedError` stub with the real Level-5 planner; **all five M05 planner nodes + templates are now built**. `async node_plan_beat(state, *, decider=None, registry=None, adapt_fn=None) -> state`: (1) resolve/create the `PlanningSnapshot` + the **scene anchor** (`fsm_pointer.scene_id` → `{snap}:scene:{id}` PlanningNode, full scene plan from `purpose`); unplanned scene → `no_planned_scene` trace + return; (2) compile constraints against the scene anchor; hard-vs-hard → block + return; (3) **PAD Grounded Translation Pipeline**: prior = mean of the scene's latest per-character PAD rows (`get_latest_pad_for_scene`; neutral origin when no history) → proposed = the scene plan's own affect key (`pad_target`/`pad`/`pad_state`) or the prior itself (affect inertia) → `smooth_pad_target(prior, proposed, alpha)` with **`alpha = config.thresholds.pad_ewma_alpha` (reused, never a new key, never hardcoded)** → `translate_pad_to_behaviour(target, ctx, adapt_fn=adapt_fn)` with the seam passed through verbatim (**`None` → the pure `pad_regions.json` static rung, zero LLM**); the rung/region/target recorded in the deliberation trace; (4) `base_context` = exactly the six P1-template vars; (5) loop + `persist_loop_outcome(..., persist_plan_fn=_beat_persist_plan_fn(...))` — the 07.00 `upsert_beat_plan` writes the structural `Beats` row (`prose`/`committed_at` NULL, `word_count` left at its schema DEFAULT 0) with `pad_constraint` = the grounded string, plus the beat PlanningNode (full-plan `purpose`, sibling convention); `beat_index` derived at write time (`max+1`/`0`), beat identity harness-owned (colliding `beat_id` → derived `{scene_id}_b{n}`), and **the harness owns the affect: persisted `pad_target`/`behavioral_constraint` are forced to the node-computed values regardless of what the model echoed**; (6) on success advance `fsm_pointer.beat_id` + `beat_index` and set **`state["scene_needs_more"]`**: a scene may close only when committed word volume ≥ the scene's `word_budget` (Scenes row) AND beat count ≥ `config.runtime.beats_per_scene_min` (reused key). | done | **ID note:** Prompt 2 of the M05 `07.13` beat-planner increment (the prompt's "set 07.12" label was stale vs this ledger). **No new config keys:** EWMA alpha reuses `thresholds.pad_ewma_alpha`, the stop signal reuses `runtime.beats_per_scene_min` + the per-scene `word_budget`; the book-level `runtime.word_count_target` is NOT a scene-stop input (the design predicate is scene-scoped). **Stop-predicate reconciliation (documented):** the LangGraph section's AND-composition is self-contradictory as written; implemented per the conceptual spec's volume-aware criterion ("a scene closes only when its volume target is met AND a minimum unit count is reached") — `scene_needs_more = not (volume_met and min_units_met)`. **Proposed-PAD derivation (documented degrade):** the design's scene-intent→PAD interpretive step has no specified deterministic mechanism and `pad_regions.json` has no reverse text→coordinate prototypes, so the node uses the scene plan's declared affect key or affect inertia — inventing coordinates would be a hidden tunable (config-key-guard). **The PAD static/fallback path is LLM-free** (pure JSON lookup; `adapt_fn` is the only optional model contact and is skippable; wiring `build_pad_adapt_fn` + its `node_plan_beat_pad` template is a recorded follow-up, as is the WARNING log on the fallback rung — the rung is trace-recorded meanwhile). `scene_needs_more` is set as a runtime state key; declaring it on `OrchestratorState` is an M01 follow-up (out of this increment's touch scope). Never persists an invalid plan; deliberation-path model contact only via the decider seam. Touched only `fsm/nodes/node_plan_beat.py` + this ledger row. **No `tests/test_node_plan_beat.py` added** (outside the "Touch only" file). | Exact `uv run python -c "from fsm.nodes.node_plan_beat import node_plan_beat; print(node_plan_beat)"` blocked by the documented Windows `.venv\lib64` error; `py_compile` clean and the exact import printed the function under system `python`. Done-check (system `python`, temp stores, seeded snapshot + planned scene node with a scene-declared `pad_target=(0.8, 0.4, -0.6)`, synthetic config with distinctive `pad_ewma_alpha=0.5` + `beats_per_scene_min=3`, injected deciders, `adapt_fn=None`): **(1)** valid finalize → EWMA verified arithmetically (prior=origin, smoothed exactly `(0.4, 0.2, -0.3)` — proving the config alpha is read), translation rung `static` (no LLM), the decider's BOGUS echoed pads (`9.9`s) and `"MODEL-INVENTED"` constraint **overwritten by the harness values** in the persisted purpose, Beats row with `pad_constraint`, commit fields untouched (`prose`/`committed_at` NULL, `word_count` at schema DEFAULT 0), 1 revision, pointer → `beat_id='b_alpha'`/`beat_index=0`, `scene_needs_more=True` (1 < 3 beats). **(2)** three beats → `beat_index` `[0,1,2]` monotonic/gapless with colliding ids reallocated, `scene_needs_more=False` at 3 beats with `word_budget=0`; a fresh scene with `word_budget=1200` and 0 committed words stays `True` at 3 beats — both axes of the predicate proven. **(3)** always-invalid (prose-carrying) decider → validator-passing baseline persisted with harness affect, never the invalid plan. **(4)** stubbed `escalate` → no beats/revisions, `planning_block_reason='planning_escalation'`, no stop signal set. **(5)** unplanned scene → decider never called, `no_planned_scene` trace. **(6)** no `call_llm` import/invocation in the node source. One done-check assertion corrected mid-run (expected `word_count` NULL; the schema declares `NOT NULL DEFAULT 0` — test artifact, not a writer defect). Rerun under WSL2/`uv` exercises the identical code. |

| VIS-03 | M05 visualization frontend | `tests/frontends/m07_planning.py` (new) — Streamlit visualizer for the real planner cascade — and `shared.seed_planning_data(db_path, *, event_log_path=None)`. The seed layers a deterministic planning surface on the narrative seed via the real 07.00 helpers (no raw SQL): `PlanningSnapshot` `snap_proj-1` (matching the id the nodes derive from `project_id='proj-1'`), six `PlanningNode`s (planned global/arc-1/chapter-1/scene-1 with full plan JSON in `purpose` incl. a scene-declared `pad_target`; unplanned chapter-2/chapter-3 stubs as chapter-planner targets), four `PlanningAnnotation`s (one soft global `tone`, one satisfiable hard on chapter-1, and a deliberate **hard-vs-hard `pin`/`remove` pair on the chapter-2 stub** so the clarification path is demonstrable), and one `PlanningRevision` `rev-seed-1` (advances `active_revision_id` through the real helper). The page drives the production `node_plan_{global,arc,chapter,scene,beat}` end-to-end (compile constraints → bounded loop → persist) with an editable **scripted-decider JSON** (a list of strict-validated `PlannerAction`s, injected through the decider seam — no `call_llm`, no network; beat runs pass `adapt_fn=None`), an execution-mode selector (`macro_outline_before_draft`/`rolling`) + approval toggle folded into the synthetic state snapshot, and per-level valid/always-invalid decider presets. Output runbook (m06-style left-to-right): run-outcome metrics (rung/revision/block reason + returned state fields); the **per-iteration deliberation trace** (think turns, tool calls incl. refusals/reuse, revise/finalize validation verdicts, phase records); compiled-constraints JSON; **validator results per configured required check** (re-run live via `REQUIRED_CHECK_REGISTRY` against the persisted plan, falling back to the loop's final verdict); the **fallback rung** indicator (`finalized`/`best_valid`/`baseline`/`needs_clarification`/`escalate`); persisted `PlanningNode` changes + the `PlanningRevision` with parsed `diff_json` + the `planning_commit` event; the **PAD pipeline panel** (beat level: smoothed target, region lookup + `pad_regions.json` entry, EWMA alpha from config, translation rung, prior PAD rows, persisted behavioural string); `PlannerToolCallTrace` rows (executed/degraded/rejected); and a raw planning-store expander. Hover-help `i` notes per the convention. **Prompt 2 (same session):** registered in the launcher (`tests/frontends/run_all.py`: `import m07_planning`, `"M07 Planning Cascade": m07_planning.render` in `PAGES`, docstring module range → `M02-M07`) and documented in `tests/frontends/STREAMLIT_FRONTENDS.md` (ToC entry; `m07_planning.py` + the M05 planning modules + `seed_planning_data` in the layer diagram; a `seed_planning_data` table under Synthetic data seeding; launch lists updated to six visualizers; full **M07 — Planning Cascade** per-module reference: file, modules exercised, run controls, output panels, and a 10-item edge-cases list incl. always-invalid decider, the seeded hard-conflict path, empty planning DB, `pad_ewma_alpha` at 0/1, and ordering across ≥2 scenes/beats; `Last updated` → 2026-07-02). Produced/changed files across both prompts: `tests/frontends/shared.py`, `tests/frontends/m07_planning.py`, `tests/frontends/run_all.py`, `tests/frontends/STREAMLIT_FRONTENDS.md`. **Follow-up (same session, user-requested): LLM-mode toggle** — a top-of-page `LLM mode` dropdown on `m07_planning.py` (the only frontend that fakes LLM calls; m02/m03/m05/m06 have no LLM path and m04 exercises parsing helpers only). **`Mocked LLM` (default)**: the scripted decider, zero network. **`Real LLM`**: bypasses the mock — `decider=None` so the node builds the production `make_planner_decider` → `call_llm_structured` against `config.endpoints.planner`, with the real `core.config_loader.load_config` (strict validation) after loading `*_API_KEY` secrets from the repo `.env` (minimal parser, shell env wins) and applying optional `*_BASE_URL` env overrides; decider preset/JSON controls disabled in Real mode; missing `.env`/secrets fail with a `.env`-naming error before any run. The selection is mirrored into a non-widget session key so it survives reruns and page switches inside `run_all.py`. `adapt_fn` stays `None` in both modes (the PAD adapter template is still unauthored). | done | **This is an interactive visual debugger, not an automated test** — it exercises the real planner cascade (production nodes/loop/validators/registry/compiler/persistence) under human inspection; the codified pytest coverage for the planner nodes is the separate `07.T1` increment (Next up). **ID note:** the prompt library labels this visualizer increment `07.13`, but `07.13 (P1)/(P2)` in this ledger are the beat-planner template/node rows — this row keeps the out-of-band `VIS-03` id to avoid a collision. Out-of-band utility; the numbered build sequence is unaffected. Config-shaped object reads real `config.yaml` planning caps/checks/thresholds without endpoint secrets (endpoints never touched — the decider is injected). The arc default decider introduces `arc-2` so it never clobbers the seeded planned chapter-1 (proven in smoke). Chapter runs with the default pointer hit the seeded chapter-2 conflict (clarification demo); pointer at `chapter-3` gives the clean run. The "run" uses `asyncio.run` per click (Streamlit script thread has no running loop). Numeric literals in the page are structural/synthetic fixture values, not tunables. | Headless smoke (isolated `%TEMP%` uv env, real temp stores): seed idempotent under replay (6 nodes, `active_revision_id='rev-seed-1'`); **global one-action `finalize` decider → rung `finalized`**, all 6 configured checks pass re-run live, revision + node change + trace render-rows non-empty; full default global decider shows `continue_deliberation`+`call_tool`+`finalize` and 1 `PlannerToolCallTrace` row; **always-invalid decider → rung `baseline` with the invalid plan never persisted** (persisted plan re-validates clean); chapter@default-pointer → `needs_clarification`, both conflict annotations marked, nothing persisted; chapter@chapter-3 → `finalized`; beat → `finalized`, PAD rung `static`, region `P+A+D+`, harness behavioural string persisted (decider's placeholder overwritten), `scene_needs_more=True`, pointer advanced to `scene-1_b4`; arc → `finalized` with chapter-1 still planned. Full-render `streamlit.testing.v1.AppTest`: initial render, seed click, global one-action finalize (metrics row shows `finalized`), and always-invalid (`baseline`) — no unhandled exception, no `st.error`. `uv run streamlit run tests/frontends/m07_planning.py` (headless, port 8517) started; localhost probe returned HTTP 200. **Prompt-2 done-check:** `AppTest` over `run_all.py` — sidebar radio lists all six pages ending in `M07 Planning Cascade`; selecting it rendered the planning visualiser (its three action/run buttons present, no exception). `uv run streamlit run tests/frontends/run_all.py` (headless, port 8518) started; localhost probe returned HTTP 200. **LLM-mode toggle checks:** `AppTest` — dropdown defaults to `Mocked LLM`; mocked run still `finalized`; switching to `Real LLM` disables the decider controls, mirrors to the session store, and persists across reruns; a Real run with no `.env` fails gracefully with a `.env`-naming `st.error` (no crash). **Live Real-LLM verification** (secrets via in-process env; local endpoint reachable with the configured model present): scene level in Real mode made two genuine structured-output calls (both `continue_deliberation`), hit the 2-loop cap, and the ladder persisted the validated `baseline` plan in 26.4s — all required checks pass on the persisted plan. |
| 07.T1 | Hierarchical Planning Cascade (M05 test) | `tests/test_planning_actions.py`, `tests/test_planning_validators.py`, `tests/test_planning_tools.py`, and `tests/test_planning_loop.py` - deterministic, no-network pytest coverage for PlannerAction construction/strict schema, generic validators plus `run_validators`, the safe traced PlanningToolRegistry, and the bounded deliberation loop. | done | Tests use synthetic in-memory plans, temp SQLite planning DB fixtures, injected config objects, fake registries, and scripted `PlannerAction` deciders only. Registry tests confirm representative §1.5 permission behavior, successful seeded reads with `PlannerToolCallTrace`, rejected unknown/disallowed tools without execution, Graphiti/Chroma/RAPTOR degradation as `unavailable`, and no shell/filesystem/network tool names in dispatch. Loop tests prove cap enforcement, fallback order (best valid candidate to valid deterministic baseline to escalate), `raise_conflict` short-circuit, injected-decider-only execution, and the never-invalid invariant for every non-escalate outcome, with no live model. | Local scoped verification passed: `python -m pytest tests/test_planning_actions.py tests/test_planning_validators.py tests/test_planning_tools.py tests/test_planning_loop.py -q --basetemp .pytest_tmp_m05` -> 31 passed. Exact requested done-check could not start pytest in this shell after two attempts: native `uv run pytest tests/test_planning_actions.py tests/test_planning_validators.py tests/test_planning_tools.py tests/test_planning_loop.py` failed before pytest on `.venv\lib64` Access denied; WSL attempt failed because no WSL distro is installed. Manual WSL command remains: `uv run pytest tests/test_planning_actions.py tests/test_planning_validators.py tests/test_planning_tools.py tests/test_planning_loop.py`. |
| 07.T2 | Hierarchical Planning Cascade (M05 test) | `tests/test_planning_annotations.py`, `tests/test_planning_node_support.py`, and `tests/test_pad_translation.py` - deterministic, no-network pytest coverage for the annotation compiler, planner-node support helpers, and PAD EWMA/grounded-translation pipeline. | done | Annotation tests seed a temp planning DB through the 07.00 helpers and cover inherited global/arc/chapter constraints, hard-vs-soft precedence, locked-pinned protection metadata, hard pin/remove conflicts that mark both annotations `needs_clarification` while emitting no contradictory hard constraints, and serializable revision diffs that preserve prior state. Planner-node support tests stub prompt rendering and structured output, prove structured-output failure surfaces as `PlannerDeciderError`, and cover `persist_loop_outcome` writes for finalized plus no-plan branches for clarification/escalation. PAD tests inject EWMA alpha through a synthetic config object, prove octant/neutral resolution, and prove the static and fallback translation rungs are LLM-free (`adapt_fn=None` or a local raising adapter) while still returning the static baseline. The never-persist-invalid invariant is covered by an asserted `ValueError` before any plan/revision/event write. | Local scoped verification passed: `python -m pytest tests/test_planning_annotations.py tests/test_planning_node_support.py tests/test_pad_translation.py -q --basetemp .pytest_tmp_m05_t2` -> 15 passed. Exact requested done-check could not start pytest in this shell after two attempts: native `uv run pytest tests/test_planning_annotations.py tests/test_planning_node_support.py tests/test_pad_translation.py` failed before pytest on `.venv\lib64` Access denied; WSL attempt failed because no WSL distro is installed. Manual WSL command remains: `uv run pytest tests/test_planning_annotations.py tests/test_planning_node_support.py tests/test_pad_translation.py`. |

| 07.T3 | Hierarchical Planning Cascade (M05 test) | `tests/test_planning_nodes.py` and `tests/test_planning_execution_modes.py` - deterministic, no-network pytest coverage for all five planner nodes plus macro-vs-rolling execution-mode state behavior. | done | Planner-node tests use temp SQLite/provisional/event-log fixtures built through `make_initial_state`, injected `PlannerAction` deciders, fake no-tool registries, and `adapt_fn=None` for beat PAD translation. Coverage asserts global story-node + revision persistence; arc rows, monotonic chapter stubs, and pointer advance; chapter obligations and scene-planning constraints with no Scene rows; scene `ordering=max+1` gapless persistence and pointer advance; beat EWMA from `thresholds.pad_ewma_alpha`, static LLM-free behavioural constraint persistence, revisions, pointer advance, and `runtime.beats_per_scene_min` scene-stop behavior. Invalid decider coverage proves fallback/escalation never persists the invalid marker; source inspection asserts no planner node calls `call_llm`/`call_llm_structured` directly. Execution-mode tests drive `planning_execution_mode` and `approval_mode` through state, not config: macro+approval sets `macro_outline_ready`, `awaiting_planning_approval`, and `planning_block_reason='awaiting_macro_approval'`; macro+approval-off marks ready without waiting; rolling leaves macro/approval pause false and proceeds to scene/beat just-in-time. A hard-conflict compile path sets the clarification block and writes no plan; an invalid decider with patched invalid baseline escalates and writes no plan. Macro/rolling state-setting is verified at the M05 state-snapshot layer only; graph routing/UI approval remain Build 14/Build 19. Headless safety is asserted by verifying planner nodes do not set `pause_requested` or `hard_stop_asserted` while setting approval state. The snapshot review-wait remains represented by `PlanningSnapshot.status='draft'` plus the state block reason because the current §2.7 enum has no literal `awaiting_approval` status. Audit helper scripts `scripts/audit.py` and `scripts/scan_literals.py` are absent in this checkout, so those scanner steps could not run; numeric literals in the new tests are deterministic fixture values or configured through the synthetic config object. | Local scoped verification passed: `python -m pytest tests/test_planning_nodes.py tests/test_planning_execution_modes.py -q --basetemp .pytest_tmp_m05_t3` -> 12 passed. The exact requested done-check could not start pytest after two attempts: first `uv run pytest tests/test_planning_nodes.py tests/test_planning_execution_modes.py` failed before pytest on `C:\Users\imarg\AppData\Local\uv\cache\sdists-v9\.git` Access denied; escalated retry reached CPython 3.12.12 but failed before pytest on `.venv\lib64` Access denied. Manual WSL/healthy-uv command remains: `uv run pytest tests/test_planning_nodes.py tests/test_planning_execution_modes.py`. |

| 07.T5 | Hierarchical Planning Cascade (M05 test) | `tests/test_planning_cascade_full.py` - deterministic, no-network full-cascade integration coverage for global -> arc -> chapter -> scene -> beat over real temp relational/planning/provisional/event-log stores. | done | The test patches each node module's `make_planner_decider` to inject scripted synthetic `PlannerAction` deciders while still letting the real nodes assemble their base contexts; beat runs use `adapt_fn=None`, so the PAD path stays static and LLM-free. Coverage proves global -> arc consumption of the persisted global plan rather than a placeholder; arc -> chapter consumption of chapter stubs; chapter obligations + scene-planning constraints persist with no Scene rows; macro readiness remains false after global/arc/first chapter and flips true only after the last chapter; scene planning consumes chapter constraints and creates two same-chapter scenes with gapless monotonic ordering; beat planning consumes the active scene, overwrites model PAD placeholders with the static behavioural floor, persists beats, advances `fsm_pointer.beat_id`, and honors `runtime.beats_per_scene_min` in the scene-stop signal. State/history assertions prove `planning_snapshot_id` and `active_planning_revision_id` thread forward, each node invocation writes a `PlanningRevision`, and prior history is preserved. The clarification case seeds hard pin/remove annotations on a chapter target, asserts both become `needs_clarification`, the decider is never called, no plan/revision/event/scene is persisted, `planning_block_reason='unresolved_hard_conflict'`, and the chain does not advance past the blocked chapter. The persistence replay case calls `persist_loop_outcome` directly and proves the event-log mirror is written while rerunning the same logical finalized persist keeps one `PlanningNode` and one `PlanningRevision` with the same id, so replay is safe and introduces no partial divergent canonical truth. Operator flag: `Architecture_Map.md` file lists and `Configuration_Reference.md` example config still need manual sync for `fsm/planning_node_support.py`, `fsm/pad_translation.py`, and the `planning.*` block; design docs were not edited here. Audit helper scripts `scripts/audit.py` and `scripts/scan_literals.py` are absent in this checkout, so scanner steps could not run; numeric literals in this test are deterministic fixture values or injected config values. | Local scoped verification passed: `python -m pytest tests/test_planning_cascade_full.py -q --basetemp .pytest_tmp_m05_t5` -> 3 passed. `python -m compileall -q tests/test_planning_cascade_full.py` passed; marker scan over the new test found no `TODO`/`FIXME`/`STUB`/`NotImplementedError`/network/LLM-call markers; `git diff --check -- tests/test_planning_cascade_full.py` passed. Exact requested done-check could not start pytest after two attempts: first `uv run pytest tests/test_planning_cascade_full.py` failed before pytest on `C:\Users\imarg\AppData\Local\uv\cache\sdists-v9\.git` Access denied; escalated retry reached CPython 3.12.12 but failed before pytest on `.venv\lib64` Access denied. Manual WSL/healthy-uv command remains: `uv run pytest tests/test_planning_cascade_full.py`. |

| INT-C-E | Hierarchical Planning Cascade (M05 integration) | `tests/test_planning_cascade_integration.py` - integration coverage for one planner node across real M14 config loading, M04 prompt rendering/structured PlannerAction seam, M05 bounded planner loop, real M02 planning-store persistence, and event-log mirroring. | done | Deterministic path is provider-neutral and network-free: temp relational/provisional/event-log stores are seeded through public 04.0x/07.00 helpers; constraints compile from a real PlanningAnnotation; `make_planner_decider("node_plan_global", ...)` resolves `config.endpoints.planner`, renders the real `node_plan_global` template through `PromptLoader`, and receives strict synthetic `PlannerAction` objects through an injected structured-output callable; `node_plan_global` then reaches a validator-accepted finalize and persists only validated planning structure plus a `PlanningRevision` and `planning_commit` event. Live path uses the planner endpoint convention from 05.T2: `MUSEAI_SKIP_LIVE_LLM` opts out, missing `PLANNER_API_KEY` skips, and no fake backend is installed; when it runs, the decider is `make_planner_decider` over real `call_llm_structured(PlannerAction)`, with LLM I/O log capture asserting the endpoint `api_key` does not leak. This closes Module 7 / M05 planning cascade at the automated-test integration layer. Approval-gate graph routing remains Build 14 and approval UI remains Build 19. Audit helper scripts `scripts/audit.py` and `scripts/scan_literals.py` are absent in this checkout, so scanner steps could not run; numeric literals in the test are fixture values or test call bounds, not production tunables. | `python -m py_compile tests/test_planning_cascade_integration.py` passed. Local scoped verification passed: `python -m pytest tests/test_planning_cascade_integration.py -q --basetemp .pytest_tmp_int_c_e` -> 1 passed, 1 skipped; deterministic wiring path passed end-to-end over real temp stores, live planner-call path skipped cleanly because no real `PLANNER_API_KEY` is set. Exact requested done-check could not start pytest after two attempts: `uv run pytest tests/test_planning_cascade_integration.py` failed before pytest on uv cache `sdists-v9\.git` Access denied; escalated retry reached CPython 3.12.12 but failed before pytest on `.venv\lib64` Access denied. Manual WSL/healthy-uv command remains: `uv run pytest tests/test_planning_cascade_integration.py`. |

| 08.01 | Drafting & Generation (M06) | `prompts/node_draft_prose.xml.j2` (new) — the strict Jinja2 XML template for the single-shot beat-drafting prompt, rendered by the M04 `PromptLoader` (`StrictUndefined`, `autoescape=False`). Unlike the five `node_plan_*` deliberation-loop templates, this is one-shot free-text generation — no `PlannerAction`/JSON output contract. XML sections: `role`, `drafting_objective`, `context` (canonical relational facts incl. confirmed coreference facts; flagged `epistemic_beliefs` explicitly marked unconfirmed; `summary_context`; `flavour_passages`; `temporal_context`), `macro_constraints` (approved macro constraints + compiled annotations for the active chapter, rendered conditionally — renders cleanly when empty), `beat_plan` (`beat_immediate_objective`, `beat_physical_constraints`, `active_continuity`), `affect_target` (`pad_behavioral_constraint`, computed by the planning pipeline — the model echoes/writes to it, never computes it), `continuation` (`prior_prose_tail` + `scene_entry_state`), `generation_parameters` (`target_word_count`, node-supplied, not hardcoded), and `output_contract` (prose only — no JSON, headings, outlines, or meta-commentary). A top `{# #}` header comment documents all 13 expected variables. | done | Followed `node_plan_beat.xml.j2`'s structure/tone per the increment's instruction. No Python or node logic written (template only, per Do-NOT); no provider/model names; no anti-slop instructions in the template (that is `core/antislop.py`'s job, not prompt text); the model is never asked to compute PAD values or emit structured output. Touched only `prompts/node_draft_prose.xml.j2` + this ledger row. | `prompt_loader.render("node_draft_prose", ctx)` with a full synthetic context (all layers populated, one Epistemic Belief, a populated macro-constraints block, a PAD behavioural-constraint string, a prior-prose tail) rendered successfully with the immediate objective, physical constraints, and PAD string all appearing verbatim (unique markers asserted present in the output). A second render with `macro_constraints={}` still rendered cleanly (empty-macro branch text present). Omitting a documented variable (`pad_behavioral_constraint`) raised Jinja `UndefinedError`. Ran under system `python`; `uv run` not attempted this session (no store/CLI dependency — pure Jinja2 render, interpreter-independent). |

| 08.02 | Drafting & Generation (M06) | `fsm/nodes/node_draft_prose.py` — replaced the `NotImplementedError` stub with the real Level-1 drafting node, built across 2 prompts. Prompt 1 (generation path): reads `active_context_package` from state (returns unchanged if absent — context assembly precedes drafting in the wired graph, Build 14) and the active beat's plan fields from its beat `PlanningNode` (`purpose` JSON, keyed by `{planning_snapshot_id}:beat:{fsm_pointer.beat_id}` via `sqlite_db.get_planning_nodes` — the existing 07.00 read helper, no new store code); assembles the 13-variable `node_draft_prose.xml.j2` render context (context-package layers, beat plan fields, `prior_prose_tail` from the scene's last committed beat, `scene_entry_state` reused from the beat's already-chained `entry_condition`, `target_word_count` as the scene's remaining word budget — package-derived, never hardcoded); renders via `PromptLoader`; fires the streamed call through an injectable async `call_seam` (default: real `call_llm` against `config.endpoints.drafter`, `stream=True`, `timeout_seconds` from `config.runtime.inference_timeout_seconds`) with an injectable `publisher` callable (default `None` → skipped) forwarding each chunk in order while accumulating into `state["streaming_buffer"]`; a publisher failure is swallowed (publishing stops, generation continues). Prompt 2 (anti-slop handoff + failure path): passes the completed text through `core/antislop.detect_slop`/`resolve_slop` (today's honest no-op) and writes the resolved text to `current_draft_text`; per explicit user decision (no documented semantics existed in `fsm/state.py`/`Data_Structures.md` — flagged as an operator decision in `BATCH_08_MANIFEST.md`), `streaming_buffer` clears to `""` once the draft completes; a call-seam failure logs through `core/logger.get_logger`/`log_node_event` (`outcome="failure"`) and re-raises with `current_draft_text` left untouched (never assigned before the seam succeeds) — no retries (that policy is M07/M09's). | done | `core/stream_bus.py` was not imported/implemented (publisher is an injectable callable only, matching the Operator notes' seam decision). No new config keys; routing uses the existing `endpoints.drafter`/`runtime.inference_timeout_seconds`. No store writes — drafting output is state-only until M10 commits it; verified no file under `data/` was created or modified across either path. `core/antislop.py` was not modified (still the honest M11 no-op, called unconditionally so the node needs no change when M11's internals are replaced). Touched only `fsm/nodes/node_draft_prose.py`; no `tests/test_node_draft_prose.py` added (the dedicated `08.T1` increment owns automated coverage, matching the M05 planner-node/test-file split precedent). | Exact `uv run python -c "from fsm.nodes.node_draft_prose import node_draft_prose; print(node_draft_prose)"` blocked by the documented Windows `.venv\lib64` permission error (`Access is denied`, reproduced this session); the identical import printed the function under system `python`. Prompt-1 Micro-check (temp SQLite store with a real beat `PlanningNode`, synthetic config, fake 3-chunk async seam): node rendered the template without error, `streaming_buffer` ended `"ABC"`, an injected recording publisher received `["A","B","C"]` in order, and omitting the publisher left the outcome otherwise identical. Prompt-2 Done-check (same fixture shape): happy path wrote the anti-slop-resolved (verbatim today) text to `current_draft_text`, cleared `streaming_buffer` to `""`, and preserved publisher chunk ordering; a seam raising mid-stream (`RuntimeError` after one partial chunk) surfaced the exception, left `current_draft_text` at its pre-call sentinel value (unchanged), and logged one `outcome="failure"` JSON line to `logs/fsm.log`; `data/` directory contents were identical before and after both runs (`ls` diff empty). No focused test file was added, so no `pytest` run was required for the done-check. |

| 08.03 | Drafting & Generation (M06, test tooling) | `tests/frontends/m08_drafting.py` (new) — Streamlit visualiser for the real drafting node, built across 2 prompts. Prompt 1: drives `build_context_package` then `node_draft_prose` end to end over a temp workspace; the page never re-implements node logic — it observes the production render by capturing the `messages` argument its own injected `call_seam` receives, and re-runs `detect_slop`/`resolve_slop` against that captured text for display. Default path is a fully offline **scripted stream** (editable text, chunked whitespace-bounded so `"".join(chunks) == text` exactly) with an **inject failing seam** checkbox (raises after ~half the chunks — demonstrates the clean-failure path) and an explicit **Live drafter endpoint** toggle (off by default; `call_seam=None` so the node's own real default seam calls `config.endpoints.drafter` with `.env` secrets; any exception warns and falls back to the scripted stream). Output runbook: assembled context package (token metrics + `shared.display_package_layers`), rendered prompt + live chunk feed, and the anti-slop step + final `current_draft_text`/`streaming_buffer` state. Additive `tests/frontends/shared.py` extension: `seed_drafting_beat_plan(db_path)` seeds one planned-but-undrafted beat (`scene-1_b3`, continuing the narrative seed's committed `beat-0`/`beat-1`/`beat-2`) with a full plan + hand-authored PAD behavioural-constraint string, via the real `upsert_beat_plan`/`upsert_planning_node` 07.00 helpers (mirrors `node_plan_beat`'s own two-write persist sequence) — needed because `seed_planning_data` only seeds through the scene level. Prompt 2: registered `"M08 Drafting": m08_drafting.render` in `run_all.py`'s `PAGES` (now seven visualizers) and added the **M08 — Drafting & Generation** section to `STREAMLIT_FRONTENDS.md` (ToC, layer diagram incl. `llm/call_llm.py`/`core/antislop.py`, launch list, per-module reference table, edge-cases list, `Last updated` → 2026-07-03). | done | **This is an interactive visual debugger, not an automated test** — codified pytest coverage for the drafting node is the separate `08.T1` increment (Next up). No production module was modified — `fsm/nodes/node_draft_prose.py` and `core/antislop.py` are exercised exactly as built in `08.01`/`08.02`, only through the documented `call_seam`/`publisher` injection points. The default offline path never imports or calls `llm/call_llm.py`; it is reached only when the Live toggle is explicitly on. `shared.py`'s change is additive-only (one new function + two module-level constants); no existing frontend's behavior changed (regression-smoke-checked below). | Both prompts verified headless via `streamlit.testing.v1.AppTest` (the same method the `VIS-03`/M07 visualizer used) — `uv run streamlit run ...` itself is blocked by the documented Windows `.venv\lib64` permission error, reproduced this session; streamlit was installed under system `python` for verification only (no `pyproject.toml`/`uv.lock` change). **Prompt-1 Micro-check:** seeded, ran a draft with the default 3+ chunk scripted stream — chunk feed matched arrival order, `detect_slop` returned `[]` with `resolved == raw`, and `current_draft_text` equaled the exact joined stream (round-trip verified); enabling the failing-seam checkbox then running surfaced the `RuntimeError`, left `current_draft_text` unchanged (`""`), and showed the partial `streaming_buffer`. (One false failure during verification — `FileNotFoundError: config.yaml` — was traced to a pre-existing cwd-relative fragility in `core/logger.py`'s `_CONFIG_PATH` constant, reproducible only when the verification script's cwd differed from the repo root the real launch command uses; not a defect in this increment's files, out of scope to fix here.) **Prompt-2 Done-check:** `AppTest` over `run_all.py` — sidebar lists all seven pages ending in `M08 Drafting`; selecting it renders the drafting visualiser (its three buttons present, no exception). Regression smoke-check: M02/M05/M06/M07 pages still render exception-free through `run_all.py` after the `shared.py`/`run_all.py` changes. |

| 08.T1 | Drafting & Generation (M06 test) | `tests/test_node_draft_prose.py` (new) — deterministic, no-network pytest coverage for `fsm/nodes/node_draft_prose.py`, 8 tests. Covers: the assembled render context includes the beat's `immediate_objective`, a `physical_constraint`, and the PAD `behavioral_constraint` string in the rendered prompt (captured via the injected `call_seam`'s `messages` argument); the template's documented-variable contract raises `jinja2.UndefinedError` (via a direct `PromptLoader().render` call) when a required context key is omitted; a scripted seam's chunks accumulate into `streaming_buffer` in order and an injected recording publisher receives every chunk in the same order; omitting the publisher leaves the outcome otherwise identical; the anti-slop handoff is proven by invocation (spy wrappers around `detect_slop`/`resolve_slop` delegating to the real no-op functions, asserting call arguments and that `current_draft_text` equals the spy-recorded `resolve_slop` return) rather than asserting slop behavior; a call seam raising mid-stream surfaces the exception, leaves `current_draft_text` at its pre-call sentinel value, and leaves the seeded SQLite file's bytes and the temp directory's file listing unchanged; a publisher that raises does not lose the draft (generation and accumulation still complete); and the missing-`active_context_package` guard returns the identical state object without invoking the seam. | done | Pure synthetic fixtures: one `tmp_path` SQLite file (`init_db` + `create_planning_snapshot` + one `upsert_planning_node` beat row, `parent_id=None`) is the only store touched — no real `data/` writes. `app_config` is an injected `SimpleNamespace` (matches the `m08_drafting.py` frontend's offline-mode convention), so the node's own config resolution never needs real endpoint secrets. The one exception is the call-seam-failure test: `node_draft_prose`'s failure branch calls the real `core/logger.get_logger`/`log_node_event`, which loads `config.yaml` for real — that test injects placeholder `*_API_KEY` env vars for all five endpoints (mirrors `tests/test_llm_config_integration.py`'s `live_endpoint` fixture convention) so config validation succeeds; this is the first test in the suite to exercise that failure-logging branch, and it writes one real line to `logs/fsm.log` (gitignored, not a "real store" write under `data/`). No production module (`fsm/nodes/node_draft_prose.py`, `core/antislop.py`) was modified. Touched only `tests/test_node_draft_prose.py` + `PROGRESS_LEDGER.md`. | `python -m pytest tests/test_node_draft_prose.py -q --basetemp .pytest_tmp_node_draft` → **8 passed** (system `python`). Full suite `python -m pytest -q --basetemp .pytest_tmp_all_08T1` → **236 passed, 1 skipped** (the skip is the pre-existing live-LLM endpoint test, unrelated to this increment). `uv run pytest ...` could not start pytest in this shell: it failed before pytest with the documented Windows `.venv\lib64` `Access is denied` error (same known issue as 07.T1–INT-C-E). A WSL2 `uv run pytest tests/test_node_draft_prose.py -q` rerun is recommended to get the project's authoritative green (per the WSL2-run convention used from 04.T1 onward) but was not available in this session. |

| INT-D-F | Drafting & Generation (M06) x Context Assembly & Budgeting (M03) integration | `tests/test_drafting_context_integration.py` (new) — 2 tests proving the real M03 -> M06 wiring. **Deterministic wiring path (always runs):** seeds a real relational hub (`Arcs`/`Chapters`/`Scenes` via raw SQL — no public write helper owns those tables yet — plus one committed beat via the real `upsert_beat_commit`) and a real planned-but-undrafted beat PlanningNode via the same two-write sequence `node_plan_beat` uses (`upsert_beat_plan` for the structural `Beats` row + minimal PAD detail, then a second `upsert_planning_node` on the same `node_id` upgrading `purpose` to the full plan JSON with `entry_condition`/`exit_condition`/`asserted_facts`/`behavioral_constraint` — the fields `node_draft_prose` actually reads); runs the real `node_assemble_context` over those stores with a `httpx.AsyncClient`/`call_llm` guard proving no inference call occurs, then feeds the resulting `active_context_package` to the real `node_draft_prose` with a fake streaming seam. Asserts the prior committed beat's prose and the beat plan's `immediate_objective`/`physical_constraint`/`behavioral_constraint` (as punctuation-free marker tokens, decoupled from the template's `xmlsafe` escaping) reach the rendered prompt; `meta.temporal`/`meta.flavour` stay `available: False` with the documented "not yet implemented" reason (M03's degrade behavior unchanged); `meta.pruned_layers == []` under the real budget; `current_draft_text` is written; and the seeded SQLite file's bytes are byte-identical before/after drafting (no store gained a row). **Live drafter path:** following the `05.T2`/`INT-C-E` convention (`MUSEAI_SKIP_LIVE_LLM` opt-out; missing `DRAFTER_API_KEY` skips with a clear reason; a reachable-but-failing call is also caught and skipped, never faked), runs the same seeded pipeline through the node's own real default seam and asserts only structure (non-empty `current_draft_text`, at least one streamed chunk, and an `llm_io` log record for the call that never contains the endpoint's `api_key`) — never exact model wording. | done | This checkpoint ends at `current_draft_text`; no M07 auditing or M10 commit behavior is exercised. No production module modified. Touched only `tests/test_drafting_context_integration.py` + this ledger row. **Closes Module 8 / M06** at the automated-integration layer (M06's unit coverage is `08.T1`; this is the M03-into-M06 wiring proof). Seed helpers are local/self-contained (not imported from `tests/test_context_memory_integration.py` or `tests/frontends/shared.py`, the latter of which pulls in `streamlit` — out of scope for a pytest integration file) per this repo's per-file-fixture convention. **Order-dependent observation, recorded for honesty, not as a passing claim:** `tests/test_server_connections.py` sets a placeholder `DRAFTER_API_KEY` (and siblings) via `os.environ.setdefault(...)` at collection time (not `monkeypatch`, so it is not endpoint-scoped or reverted); in this dev environment a local drafter endpoint (`http://localhost:11434/v1`) is genuinely reachable, so when the full suite runs, the live test's "has a key" check sees that placeholder, proceeds, and the live drafter call succeeds for real against the reachable local server — it is not skipped in that context. This is the intended "configured and reachable" behavior, not a faked pass; the placeholder key is not a real credential, and the canonical done-check below (this file run alone, no cross-test leakage) reports the live test as an explicit **SKIP**, per this increment's own instruction to never record a live skip as a pass. | `tests/test_drafting_context_integration.py` run alone (system `python`, canonical done-check): **1 passed, 1 skipped** — `test_real_context_assembly_feeds_real_drafting_node` passed; `test_live_drafter_over_real_context_or_skip` **SKIPPED**: "no real DRAFTER_API_KEY configured — live drafter path unavailable". Full suite: **238 passed, 1 skipped** (the one skip is `test_planning_cascade_integration.py`'s pre-existing live-planner path, unrelated to this increment). Separately observed (not part of the canonical done-check, recorded for transparency): running `tests/test_server_connections.py` immediately before this file in the same process causes its leaked placeholder `DRAFTER_API_KEY` to make the live drafter test run for real against the reachable local endpoint and pass — see the Notes column. `uv run pytest ...` could not start pytest in this shell: it failed before pytest with the documented Windows `.venv\lib64` `Access is denied` error (same known issue as 07.T1 onward). A WSL2 `uv run pytest tests/test_drafting_context_integration.py -q` rerun is recommended for the project's authoritative green but was not available in this session. |

| 09.01 | Anti-slop Detection & Correction (M11) | Formalized the frozen M11 contract in `core/antislop.py`: strict Pydantic v2 `SlopFlag` model (`extra='forbid'`; `offending_text` verbatim span, `reason`, optional `suggested_replacement`) and precisely typed `detect_slop(text: str) -> list[SlopFlag]` / `resolve_slop(text: str, findings: list[SlopFlag] \| None = None) -> str`; module docstring rewritten honestly (contract frozen, detection has no viable approach yet per `Open_Problems.md`, passthrough deliberate). Added `supports_inference_antislop: bool = False` to `EndpointConfig` in `core/config_loader.py` and `supports_inference_antislop: false` (documented-provisional, read by no logic) to all five endpoint blocks in `config.yaml`. | done | Behavior is still the exact no-op: `detect_slop` returns `[]`, `resolve_slop` returns input unchanged regardless of `findings`. No detection heuristic added. `node_draft_prose.py` (08.02) was not touched — the frozen signatures are drop-in; it still imports and calls `detect_slop`/`resolve_slop` unconditionally. The new config key is unread by any logic and gates nothing. Touched only `core/antislop.py`, `core/config_loader.py`, `config.yaml`, and this ledger. | `python -c "from core.antislop import SlopFlag, detect_slop, resolve_slop; ..."` → printed `contract holds` (system `python`; `uv run` blocked by the documented Windows `.venv\lib64` `Access is denied` error). `python -m pytest tests/test_config.py -q --basetemp .pytest_tmp_config` → **9 passed**. Full suite `python -m pytest -q --basetemp .pytest_tmp_all` → **238 passed, 1 skipped** (pre-existing unrelated live-endpoint skip). As with prior rows, a WSL2 `uv run pytest ...` rerun is recommended for the project's authoritative green but was not available in this session. |

| 09.T1 | Anti-slop Detection & Correction (M11 test) | `tests/test_antislop.py` (new, 13 tests) — deterministic, offline contract tests pinning the frozen M11 interface: `detect_slop` returns `[]` for empty/short/long/cliché-dense text (pinning the no-op, not a clean-text claim); `resolve_slop(t)`/`resolve_slop(t, [])`/`resolve_slop(t, [valid SlopFlag])` all return `t` unchanged (passthrough ignores findings by design); `SlopFlag` rejects unknown keys (`extra='forbid'`) and requires `offending_text`, with no integer-offset fields; the functions accept partial/truncated text indifferently; and an import-level test confirms `fsm.nodes.node_draft_prose` imports the same `detect_slop`/`resolve_slop` symbols from `core.antislop` (cycle-free `core/` placement holds). | done | Explicitly not detection tests — no assertion that any text gets flagged, no xfail placeholders; when real detection lands it brings its own suite. `core/antislop.py` and no other production module was modified. Touched only `tests/test_antislop.py` + this ledger row. **Closes Module 9 / M11 as a frozen interface** — detection itself remains open research per `Open_Problems.md`. | `python -m pytest tests/test_antislop.py -q --basetemp .pytest_tmp_antislop` → **13 passed** (system `python`; `uv run` blocked by the documented Windows `.venv\lib64` `Access is denied` error, reproduced this session). Full suite `python -m pytest -q --basetemp .pytest_tmp_all2` → **251 passed, 1 skipped** (pre-existing unrelated live-endpoint skip). A WSL2 `uv run pytest tests/test_antislop.py -q` rerun is recommended for the project's authoritative green but was not available in this session. |

| 10.01 | Quality Gauntlet (M07) | `fsm/nodes/node_programmatic_audit.py` — replaced the `NotImplementedError` stub with the real model-free Stage-1 audit. `async node_programmatic_audit(state, *, distance_provider=None) -> state`: (1) structural checks — `EMPTY_DRAFT` when the draft is blank (short-circuits the other structural checks); an odd count of `"` → `UNCLOSED_QUOTE`; an odd count of `**` or `` ` `` → `UNCLOSED_MARKDOWN` — each its own `FailureObject` with a verbatim tail-slice `offending_text`; (2) passive-voice check, density-based — sentences split on a `.`/`!`/`?` boundary regex (exact substrings of the original text, never re-stripped beyond the kept slice), each classified passive by a documented "be-verb + participle" heuristic (regular `-ed` or a listed irregular participle); **one** aggregate `FailureObject` fires only when `passive_count/total_sentences` strictly exceeds `config.thresholds.passive_voice_density` (reused key, no new config) — a single passive sentence in an otherwise-active beat never fires; (3) `best_seen_draft` fewer-failures retention — because `OrchestratorState` has no field to cache a prior failure count, the node recomputes the same pure checks against the currently-stored `best_seen_draft` text on demand and overwrites only on a strict improvement (or when `best_seen_draft is None`); in-memory only, never persisted; (4) `stylometric_distance` via an injectable `distance_provider` seam — default returns the stubbed `0.0` and logs (`core.logger.get_logger`, a structured JSON note) that the metric is stubbed pending `memory/style_store.py`, per the design's own Sprint-3 note that $D_c$ gating runs against a stubbed value while routing stays real; `memory/style_store.py` itself was not read. `critic_failures` receives only this call's delta (`state["critic_failures"] = new_failures`, never `.append`d onto the existing list reference) per the `accumulate_or_reset` reducer contract — clearing/history-merge is the graph's/`node_revise_prose`'s job, not this node's. Pure Python: no LLM, no network, no store writes, no routing (that is `10.02`). | done | No new `config.yaml` keys — only the existing `thresholds.passive_voice_density` is read. The passive/structural heuristics are explicitly documented as heuristics, not a grammatical parser or STEL/Burrows math. Touched only `fsm/nodes/node_programmatic_audit.py` + this ledger row; `fsm/state.py` was not modified (no new state field was needed — the best-seen comparison is recomputed instead of cached). No `tests/test_quality_gauntlet.py` exists yet (matches the M05/M06 precedent of a later dedicated `.T` test increment), so no focused pytest run was required for the done-check. | Manual synthetic-draft verification (system `python`, injected `SimpleNamespace` config with `passive_voice_density=0.25`, `distance_provider=None`): a 4-sentence active-voice draft → 0 failures, `best_seen_draft == current_draft_text`, `stylometric_distance == 0.0`; a majority-passive draft → exactly 1 `PASSIVE_VOICE_DENSITY` failure; passive density exactly at 1/4=0.25 (at threshold) → 0 failures, 2/4=0.5 (above threshold) → exactly 1 failure (confirms the strict `>` boundary); an empty draft → exactly 1 `EMPTY_DRAFT` failure; an unclosed-quote draft → exactly 1 `UNCLOSED_QUOTE` failure with a verbatim `offending_text` tail; a worse redraft over a better `best_seen_draft` left it unchanged. Full suite `python -m pytest -q --basetemp .pytest_tmp_all2` → **251 passed, 1 skipped** (identical count to `09.T1`'s baseline — no test file added, no regression; the one skip is the pre-existing unrelated live-endpoint test). `uv run pytest` blocked by the documented Windows `.venv\lib64` `Access is denied` error, reproduced this session. |

*(Append a row per increment. Keep "Current position" accurate - it is read first each session.)*

## Stub inventory (deliberate no-ops, wired later)
- Store init signatures called by `init_resources()` - the SQLite relational init is now **real** (`init_db`, wired in INT-A·B); the rest (Graphiti, RAPTOR, Chroma, style stores, snapshot archive) remain no-op placeholders until their modules land. The event-log init only touches its `.jsonl` artifact.
- `fsm/nodes/node_plan_chapter.py::_recent_chapter_summaries()` — returns `[]` (documented) until the RAPTOR summarization pipeline lands and defines a chapter-summary level convention to read; the P1 template renders its empty-state branch meanwhile. Wired by the RAPTOR build (M02/M09 increments).
- `fsm/nodes/node_plan_scene.py::_continuity_facts()` — returns `[]` (documented) until the Graphiti temporal-fact store is real; the scene template renders its empty-state branch and the `continuity` validator passes trivially on zero facts. Wired by the Graphiti build (M02 increments).
- `fsm/nodes/node_plan_beat.py::_continuity_facts()` — same documented `[]` degrade as the scene planner's, wired by the same Graphiti build.
- `fsm/nodes/node_plan_beat.py` PAD adaptation rung — the node passes `adapt_fn` through verbatim and defaults to the LLM-free static floor; wiring the default small-tier adapter (`fsm.pad_translation.build_pad_adapt_fn`) awaits its `prompts/node_plan_beat_pad.xml.j2` template (recorded in Next up).
- `scan_startup_crash_sentinel()` - no-op until CommitIntent records and crash recovery land (13).
- `core.antislop.detect_slop()` and `core.antislop.resolve_slop()` - no-op passthroughs behind a now-typed frozen contract (`SlopFlag`, `09.01`) until anti-slop implementation.
- `memory.graphiti_client._apply_event()` - intentional early no-op for graph writes during crash-recovery scaffolding.

### Remaining M02 deferred store stubs (not yet implemented)
- `memory.graphiti_client._apply_event()` - temporal knowledge-graph edge writes / crash-replay application (§2.2).
- `memory.raptor.cluster_scenes()` - documented placeholder returning scene texts unmodified; real embed→similarity→Leiden→summarize pipeline pending (§2.3).
- `memory.chroma_client` - associative flavour HNSW vector index (§2.6).
- `memory.style_store` - frozen baseline + rolling-EMA stylometric profiles, author + per-character (§2.5).
